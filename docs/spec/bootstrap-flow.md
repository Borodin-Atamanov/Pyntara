# Bootstrap flow

Bootstrap is fully specified by the mandatory contract:
`docs/contracts/bootstrap.md`

The runtime interactive flow is specified by:
`docs/contracts/interactive-ui.md`

This file exists as a spec index entry. No content should duplicate the contracts.
