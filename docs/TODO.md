# TODO

Planned future work. После реализации - удаляем из этого файла.

## Recoverable failure handling for every task

A task must apply whatever it can and must not stop because of one bad step or one crashing external tool: the goal is a configured target system, so a setting that failed must never stop a setting that does not depend on it, and a task must do its configuration instead of falling at every external hiccup. The engine contract already fixes that a recoverable failure is never fatal (docs/contracts/architecture.md, Task contract; docs/contracts/task-model.md): the failure is reported in warnings, the task completes, the runner converts an error or raising task as a safety net, and the entry point exits nonzero. The remaining gap lives inside the task modules: several still wrap their whole apply phase in one try/except or return an error result, so the first failure drops the rest of the machine configuration on the target. Extend the policy to every task module.

The universal rules a task module must follow:

Apply the configured values one independent step at a time and guard each step. Catch the subprocess failures (a bad return code, a timeout) and the environment errors (OSError) raised inside a step, log the failed step as a progress line, collect it into the result warnings and continue with the next step.

Guard each value in a loop over independent records. One failing record is reported and the remaining records still apply.

Write the persistent value into its config file before the live effect. A live mechanism that crashes loses only the live switch, never the persistent value, which applies at the next login.

Degrade without a live session. Live-only mechanisms (GUI apply tools, a reload, a live DBus change, notify signals) run only when a desktop session is reachable; without one the written value applies after the next login.

Stop the own remaining steps only when the mechanism of the whole task is missing (its provider package cannot be installed), and report even that as a done result with warnings, never as an error result.

Never raise out of task(ctx) and never return an error result for a recoverable failure.

Completed exemplars to follow: kde_settings applies the policy step by step and per kconfig record (commit 79e0b48), kde_keyboard_setup guards each write and returns done with warnings. Audit every task module against the rules, add unit tests proving that a failing step keeps the independent steps running, and keep the full suite green.

Done 2026-09-13, the hundred-and-third block, the audit this paragraph asks for, and with it the front. Every task module was measured by its calls (run_command and the shared installers) against its guards (except clauses and collected warnings) and then read where the ratio was thin: cli_tools had no guard of its own because it delegates to the shared installer, chrome_setup and system_metrics_setup guard their steps in helper functions, and rustdesk_setup, telegram_setup, swapfile_service_install, port_forwarding_setup, playwright_setup, i2pd_service_setup, kde_settings, kde_keyboard_setup, ssh_daemon_setup, ssh_client_setup, tor_setup, zram_service, zswap_service, yggdrasil_service_setup and three_x_ui_xray_setup all guard each step and collect the reason into warnings. The audit found two gaps, both fixed: cli_tools reported a package that could not be installed in the error field of a successful result, so the run's warning count and its nonzero exit missed it (block hundred-and-one), and vocalinux_setup called its write helpers without a guard, so one unwritable user file dropped the files after it and the shortcut (block hundred-and-two). One module is deliberately one transaction: dnsproxy_setup wraps its cutover in a single try whose except reverts the resolver drop-in, the systemd-resolved restart and the NetworkManager sweep, because those steps are not independent, a half cut-over resolver would break DNS on the machine, and the spec of the task states the revert as the design; the mechanism-missing case (the binary cannot be downloaded) stops the task there and is reported as done with warnings, which the rules allow. The front is held by tests/test_runner.py::test_task_modules_report_findings_in_warnings (no module builds an error result) and by test_run_reports_warnings_and_exits_one in tests/test_entry.py (a warning makes the run name the task and exit nonzero), and both passed unchanged through this audit.

## REPO_ROOT duplication in task modules

Done 2026-09-10: REPO_ROOT was computed once in src/pyntara/utils.py and imported by every module that reads a shipped file: dnsproxy_setup, ffmpeg_setup, i2pd_service_setup, imagemagick_setup, kde_settings, local_vault_setup, port_forwarding_setup, ssh_daemon_setup, swapfile_service_install, system_metrics_setup, vocalinux_setup, zram_service and zswap_service. The copy in nextdns_setup_system_wide was dead: the module defined it and read it nowhere, so it was deleted instead of imported.

Corrected 2026-09-13: the paragraph that stood here said the deeper improvement was optional and still open, and it stated that the constant is computed in utils.py, which is false today. The improvement was done on 2026-09-11 (Context gained repo_root and every task reads the root from its context) and the single computation then moved out of utils.py into the composition root (2026-09-12), where it is Path(__file__).resolve().parents[2] of src/pyntara/pyntara.py, corrected from parents[1] on 2026-09-13 after a live run proved the shallow depth wrong. The current state: utils.py holds no REPO_ROOT, no module under src/pyntara/tasks/ imports the constant, the composition root fills Context.repo_root, and tests/test_utils.py refuses both a second computation and an import of the constant outside pyntara.py. The standalone secrets/ scripts (read_google_script_credentials.py, regenerate_vault_by_config.py) stay outside the engine flow and keep a local definition.

## Fresh install log findings

Findings from the real fresh-install run captured in gitignore/pyntara_install_real_issues.log (2026-08-26). Each item names the task, the observed line and the place in the code to investigate; reproduce on the target machine and verify the implied goal live.

ca-certificates: 25 trusted certificates were removed during the install. The apt upgrade that three_x_ui_xray_setup triggers updated ca-certificates, and the update removed 25 certificates. Seen in gitignore/pyntara_install_real_issues.log: 'rehash: warning: skipping ca-certificates.crt, it does not contain exactly one certificate or CRL' and '2 added, 25 removed; done.'. Verify on the machine where the install ran whether /etc/ssl/certs now misses certificates and whether HTTPS verification still works; a removal of 25 certificates is abnormal for a plain upgrade and may break TLS trust. Determine whether the engine or the official 3x-ui installer caused it and record the finding; if the engine is the cause, prevent it. Исследовать. Может быть не критично.

three_x_ui_xray_setup: the repository venv python leaks into the official installer. Seen in gitignore/pyntara_install_real_issues.log: '/var/cache/pyntara/repo/.venv/bin/python3: No module named pip' during the installer's fail2ban step. The engine runs under uv run, which prepends .venv/bin to PATH, so python3 inside the official installer resolves to the uv venv python that has no pip. Make _run_installer in src/pyntara/tasks/three_x_ui_xray_setup.py run the external installer with a clean environment, for example a PATH without the repo venv, so it uses the system python3. Reproduce the line first, then confirm the installer no longer sees the venv python.

Logging: decide a consistent ordering for task results. A failed mechanism is reported as '[done] ... completed with warnings' right after the same error text, and the same failure text is then repeated as a '[warn]' line, seen in gitignore/pyntara_install_real_issues.log for dnsproxy_setup. The summary 'Finished 19 of 19 tasks, 3 with warnings: ...' is immediately followed by exit code 1 without stating that the warnings caused the nonzero exit and without separating a real failure (dnsproxy) from cosmetic warnings (kde_keyboard_setup hotkey). Print the failed task reason once, never mark a failure as done, and make the final line state the cause of a nonzero exit. Финальные результаты в пинтары писать с двойным разделителем строки для каждого таск. Внизу - статистику целиком, результат работы пинтары в целом.

i2pd_service_setup: make the service work and write the SSH tunnel address into the log. Today the task finishes with 'SSH tunnel address appears after the first start' and no address is saved, seen in gitignore/pyntara_install_real_issues.log together with the check line 'checking saved address file /var/lib/pyntara/i2pd_ssh_address: missing or stale'. The task waits for the service to become active but not for the address to appear, unlike tor_setup which waits and writes the address file. Make the task wait for the address to appear within a bounded number of checks, write it to /var/lib/pyntara/i2pd_ssh_address and print it in the [done] line. Да, адрес - надо показывать в логах.

Done: dnsproxy_setup: DNS bypasses dnsproxy after installation. On the fresh desktop install the active NetworkManager connection keeps static provider DNS on the per-link scope, so the system would bypass dnsproxy. Seen in gitignore/pyntara_install_real_issues.log: 'dnsproxy setup failed: per-link DNS still lists provider resolver(s) 2800:810:100:1:200:115:192:29, 2800:810:100::15, 2800:810:100::9, 810:100::15; the system would bypass dnsproxy'. The task keeps the resolver drop-in and the dnsproxy service and reports a warning, but DNS does not fully go through dnsproxy. The failing check is _verify_system in src/pyntara/tasks/dnsproxy_setup.py, which returns an error when nm_managed and leftover provider addresses remain on a per-link scope. Reproduce on the machine where the install ran, find how the static IPv6 provider DNS ends up in the active connection, and make the task remove static per-link DNS on the connections it manages, or fail the task outright instead of leaving a partial state. The implied goal is that every DNS query on the machine goes through dnsproxy. Хуже всего, что после установки DNS вообще не доступен, ни какой сайт не открывается. Исследовать. Повторить. Спланировать, как исправить. (fixed 2026-08-26: the per-link-leftover check was a false positive; systemd-resolved routing sends every query to the global scope when the drop-in sets the wildcard routing domain ~., so surviving per-link DNS without a routing domain does not bypass dnsproxy. _verify_system now proves routing through the Global block of resolvectl status, leftover per-link DNS is a warning, and address matching is per whole token.)

Done: kde_keyboard_setup: the per-layout hotkey is not applied. The task sets the hotkey, seen in gitignore/pyntara_install_real_issues.log: 'set hotkey Switch keyboard layout to Spanish: Meta+Q', but the kglobalaccel daemon reply after applying is an empty list: '[warn] kde_keyboard_setup: cannot apply hotkey Switch keyboard layout to Spanish: daemon reports []'. The failure is reported at src/pyntara/tasks/kde_keyboard_setup.py line 403. This is an important problem: the keyboard layout switching hotkey is a core part of the KDE setup. Reproduce on a KDE Wayland machine, find why the assignment does not stick (daemon not ready, wrong component_unique, wrong shortcut format), make the hotkey actually apply, and make the task report the applied value instead of an empty list. Критично! Не работает переключение языков после свежей установки! Ни на английски, ни на русский, ни испанский. Исследовать. Решить как исправить.

Done: system_metrics_setup: the deployment venv is not pinned to uv.lock. _ensure_venv in src/pyntara/tasks/system_metrics_setup.py installs pyntara into /usr/local/lib/pyntara/venv with 'uv pip install --python <venv_python> <REPO_ROOT>' and no --locked, so resolved versions drift from the lockfile. Seen in gitignore/pyntara_install_real_issues.log: the repo venv got argon2-cffi-bindings==25.1.0, cffi==2.1.0, pykeepass==4.1.1.post1, while the system venv got argon2-cffi-bindings==26.1.0, cffi==2.1.1, pykeepass==4.2.0 and extra importlib-metadata and zipp. Make the deployment venv install from the lockfile so the deployed services run the same versions as the repository. (fixed 2026-08-26: _ensure_venv now installs the deployment venv with uv sync --project REPO_ROOT --active --locked --no-dev --no-editable, so the deployed services run the exact locked versions; --reinstall-package pyntara is added on force and on a stale-venv refresh, because uv sync does not rebuild a local project whose lockfile entry carries no version. Verified live on hujim-tutav: the deployed venv now reports argon2-cffi-bindings 25.1.0, cffi 2.1.0, pykeepass 4.1.1.post1 and no extra importlib-metadata or zipp.)

Done: inst.sh: the first apt attempt always fails on a fresh system. Seen in gitignore/pyntara_install_real_issues.log: 'E: Package 'python3-venv' has no installation candidate' then 'First install attempt failed, refreshing package index'. The retry after apt-get update works, but on a fresh machine the first attempt is a guaranteed failure and adds about 50 seconds. Consider refreshing the apt index before the first install on a fresh system, or reword the message so an expected retry is not reported as failed. Исследовать. Предложить план исправления на утверждение.

Done: Logging: the KDE tasks must log their detailed actions. kde_keyboard_setup and kde_settings currently write too little about what they do. The hotkey warning shows only 'daemon reports []' with no expected value and no shortcut (src/pyntara/tasks/kde_keyboard_setup.py line 403), and kde_settings writes conflicting keys without explanation, seen in gitignore/pyntara_install_real_issues.log: PowerProfile set to performance then power-saver, LidAction set three times. Detail the KDE tasks' own progress: log each setting with its target value, log the applied result, and add a final verification line showing the resulting state. Важно не показывать ключи, куда мы пишем, например, разные версии энергосбережения, для разных режимов: через батарейку, от сети, мало энергии. Аналогично по всем другим действиям - полностью показываем, что делаем, полные пути к ключам.

## File mode constants inside task modules

Done 2026-09-10. The rule that every value lives in config/ (architecture contract, Configuration) was broken by file modes inside task modules: vocalinux_setup (USER_FILE_MODE = "0644", EXECUTABLE_MODE = "0755"), ffmpeg_setup (WAYRECORD_MODE = 0o755), telegram_setup (LAUNCHER_MODE = 0o644, ICON_MODE = 0o644, EXECUTABLE_MODE = 0o755). chrome_setup was fixed on 2026-09-10 (commit 6e2a3ce), ffmpeg_setup, telegram_setup and vocalinux_setup in commit c5fb4e0: each mode lives in its own section as a readable string of four octal digits (file_mode = "0644"), the runtime reader turns it into the int chmod expects, and the checks demand the readable form.

## Values outside config/ are not classified by a written rule

The rule is written in docs/contracts/architecture.md (Configuration) since 2026-09-10, and the closed list of types is docs/spec/config-content.md, one click from the README index: a value of a listed type is a config value, a value of an exception type stays in code, and a value that fits no type is a config value. The reason is manageability and understanding, not mutability, so a value goes into config/ whether or not it ever changes; a given value is written once in config/ and read where it is needed, a computed value is computed once in a shared place and imported.

Refined 2026-09-10: the closed list of types is now docs/spec/config-content.md, one click from the README index, and the user approved it. Values of the listed types go into the config with no exception: paths of files and directories, unit names, generated names, journal identifiers, paths of foreign programs and of external API interfaces, durations, counts, sizes, percents, ports, syslog priorities, file modes, tool parameters, numbers that describe the machine or the run, command argv, URLs, hosts and addresses, repository owner/name pairs, tool vocabularies, file text up to five lines, stored titles and descriptions, shortcuts, account names, site addresses, package and component and host lists, lists of records, mappings. The Exceptions section of the same document holds the four types that stay in code and one that lives as files: kernel and device paths, regular expressions, the wording of a message, file bodies longer than five lines which live under task_data/, and numbers that encode a protocol or an algorithm. Each of the two batches below is the mechanical work that follows from that list.

The audit of the 36 modules outside src/pyntara/config/ counted 51 value candidates, 43 strings that carry a judgement or a message, 43 regular expressions, 9 algorithm tables and 14 copies of REPO_ROOT.

Done 2026-09-10: SYSTEMD_UNIT_DIR, the same /etc/systemd/system path in six task modules (zram_service, swapfile_service_install, zswap_service, system_metrics_setup, system_metrics_initial_collect, port_forwarding_setup), is one [engine] value systemd_unit_dir read through ctx.config.engine.systemd_unit_dir.

Done 2026-09-10: the identifier under which the engine mirrors its own messages into the journal is one [engine] value journal_identifier, which the composition root exports into PYNTARA_JOURNAL_IDENTIFIER before the first message, and src/pyntara/logger.py no longer invents the name pyntara-engine when the variable is unset: an absent or empty value forwards nothing. inst.sh keeps its contract identifier pyntara-install and falls back to it instead of the meaningless default it used before, so the two identifiers stay distinct in the journal.

Done 2026-09-11, first stage of one block: the clone root travels in the Context (user decision). Context gained the field repo_root, the composition root fills it from the single computation utils.REPO_ROOT, and utils gained task_data_dir(repo_root, section), the one place that knows the task_data/<section> layout. Four tasks read the root from their context and no longer import the constant: zram_service, zswap_service, swapfile_service_install and i2pd_service_setup, whose render helpers now take the template path (for i2pd, _render_config(cfg, template_path), _render_tunnels_config(cfg, ssh_port, template_path), _write_config(cfg, template_path), _write_tunnels_config(cfg, ssh_port, template_path)). The test harness gained make_context(repo_root=...), whose default is the real clone root, so a test that reads a real template is unchanged while a test with fixtures passes its own directory instead of monkeypatching a module constant. Still to convert, each with its test file: chrome_setup, dnsproxy_setup, ffmpeg_setup, imagemagick_setup, kde_settings, local_vault_setup, port_forwarding_setup, ssh_daemon_setup, system_metrics_setup and vocalinux_setup, plus the stale comment in nextdns_setup_system_wide. When they are done, the guard test_repository_root_is_computed_once becomes "only utils.py computes REPO_ROOT and only pyntara.py imports it", which is the point of the block: no task may reach for the constant.

Done 2026-09-11, second and last stage of the same block: every remaining task reads the clone root from the Context, so no task imports REPO_ROOT at all. Converted: chrome_setup (the mirror unit template travels into _ensure_profile_mirror), dnsproxy_setup (_render_service takes the template path), ffmpeg_setup (_wayrecord_sources and _build_wayrecord take the source directory), imagemagick_setup, kde_settings (_apply_kwin_scripts takes the script template root, _apply_konsole_profile the profile template), local_vault_setup (_resolve_source_vault takes the root and the shared open_source_vault gained it as its first argument, so nextdns_setup_system_wide and three_x_ui_xray_setup pass ctx.repo_root), port_forwarding_setup (_render_service_unit takes the template path), ssh_daemon_setup, system_metrics_setup (the five unit renderers and the commit command take their template paths, _ensure_venv takes the root, _system_config_matches and _write_system_config take the repository config directory) and vocalinux_setup. The sixteen touched test files changed the fixture mechanism only: the context builder passes repo_root instead of monkeypatching a module constant, and a test that needs the shipped template reads it from its own clone root. Two test files (test_ffmpeg_setup, test_imagemagick_setup) carry a module-level _FIXTURE_REPO because their context helpers do not receive tmp_path; that is a test-only compromise to remove when those helpers next change. The guard test_repository_root_is_computed_once now refuses both a second computation of REPO_ROOT and any import of the constant outside pyntara.py, which is the point of the block.

Done 2026-09-11: the last site address left the code. vocalinux_setup carried GITHUB_REPO = "VocaHQ/vocalinux" and composed the asset URL of the pinned release by hand, while every sibling task reads its owner/name pair from the config and the engine already holds the release query URL. The pair is now [vocalinux_setup] github_repo and the URL is the engine-wide template [engine] github_release_download_url, so the host of a pinned asset download is a config value and a mirror needs no code change; _release_download_url(engine, repo, version, asset_name) substitutes into the template, and _install_appimage takes the engine object. A test proves that another pair and another host in the config produce the URL the task downloads from, and the document and the section literal of the config test carry the new keys. With this the site addresses of the remaining list are done: www.kde.org had already moved into [kde_settings] as the metadata owner and the Places namespace.

Done 2026-09-11, first task of the per-task sweep to config compliance (user request: check every task and move its values into the config). The hostname task is the first of the three mold tasks. Its audit found one value of a listed type in code, the number of random bytes the generated name is encoded from (secrets.token_bytes(4)), which is the length of a generated part; it is now the [hostname] key hostname_random_bytes, read as cfg.hostname_random_bytes by _generate_hostname(random_bytes), and the section of docs/spec/users-and-host.md names the key. The rest of the module is already read from the config: the hostname file path, the apply command, the command timeout, the task-data root and the repository root. What is left in the module is the wording of messages, which the spec lists as an exception.

Done 2026-09-11, the same day, on the user's decision: the task's own name left the code and is read from the context instead. The hostname audit found the literal in the force check ("hostname" in ctx.force_tasks) and the same name in the task-data directory of the modules that render a template (task_data_dir(ctx.repo_root, "<name>")), while docs/contracts/task-model.md fixes that name as the catalog entry, the module file name and the task-data subdirectory; the name is therefore already a config value and the code held a copy of it. The user chose the fix that adds no third copy: Context gained the field task_name, which the runner fills from the catalog name it already holds (task(replace(ctx, task_name=name))), so the 21 force checks read ctx.task_name in ctx.force_tasks and the 13 template paths compose task_data_dir(ctx.repo_root, ctx.task_name). No section carries a task_name key. Two guards prove the block: test_run_tasks_hands_each_task_its_own_name proves the runner hands the name over, and test_task_modules_do_not_write_their_own_name refuses a string literal left of in ctx.force_tasks and a string literal inside task_data_dir(...) anywhere under src/pyntara. The test factory make_context gained the task_name parameter, and the 23 test files that exercise force mode or a task-data template pass the name of the module they call. The Context contract of docs/contracts/architecture.md now lists repo_root, which was missing since the clone-root block, and task_name.

Done 2026-09-11, second task of the wave and the first one after the task-name block: imagemagick_setup. The audit found two values of listed types in the code, the name of the policy template the task reads from task_data/ of the clone and the suffix of the backup it writes once next to the system policy. They are now the [imagemagick_setup] keys policy_template_file_name and policy_backup_file_suffix, read by _deploy_policy as cfg.<key>; the four declaration places carry them, the Parameters section of docs/spec/imagemagick-setup.md names them, and two proofs back them: a fixture clone that carries only the configured template name, and another suffix in the config that names the single backup. The rest of the module already read from the config (the packages, the policy path, the status timeout, the install retries); what is left in it is the wording of messages, which the spec lists as an exception.

Done 2026-09-11, third task of the wave, together with the block it needed: nextdns_setup_system_wide and the root owner. The audit of the task left one value in code, the ownership its profile ID file received: the module wrote os.chown(path, 0, 0) under an own os.geteuid() == 0 test, while the shared helper utils.ensure_root_owner held the same literal for the eight tasks that use it. On the user's decision the owner is now the [engine] pair root_owner_uid and root_owner_gid, which the helper takes as arguments (ensure_root_owner(path, owner_uid, owner_gid)) and no module writes, and the nextdns task drops its own chmod-plus-chown copy for the shared helper, so the duplicate logic is gone too. Threading the pair reached 22 call sites: the five inside task(ctx) bodies pass two locals (owner_uid, owner_gid), and the 17 sites inside helpers of chrome_setup, dnsproxy_setup, i2pd_service_setup, rustdesk_setup, system_metrics_setup, tor_setup and yggdrasil_service_setup made those helpers take the pair, which mypy --strict found one by one. Two proofs cover the chain: ensure_root_owner applies the pair it is given and skips outside root, and the local_vault_setup and nextdns_setup_system_wide tasks chown their files to the pair another [engine] value names. The Configuration section of the architecture contract now lists the owner pair among the values that must live in the config.

Audited 2026-09-11, wave 2 first task: local_vault_setup. The audit found no value of a listed type left in the code: the runtime vault path, the two source vault paths, the password entry title, the four modes, the error priority and the force flag all come from the config, and the ownership goes through the shared helper with the [engine] pair. What the audit found instead is drift in the text: a comment claimed that module-level path constants are monkeypatched by the tests, although those constants left the module in the clone-root block, and two docstrings repeated values that live in the config (the secrets directory mode, the vault file mode, the password file mode written as 0400, and the owner written as root:root). The comment is gone and the docstrings name the configured values, so the text cannot disagree with the config any more. The lesson for the sweep: a task can be clean in values and still wrong in the text that describes them, so the audit reads the comments and the docstrings, not only the literals.

Done 2026-09-11, wave 2 second task: add_extra_repos. The module constant APT_KEEP_DEBS_CONTENT, the four-line body of the apt drop-in that keeps downloaded .deb files, is a value of the type Text of at most five lines that becomes part of a file the run writes, so it is now the [add_extra_repos] key keep_debs_dropin_content: the four declaration places carry it, the helper takes it as an argument (_ensure_keep_debs_dropin(keep_debs, keep_debs_file, content)) and the task passes cfg.keep_debs_dropin_content, and a proof puts another body into the config and checks the written file. The config comment describes the key, because this task has no spec document: the catalog and the config file are its only description. The audit also fixed the same drift local_vault_setup had: the test module docstring claimed that the task reads apt source files from module-level path constants, which left the module in the clone-root block.

Audited 2026-09-11, wave 2 second task, together with add_extra_repos: cli_tools. It holds no value of a listed type in code; the packages, the status timeout, the install retries and the success threshold percent all come from the config. The only number outside it is the 100 of the percent conversion in installed_total * 100 // len(packages), and the same factor appears in metrics_collect.percent_ready as int(ready * 100 / len(entries)) and in zram_service as ram_kib * 1024 * cfg.memory_fraction_percent // 100. I read it as a number that encodes a representation rather than one that describes the machine, the tool or the run, which is the class the Exceptions section of the config spec names with the 1024 factor of a unit conversion, so it stays in code; the user can correct this reading the way the earlier vocabulary claim was corrected.

Done 2026-09-11, wave 2 third task: ssh_client_setup, together with the augeas helper. Four values left the module: the ownership comment of the drop-in (dropin_header), the augeas lens (augeas_lens), the container node (augeas_container) and the pattern that node carries (augeas_container_value, the Host * wildcard, which lived as a signature default of augeas.write_dropin and augeas.sync_dropin). They are now keys of [ssh_client_setup], the four declaration places carry them, the Parameters section of docs/spec/ssh-client-setup.md lists them (before it listed nothing but the table name) and a proof shows that another header and another container value in the config are what the rendered drop-in carries. The container now travels as one pair (name, value) through augeas, so the node and its pattern cannot drift apart, and the "*" default is gone from the helper.

Found in the same pass and corrected: the owner of a file the run creates as root, which the previous block recorded as closed, was still in the code once more. augeas.sync_dropin chowned its drop-in with the literal apply_owner(dropin_path, 0, 0), and augeas.apply_owner is itself a copy of utils.ensure_root_owner, used with a user uid and gid by ssh_daemon_setup and tor_setup. The literal is gone: sync_dropin takes owner_uid and owner_gid and both of its callers pass the [engine] pair. The claim in the previous entry that the owner lived in the code twice was wrong; it lived there three times. Two items stay for the user: augeas.apply_owner and utils.ensure_root_owner are the same logic in two modules and one of the two names should win, and augeas.sync_dropin still carries the literal port_directive="Port" in ssh_daemon_setup, which that task's own pass will move.

Done 2026-09-11, on the user's ruling that function names are neutral and duplicated logic is hoisted out of modules: the owner helper is one function. utils.apply_owner(path, owner_uid, owner_gid) replaced utils.ensure_root_owner, whose name said root although the pair is a parameter and ssh_daemon_setup and tor_setup pass the uid and the gid of a user, and augeas.apply_owner, which was the same logic in a second module, is deleted; augeas imports the one function. The nine task modules that apply an owner, the engine config docstring, the two tests of the helper and the utils line of docs/guides/project-structure.md follow the name. The historical entries above keep the old name, because that is what the code said then.

The same ruling applies to the multipliers, and the next block moves them into the config: the percent scale 100 of cli_tools and metrics_collect and the byte factors 1024 and 1048576 of zram_service and swapfile_service_install are values, not exceptions, so the Exceptions section of docs/spec/config-content.md must lose the factor 1024 example when they move.

Done 2026-09-11, on the user's ruling that the multipliers are values and not exceptions: the conversion factors left the code. [engine] gained percent_scale (100), bytes_per_kib (1024) and bytes_per_mib (1048576), the four declaration places carry them, and the suite checks that bytes_per_mib equals bytes_per_kib squared, so the two byte factors cannot drift apart. The sites that counted with the literals now read the keys: cli_tools computes the installed share with percent_scale, metrics_collect.percent_ready takes the scale as an argument, zram_service._calculate_devices takes the byte factor and the scale (its task passes both from the engine table) and swapfile_service_install passes them to _calculate_swap_size_mb and _current_swap_size_mb. Four proofs show the factors are read: another scale in the config changes the share cli_tools reports and the share percent_ready counts, another byte factor changes the device target of zram_service and the size the swapfile task allocates. The Exceptions section of docs/spec/config-content.md lost the false example it carried, the factor 1024 of a unit conversion, and the Numbers section of the same document names the conversion factors among the types that go into the config, so the ruling is written down where the implementer looks.

Done 2026-09-11, wave 2 fourth task: ssh_daemon_setup. Three values left the module: the ownership comment of the drop-in (dropin_header), the augeas lens (augeas_lens) and the literal port_directive="Port" that the task handed to augeas.sync_dropin to decide between a reload and a restart. They are now keys of [ssh_daemon_setup], the four declaration places carry them, the Parameters section of docs/spec/ssh-daemon-setup.md lists them (before it listed nothing but the table name) and a proof shows that another header in the config is the ownership comment the rendered drop-in carries. The audit also fixed the stale comment that claimed module-level path constants are monkeypatched by the tests, which left the module in the clone-root block.

Done 2026-09-11, wave 2 fifth task: ffmpeg_setup. Five values left the module. The names of the C sources of the capture engine became the [ffmpeg_setup] key wayrecord_source_file_names; the staged suffix .build became wayrecord_build_file_suffix; and the two commands the task assembled in code became wayrecord_build_flags_command (pkg-config with its own arguments) and wayrecord_compile_command (gcc -O2 -o {output}), with the source paths and the flags the other command prints appended by the code, because those are not ours to choose. The eight-line desktop entry left the code for a template under task_data/ffmpeg_setup/, which is what the spec asks for a body longer than five lines: the run renders it and substitutes $bin_path, so the entry text and the engine interface name live with the template instead of in the module, and the task reads the template name from the config (wayrecord_desktop_template_file_name). The module docstring no longer repeats the values it used to name, and the config test of the section covers the five new keys, including the two arrays that must not be empty. A proof shows that another template name in the config is the template the task renders.

Done 2026-09-11, wave 2 sixth task: playwright_setup. Five values left the module: the user prefix name .local and the binary path bin/playwright-cli became user_prefix_relative_path and cli_bin_relative_path, and the three commands the task assembled in code became command templates in the config, runuser_command (with {username} and {home_dir}), cli_version_command (with {cli_bin}) and npm_install_command (with {cli_package} and {prefix}). The task fills the placeholders through one new helper, _substituted_command, so the substitution lives in one place; the runuser prefix is still added by the code to both the probe and the install, because it is the same wrapper, not a second command. The module docstring no longer repeats the paths, the Parameters section of the spec names the five keys, and a proof shows that another runuser template in the config is the prefix the task runs. The test fake stopped keying on the program name runuser and now answers by the arguments --version and --prefix, so a test that changes the wrapper still exercises the whole path.

Done 2026-09-11, wave 2 seventh task: tor_setup. Seven values left the module, and the block needed a shared helper. The rendered drop-in is eight lines, which the spec sends to task_data/ as a body longer than five lines: the ownership comment and the six options are now the template task_data/tor_setup/torrc.conf with $socks_port, $log_level, $hidden_service_dir, $num_introduction_points, $onion_ssh_port and $ssh_port, its name is the key dropin_template_file_name, and _render_config(cfg, ssh_port, template_path) renders it through string.Template, so the wording of the file lives with the template and the values in the config. The remaining five values are include_directive (the %include form the task appends with the drop-in path), hostname_file_name (the file Tor writes the onion hostname into), and the three commands the task assembled in code: verify_config_command (runuser with the Tor user), service_enable_command, service_start_command and service_restart_command, which carry the {tor_user} and {service_unit_name} placeholders of their keys. On the earlier ruling that duplicated logic is hoisted out of modules, the placeholder filler of playwright_setup moved up to utils.substituted_command, so both tasks fill the placeholders of a configured command the same way. The four declaration places carry the seven keys, tests/config_checks.py validates the four arrays through the shared _string_list helper, the module docstring no longer repeats the unit name, the verify command or the directive it used to name, the Parameters section of docs/spec/tor-service.md now lists every key of the table, and a proof shows that another socks_port, another log_level and another ssh port produce another render; the test builds the template path from the clone root and the configured file name, so a drifted directory or name fails instead of being mirrored. Two lessons of the block are worth the next task's attention: the shared test document must reproduce the shipped text exactly, placeholders included, because a replace() whose target does not match is silent, and a key inserted in the middle of a section breaks the multi line replace() anchors of the wrong type parametrisations, which is why the four new command cases came out as no-ops until the keys were placed at the end of the block.

Done 2026-09-11, wave 2 eighth task: telegram_setup. Nine values left the module. The three relative paths the task derived under the desktop user home became install_dir_relative_path, launcher_relative_path and icon_relative_path; the two file names inside the archive and inside the install directory became binary_file_name and updater_file_name; the directory of the extracted archive became archive_directory_name; the suffix of the partial download became partial_download_file_suffix and the prefix of the extraction temp directory extract_dir_prefix. The eight line launcher entry left the code for the template task_data/telegram_setup/telegramdesktop.desktop with $binary and $icon, its name is launcher_template_file_name and _desktop_content(cfg, template_path) renders it, which follows the decision already taken for the ffmpeg desktop entry. The five paths are now composed in one place, _install_paths(cfg), so the files the task writes and the paths the launcher entry names cannot disagree. The four declaration places carry the nine keys, the config test of the section names a wrong type for each of them, the section literal of the missing section test follows, the module docstring no longer repeats the layout, and the Parameters section of docs/spec/telegram-setup.md lists every key of the table. Two proofs back the block: another install directory, another binary name and another icon path change the rendered launcher entry, and another whole layout (four paths, two names, the archive directory and the suffix) is what the task writes, with the subprocess fake reading the same section so the archive it builds matches the task. Deliberately left for a separate cross cutting block: the curl and tar argv assembled inside the module. The same shapes live in the five other download tasks, so moving them per task would copy one flag set into six sections; they belong in [engine] as command templates once, for all of them.

Done 2026-09-11, wave 2 ninth block, the two producer tasks of the System Metrics queue: commit_final_system_metrics and system_metrics_initial_collect. The audit found the hand-off call assembled in two modules: the final commit task and the deployed collector both ran [str(command_path), str(file)], so the same argv shape lived twice and neither was a config value. [system_metrics_setup] gained commit_command (["{command_path}", "{file}"]), both callers fill it through utils.substituted_command, and the program stays in one place, because the template names the configured command_path instead of repeating the path. The collector start command of the second task, ["systemctl", "start", "--no-block", unit], became [system_metrics_setup.collector] start_command with the {service_unit_name} placeholder, which is the same decision as the tor service commands. The four declaration places carry both keys, the section tests name wrong types for them, the Parameters descriptions live in the dataclass docstrings and the spec prose now says configured command instead of the literal call, and two proofs show that another commit_command and another start_command are what the tasks run. Found and fixed on the way: a config with an empty commit_command (the runtime reader cannot tell an absent array from an empty one) would have reached subprocess with an empty argv and raised inside the deployed collector and inside the task; both now report the key in words and return an error, and the task reports it before copying the vault so no temporary file is left. The module docstring of the final task no longer repeats the mode as 0600, which was drift of the same kind the local_vault_setup audit found.

Done 2026-09-11, wave 3 first block, the duplicated architecture table and the asset names: vocalinux_setup needed the release asset architecture and rustdesk_setup carried the very same table, DPKG_TO_ASSET_ARCH with amd64 to x86_64 and arm64 to aarch64, so the mapping was knowledge in two modules. The mapping is now [engine] release_asset_architectures, which belongs to the engine because every task that downloads a release asset maps the one dpkg architecture the same way, and the fallback rule (an architecture the mapping does not name keeps the dpkg spelling) lives in one new helper, utils.release_asset_architecture(architectures, dpkg_arch), instead of a .get(arch, arch) in each module. The asset file names left the code as well: the f-string Vocalinux-{version}-{arch}.AppImage and the f-string rustdesk-{version}-{arch}.deb became [vocalinux_setup] asset_name_template and [rustdesk_setup] asset_name_template, so the geometry of a release asset name is a config value and a rename needs no code change. The four declaration places carry the three keys, the section tests name wrong types for them, both specs list them, and the vocalinux Parameters list was repaired on the way: two of its lines had run together and the item order did not follow the table. Noted for the next block in this same task: the same table shape still lives in dnsproxy_setup as its own spelling (amd64, arm64, arm7), which is a different vocabulary and therefore a value of its own section, not a copy.

Done 2026-09-11, the same day, on the way to the vocalinux layout: the suffix of a half written download is one engine value. telegram_setup carried partial_download_file_suffix in its own section and vocalinux_setup wrote the same ".download" literal by hand, so the same knowledge lived twice in two shapes. It is now [engine] partial_download_file_suffix, the key of the telegram section is gone, and the telegram helpers take the engine object instead of six curl values: _resolve_latest_url(engine, latest_url), _download_archive(engine, cfg, url, name) and _ensure_icon(engine, cfg) read the timeouts, the retry bounds and the suffix from the table that owns them. The four declaration places follow, the engine section test names an empty suffix, the telegram test reads the suffix from the engine, and the telegram spec names the value as engine-wide.

Done 2026-09-11, wave 3 second task, the rest of vocalinux_setup: the layout, the KDE shortcut record and the templates. Twelve values left the module. The four paths under the desktop user home became appimage_dir_relative_path, app_config_relative_path, autostart_relative_path and echo_desktop_relative_path; the three template file names that were hardcoded in task() became app_config_template_file_name, autostart_template_file_name and echo_desktop_template_file_name, and the autostart entry, which was built in code as a nine line string, is now the template task_data/vocalinux_setup/vocalinux.desktop rendered with $appimage, which is what the spec asks for a body longer than five lines; the empty Meta+S consuming shortcut is the configured record shortcuts_file_name, shortcut_group_name, shortcut_entry_name, shortcut_action_name and shortcut_key_sequence instead of five module constants. Reading the three templates moved into one helper, _read_task_template(template_dir, file_name, label), which reports a missing template before anything is written, so a broken clone is visible before the run touches the user home, and the three deployed files are named once in the task instead of being formatted into messages twice. The four declaration places carry the twelve keys, the section test names wrong types for them, the section literal of the test follows, and the task tests read the fixture template names and the deployed paths from the section, so a renamed key is what the task looks for. Still in the module and deliberately left for the commands block: the runuser wrapper and the kreadconfig6/kwriteconfig6 argv, which the KDE tasks build the same way.

Done 2026-09-11, wave 4 first task, done in full: chrome_setup. The audit found twenty three hardcoded values, more than in any task before, and all of them left the module. The browser identity: package_name and process_name. The taskbar pinning: appletsrc_file_name and appletsrc_relative_path (the second one removes the ".config" literal), appletsrc_launchers_key, taskbar_plugin_names, panel_launcher_id and panel_restart_command. The settings repository layout: settings_system_tree_relative_path, preferences_relative_path (one key serves both the repository file and the profile file, so the two cannot drift), profile_dir_relative_path. The apt repository: the six line deb822 body, which is longer than five lines and therefore a template, became task_data/chrome_setup/google-chrome.sources rendered with $keyring_path, its name is apt_source_template_file_name and keyring_temp_dir_prefix names the temporary directory; the dearmor call became keyring_dearmor_command. The version control: the five git calls became settings_clone_command, settings_fetch_command, settings_revision_command (with {revision}, so the HEAD and FETCH_HEAD queries are one template) and settings_reset_command. The remaining one-off commands became process_check_command, mount_check_command, mount_reload_command, mount_enable_command and menu_refresh_command, and the mirror unit template name became mount_unit_template_file_name, checked before the run writes anything. The launcher flags became launch_flags, a list of flag templates with placeholders: a flag whose placeholder has no value is left out, which is the rule the code used to spell out for the proxy and the mirror, so a third flag is a config line and not a code branch, and the flag placeholder pattern is the one regular expression of the block. The kreadconfig6 and kwriteconfig6 shapes stay in code on purpose, as in the two KDE tasks: the file comes from the config and the group, the key and the value are arguments of the call, so the tool interface is the code and the data is the config. The four declaration places carry the keys, the section test names a wrong type for every one of them, the tests read the repository layout, the appletsrc path and the template names from the section, and two proofs show that another launch_flags list with another debug port is what the Exec lines carry.

Done 2026-09-11, next task after chrome_setup: zswap_service, and the module came out smaller than it went in. The audit found three things: the kernel attribute directory was a module constant the tests replaced with monkeypatch.setattr, the five parameter names were written twice in code (PARAM_ORDER and PARAM_PATHS, so the order and the paths could drift apart), and the name of the unit template was hardcoded in task(). Now parameters_dir_path is a config value, so the tests pass a fixture directory through the context and no constant is replaced; parameter_names is one config list in the order the task writes, and the value of a parameter is the key of the section with the same name, read by getattr, so a parameter the kernel drops or adds is a config line and never a code change; unit_template_file_name names the template. A neat simplification came with it: the boolean rendering and the comparison no longer carry a list of boolean names, because the expected value decides. _target_values renders a boolean key as Y or N and a number or a string as it is, and _normalize(expected, value) maps the accepted J kernel spellings (Y, y, 1, N, 0) to the canonical form of the expected value, so the kernel interface names live in one place and the code holds no parameter name at all. The four declaration places carry the three keys, the memory section test names wrong types for them, the module docstring and the Zswap section of docs/spec/users-and-host.md describe the config shape, and a proof writes another parameter list in another order into another directory and checks both the writes and the rendered unit.

Done 2026-09-11, the same day, the apt index refresh is one function. The audit of the remaining tasks found the same command spelled out in five modules (add_extra_repos, chrome_setup, i2pd_service_setup, rustdesk_setup, ssh_daemon_setup) and a sixth time inside utils.install_packages, every copy with the same non-interactive environment. They all call utils.refresh_apt_index(timeout) now, so the command and its environment live in one place and a change to the refresh (a proxy flag, a quiet option) is one edit. Each caller keeps its own error handling on purpose: the tasks that can continue report a warning and the ones that cannot report an error, which is their decision and not the helper's.

Done 2026-09-11, next task: rustdesk_setup. Eleven values left the module. The client interface is now the configured command set: version_check_command, machine_id_command, get_option_command and set_option_command (the option key and value enter as data through the placeholders) and set_password_command, which keeps log_command=False because the password must never reach a log. The three service commands became service_stop_command, service_enable_command and service_start_command with the {service_unit_name} placeholder, the same shape the tor task uses. Two more values came out with them: the identity file name inside config_dir, which force mode removes to regenerate the machine ID, became identity_file_name, and the five second cap of one machine ID probe inside the readiness loop became readiness_probe_timeout_seconds, because a number that describes the run is a config value and not a judgement of the code. The four declaration places carry the eleven keys, the section test names a wrong type for each of them, the Parameters section of docs/spec/rustdesk-setup.md lists them, and a proof puts another command set in the config (query instead of option, another start argv) and checks that those are the argv the task runs.

Done 2026-09-11, next task: dnsproxy_setup. Fifteen values left the module. The release asset naming was code: the architecture suffix table {amd64, arm64, armhf} and the f-string dnsproxy-linux-{arch}-{tag}.tar.gz became asset_architecture_names (a mapping of this section, because dnsproxy names its architectures itself and the engine table would be wrong here) and asset_name_template with {asset_arch} and {release_tag}, the tag keeping its leading v because the published asset name carries it. The unpacking layout became extract_dir_name, binary_file_name and staged_binary_file_name. The direct DNS probe got probe_address and probe_ident_bytes instead of the loopback literal and the hardcoded two random bytes. Four NetworkManager tokens became values: tun_device_type, loopback_connection_name, nmcli_auto_dns_ignored_value (the answer that means auto DNS is already ignored) and the two values the sweep writes to ignore and restore it, which the code spelled out as "true" and "false" while the read compared "yes". The five systemctl argv sites became service_stop_command, service_enable_command, service_start_command and service_restart_command. On the way two real defects came out: the module still forced itself with ctx.force_tasks.intersection({"dnsproxy_setup"}), a task-name literal the earlier task-name block did not catch because its guard looks for a literal left of in ctx.force_tasks, and the nmcli commands were filled with str.replace while every migrated task uses utils.substituted_command, so the placeholders were handled in two ways. Both are fixed. The four declaration places carry the keys, a proof puts another asset template and another architecture table in the config and checks the name the task looks for, and the spec gained the naming, the probe, the service commands and the NetworkManager vocabulary. Noted: the dnsproxy section still has no wrong-type parametrisation in the config tests, which is the state it was in before this block; the guard of the last wave is the place to decide whether every section needs one.

Corrected 2026-09-13: the remainder this paragraph listed is not open work any more, and its numbers were an unaudited snapshot, never a limit. The module level constants left the code: the guard's allowlist of value constants holds the documented exceptions of the spec and nothing else. The commands assembled inside the task modules are configured command templates: the guard's command rule has an empty allowlist, no module under src/pyntara outside the config layer spells a program name, and the last block that moved them was the service group of utils. The paths of foreign programs and of external API interfaces are keys of the section that owns them, the generated file names are keys, and the site addresses followed with the KDE metadata owner, the Places namespace and the owner/name pair of vocalinux_setup. One statement of this paragraph was wrong and is corrected here: REPO_ROOT is not computed in utils.py, it is computed once in src/pyntara/pyntara.py and carried to every task as Context.repo_root. The message wording and the regular expressions are exceptions of the spec, not work.

## Leftovers of the config split

Items from the split of reading and checking (commits f92b8e5 and a69df8f).

Done 2026-09-10: the deployed metrics services report an incomplete config in one line instead of a traceback, and tests/test_metrics_collect.py, tests/test_metrics_ingest.py and tests/test_metrics.py now cover that path with the real reader on a document that holds only [engine]. Each service names the keys it reads and cannot find, clause by clause per table, instead of showing a Python error: "error: the ingest cannot run: [system_metrics_setup] has no spool_dir, spool_temp_prefix, ...". The keys a service reads live in one list in its own module, absent_config_keys and describe_absent_config_keys do the rest, and an absent array is not reported because the reader cannot tell it from an empty one.

Done 2026-09-10: the two scripts in secrets/ no longer catch the ConfigError that the runtime reader never raises, and their comments describe the loader as it is now.

Done 2026-09-10: the vocabularies that only the checks validate against (SEND_ORDERS, I2PD_LOG_LEVELS, TOR_LOG_LEVELS, DNS_OVER_TLS_VALUES, YGGDRASIL_LISTEN_SCHEMES, YGGDRASIL_PEER_SCHEMES, NUMLOCK_STATES, CLICK_METHODS, SHARE_ADDR_STRATEGIES, DOMAIN_STRATEGIES) and the ConfigError they raise moved from src/pyntara/config/_fields.py into tests/config_checks.py, and the package stopped re-exporting them. MODES stays in the package: production reads it to accept or reject an install mode. src/pyntara/config/_fields.py now holds that one vocabulary and says where the others live.

Done 2026-09-11: tests/support.py make_config lost the parameters that did nothing. A parameter is dead when no test passes it and its default repeats the value of the shared test document (tests/config_helpers.py::base_config()), because the factory would produce the same Config without it. Measured that way: 377 parameters, 224 passed by no test, 212 of those dead; 212 parameters and the matching replace() keywords are gone, and the file shrank from 1257 to 735 lines. The proof that no behaviour changed is the repr of make_config() captured before the edit and compared after it, together with the whole suite on the same commit. The 12 parameters kept despite being passed by no test carry deliberate test-safe defaults that differ from the document and must not be removed: engine_desktop_detect_processes, add_extra_repos_ubuntu_hosts, kde_settings_packages, swapfile_ram_multiplier (the document holds the integer 2 where the parameter default is the float 2.0), yggdrasil_listen, and the seven collector ones (boot_delay_seconds, daily_send_time, command_timeout_seconds, service_unit_name, timer_unit_name, journal_identifier, report_file_name). The rule for the next author: a parameter appears when a test passes it, not before, and comparisons of this kind must use the shared document, never the factory output, because that comparison is circular.

Done 2026-09-11: the 17 paths of the 3x-ui panel REST API moved from src/pyntara/xui.py and the task module into the [three_x_ui_xray_setup] table as panel_root_path, panel_login_path, panel_csrf_token_path, the four inbound paths, the three client paths, the server, settings and xray paths. The four paths that carry a value of one call hold a {placeholder} filled at the call site, so the client composes f"{base_url}{cfg.panel_..._path}" and the two tests of TestPanelPathsComeFromConfig prove with another path that the client reads the config instead of a literal.

Done 2026-09-11: the Places panel addresses of the kde_settings task left the module. places_bookmark_namespace, places_kdepriv_namespace and places_mime_namespace hold the XBEL namespaces declared on the root tag, and places_metadata_owner is the owner an entry metadata block must carry before the task hides that place. The two helpers now take the config (_declare_missing_prefixes(cfg, current), _places_xbel_hidden(cfg, current, hidden)). Two tests prove the values are read: another namespace address in the config becomes the declaration the task injects, and another owner in the config leaves the same file unchanged.

Done 2026-09-11: the last values kept in code under an invented exception went into the config, and the rule that governs the doubt was written down. The exceptions I had reasoned for myself were these: the two permission masks of the system metrics checks (0o777 and 0o7777, kept as "the definition of the comparison"), the 0o400 bit that identifies the zram hot_add attribute as readable (kept as "the kernel interface"), and the path /etc/os-release, which i2pd_service_setup carried as a constant with the comment that it is "a fixed machine contract (architecture contract, Configuration)". All three are values of listed types, and the config now holds them: command_permission_mask and spool_dir_permission_mask in [system_metrics_setup], hot_add_readable_mode_bit in [zram_service] and os_release_file_path in [i2pd_service_setup]. The dnsproxy_setup copy of the os-release path was dead and is deleted. The helpers take the values as arguments (_command_file_matches(path, expected, mode, permission_mask), _spool_dir_ok(dir, mode, permission_mask), _hot_add_read_interface(readable_mode_bit)), so no module under src/ holds an octal literal at all, and the guard test_no_module_applies_a_literal_file_mode now refuses any octal literal anywhere under src/ instead of only a literal chmod. The i2pd tests no longer monkeypatch the module constant: the fixture directory enters through the config, which is itself the proof that the path is read. Two further tests prove the masks and the bit decide the outcome. The rule itself is now written in docs/spec/config-content.md, in the new sections File permissions and A doubtful value goes into the config: every permission is a config value including the masks and bits used to inspect a mode, and a doubtful value goes into the config rather than staying in the code, while the Exceptions section remains the whole list and may not be extended by the implementer alone.

Done 2026-09-11: the file modes the run applies left the code. Ten keys carry them as octal strings, the form the other mode keys already use (resolved_dropin_file_mode, queue_file_mode): nm_unmanaged_conf_file_mode in [yggdrasil_service_setup]; cert_privkey_file_mode and cert_fullchain_file_mode in [three_x_ui_xray_setup], used by both the Let's Encrypt pair and the self-signed pair so one value decides the private key and one the chain; staged_binary_file_mode in [dnsproxy_setup] for the downloaded binary the version probe executes; pass_file_writable_mode in [local_vault_setup] for the mode that makes an existing read-only password file writable before the rewrite; vault_backup_file_mode and collector.report_file_mode in [system_metrics_setup] for the temporary vault copy and for the collector report; askpass_helper_file_mode and state_file_mode in [port_forwarding_setup]; script_file_mode and default_file_mode in [kde_settings], where _write_user_file now takes the mode as an int and formats it into the chmod command instead of receiving a literal string. Four proofs and one guard back the block: another mode in the config decides the chmod of a written user file, the middle chmod of the password-file rewrite is the writable mode, the state file carries the mode given to save_state, and the guard test_no_module_applies_a_literal_file_mode refuses a chmod with a literal mode anywhere under src/. The masks that only compare a stat result against a mode the config already supplies (st_mode & 0o777, the 0o7777 mask that covers the sticky bit) and the 0o400 bit that identifies the zram hot_add attribute as readable stay in code: they define the comparison and the kernel interface rather than a permission the run applies, and this reading is recorded here so it can be corrected if it is wrong.

Done 2026-09-11: the numbers that describe the run left the code. The [dnsproxy_setup] table gained verification_error_excerpt_length (200 characters of the failed verification output, empty output becoming the placeholder <no output>) and service_log_excerpt_length (400 trailing characters of the unit log kept for the diagnosis of a failed start); the [port_forwarding_setup] table gained own_addresses_timeout_seconds (the ip call that lists this machine's own addresses), agent_start_timeout_seconds (the ssh-agent start) and key_unlock_timeout_seconds (the ssh-add unlock of the port-forwarding key). port_forwarding.py no longer holds a timeout literal: own_addresses takes the bound as its argument and _start_agent takes both bounds, and main passes the three values from the config it already holds. Three tests prove the values are read: another verification_error_excerpt_length and another service_log_excerpt_length decide the length of the text the task reports, and the timeout of the ip call is read back from the call the module makes.

Done 2026-09-11: the KConfig structure, the vocabularies and the Qt flags of the two KDE tasks left the code. [kde_settings] gained 40 keys: the file names (kdeglobals, kcminputrc, kwinrc, plasmakeyboardrc, kglobalshortcutsrc), the group names, the key names, kconfig_true_value and kconfig_false_value as the boolean spelling, numlock_values and click_method_values as the value each choice takes in the file, automatic_theme_switch_idle_interval, the XBEL structure names of user-places.xbel with places_root_tag and the places_hidden_element, and the KWin scripts with the files, hotkeys and actions each one claims. places_bookmark_namespace, places_kdepriv_namespace and places_mime_namespace became one places_namespaces mapping, so the prefix and the address it is declared with can no longer drift apart. [kde_keyboard_setup] gained 16 keys: the kxkbrc file name and group with its seven keys, appletsrc with the applet configuration group and display_style_key, the kglobalshortcutsrc name, the unique and friendly component names, the boolean spelling, and shortcut_modifier_bits, the Qt modifier flags a portable shortcut is combined from by modifier name. Every module constant of the block is deleted and the helpers take the config (_keyboard_layout_config_group(cfg, text, plugin), _shortcut_to_combined(cfg, shortcut), _notify_flag(cfg, file_name, env), _script_hotkey_owners(cfg, text), _declare_missing_prefixes(cfg, current), _places_xbel_hidden(cfg, current, hidden), _places_prefix_addresses(cfg)). tests/config_checks.py validates the two new mappings as a non empty dict of string to string and of string to integer (bool rejected), and three tests prove the values are read: another kwinrc_file_name in the config decides which file carries the notify flag, another shortcut_modifier_bits table decides the combined key code, and the earlier Places proof keeps the namespace path config driven. The block moved every module constant of the two tasks and every occurrence of their values; the data no longer lives in the code at all, so a KDE release that renames a key is answered in the config.

Correction 2026-09-11, kept as a lesson: an earlier version of the entry above it claimed the prefix and element names of the Places file stay in code because they are "the vocabulary of the file format". That claim is wrong and was my own invention: the closed list of types in docs/spec/config-content.md carries no such exception, and the project already stores exactly this kind of data in the config. The same false reasoning had left the Qt modifier table in code, which the user then ordered into the config together with this block. The rule that follows: a value of a listed type goes into the config whatever it is, and only the spec exceptions section keeps anything in code.

Done 2026-09-10: the four config key lists of the deployed metrics components (COLLECTOR_SECTION_KEYS, COLLECTOR_TABLE_KEYS, INGEST_CONFIG_KEYS, SERVICE_CONFIG_KEYS) are checked against the tables they belong to, so a renamed or mistyped key can no longer make the one-line report point at a value nobody can set.

Done 2026-09-11: the desktop session mechanism left the code. The run reads the environment of the desktop user's session manager (session_environment_command of the [engine] table, systemctl --machine=<user>@.host --user show-environment by default) and exports the variables of session_environment_keys into the environment of the process once, before the tasks, so every task and every child process inherits them whether the run started inside the session or over a remote console. [engine] gained desktop_username, session_environment_command, session_environment_keys, session_bus_key and session_display_keys; [port_forwarding_setup] gained askpass_display, the display the askpass helper of the key unlock is given. The last literals of the mechanism are gone: the module no longer names the desktop processes (plasmashell and kwin_wayland are read only by the mode detection through desktop_detect_processes), the six checks of kde_settings ask whether a live session environment was passed instead of comparing against DBUS_SESSION_BUS_ADDRESS, and kde_keyboard_setup builds its one-entry bus environment from session_bus_key. The helpers take the values as arguments (session_environment, session_bus_address, user_session_environment, parse_session_environment, export_session_environment), the old /proc readers are deleted, and a live session is one that reports the bus and one display variable, so a partial environment is dropped with a log line instead of making a GUI tool abort against a display that is not there.

Done 2026-09-13, the hundred-and-fourth block: the completeness this paragraph called open is proven for the four address components and the four inline tuples are gone. The components read the keys of their own section directly in one module, so the direction this paragraph believed impossible is provable there: tests/test_address_component_config_keys.py follows the binding of the section (cfg.<section> through its local alias) and compares the keys read with the list of the config layer in both directions, and the four lists joined COMPONENT_KEY_LISTS of tests/test_config_coverage.py, which proves every name is a field of its table. The claim that a key read without being listed costs the specific error line only was wrong: the deployed code on this machine printed a record with "channel": null and exited 0 when the key was absent. The lists of the metrics components remain checked in one direction only, for the reason the coverage test states, because those components read their keys through shared modules such as metrics_commit.ingest_spool.


Done 2026-09-11: the two inline python scripts of the KDE tasks left the modules and their names went into the config, closing a case the config spec already called out: a body longer than five lines lives under task_data/ with a file name from the config. The DBus client of the live hotkey apply became task_data/kde_keyboard_setup/apply_hotkeys.py, named by the new [kde_keyboard_setup] key apply_hotkeys_script_file_name; the virtual desktop id client of kde_settings became task_data/kde_settings/list_desktop_ids.py, named by the new [kde_settings] key desktop_ids_script_file_name. Both modules read the file and pass its text to the system interpreter exactly as before, and both report a missing file with its path instead of crashing: the hotkey apply answers with the path in its warning, the desktop count returns an error before it removes anything. The move exposed two more literals of the same kind in kde_settings, the task_data directory of the KWin scripts and the Konsole profile file name, so [kde_settings] gained kwin_scripts_dir_name and konsole_profile_file_name as well. The three new tests hold the behaviour: the live apply runs the text of the file the config names, a missing hotkey script is a warning that carries the path, and a missing desktop id client stops the count with the path in the error and no removal issued.

Done 2026-09-12: i2pd_service_setup went through the cycle, and the finding recorded in the fresh install log was closed with it. The asset names the task builds by hand became the configured templates codename_asset_name_template and generic_asset_name_template, the os-release field the codename is read from became os_release_codename_key, the version query became version_command, the three unit commands became service_enable_command, service_start_command and service_restart_command with the {service_unit_name} placeholder, the two template names became config_template_file_name and tunnels_template_file_name, and the boolean spelling of the rendered file became config_true_value with config_false_value. The task now guards what used to crash or mislead: an empty command list is reported with the key name, and a missing task data template is reported with its path before anything is downloaded or written. The address finding of the install log is fixed at its root: i2pd writes the tunnel identity only after the router is up, so the run used to finish with "the address appears after the first start" and save nothing. The task now waits for the identity file after the start for address_check_attempts attempts with address_check_retry_delay_seconds between two decodes, so one run saves the address and prints it in the [done] line, and a machine where the file never appears still ends the wait with an honest message instead of hanging. The two new unit tests hold the wait on both paths, the template and command guards have their own tests, and the Parameters section of the spec now names every key of the table.

Remaining for the same task: the curl argv of the package download still lives in the module, together with the same shape in dnsproxy_setup, and the os-release vocabulary of the shared os_family_is_debian helper (ID, ID_LIKE and the two family names) is still in code. Both are cross-cutting blocks of their own: the download command of every task that fetches a release asset, and the os-release fields the shared helper reads.

Done 2026-09-12: the download command of every task went into the config, closing the second half of that note. [engine] gained curl_download_command, curl_download_write_out and curl_query_command, and utils.py gained one assembler (curl_command) with two wrappers (download_command and release_query_command), so the ten identical download curls and the release query of nine modules became one definition each: chrome_setup, dnsproxy_setup, i2pd_service_setup, rustdesk_setup, telegram_setup (archive and icon), three_x_ui_xray_setup, vocalinux_setup and yggdrasil_service_setup (package and peer tarball) call download_command, github_release calls release_query_command, and telegram_setup keeps its own configured latest_url_command for the HEAD request that resolves the redirect. The five curl parameters that travelled through the private helpers of five modules are gone with the flag lists they fed, and the CURL_DOWNLOAD_WRITE_OUT constant of utils.py is gone as well: the write-out text is a config value of its own, because curl reads its fields as %{name} and a command template is a format string, so a text carrying braces must be substituted as a value and never formatted itself. The checks demand both placeholders and a non-empty query command, four new unit tests hold the assembler (template filled, flags inserted before the URL, unknown placeholder still a loud KeyError), and the Parameters sections of the i2pd, rustdesk, yggdrasil and telegram specs now name the three keys.

Remaining after that block: the curl calls that are each unique to one task and not part of a download - the two probes and the tunnel check of three_x_ui_xray_setup, the metrics upload of metrics_send, and the parallel address query of utils.fetch_urls_in_parallel with its source marker - and the os-release vocabulary of os_family_is_debian.

Done 2026-09-12: the three probe calls of three_x_ui_xray_setup left the module. [three_x_ui_xray_setup] gained port_forward_probe_command with port_forward_probe_url_format, panel_probe_command, tunnel_probe_command with tunnel_probe_write_out, and proxy_check_command_timeout_seconds, the outer bound of a tunnel check that was twice the probe timeout plus ten. The port 80 self-test composes its URL from the configured format with the public address and the ACME port, the panel check carries the insecure flag and the XMLHttpRequest header as the config spells them, and the tunnel check fills its proxy address, timeout and write-out text from the table. The write-out text is a key of its own and not part of the command: curl reads its fields as %{name}, and a command template is a format string, so a text carrying braces raises KeyError when it lives inside the template. That mistake was made here first, then fixed by the key, which is why the lesson of the previous block repeats in a second place: any curl format text belongs in a value of its own. The checks demand the placeholders of each command, and the Config reference of the spec names all five keys.

Done 2026-09-12: the os-release vocabulary left the shared helper. [engine] gained os_release_family_keys (ID and ID_LIKE) and os_release_debian_family_names (debian and ubuntu), and os_family_is_debian now takes the engine config and reads both lists instead of carrying them, so a distribution that renames its family field or reports a new family name is a config change. The helper splits every field into words before the comparison, so a derivative of a derivative stays accepted, and the error message of i2pd_service_setup now reports the fields the config names rather than two hardcoded ones. The checks demand both lists non-empty, the engine section test asserts the round trip, and a new unit test proves the helper follows a narrowed and a renamed vocabulary.

Remaining after that block: the curl calls that are each unique to one task and not part of a download - the two probes and the tunnel check of three_x_ui_xray_setup, the metrics upload of metrics_send, and the parallel address query of utils.fetch_urls_in_parallel with its source marker - then the remaining tasks of the migration (port_forwarding_setup, three_x_ui_xray_setup, system_metrics_setup and the cross-section duplicates) and the guard of the fifth wave.

Done 2026-09-12: the last two curl shapes left the code, closing that note. [system_metrics_setup] gained google_script_upload_command with {timeout_seconds}, {file_name} and {key}, so the Google Drive sender spells no argument of its own. [engine] gained curl_parallel_command, curl_parallel_write_out and curl_parallel_source_marker, and the parallel query of utils reads all three: the command is filled with the number of URLs, the per-transfer bound and the write-out text, the URLs are appended, and split_url_answers looks for the configured marker instead of a module constant. The marker lives in the write-out text and the two values stay in step through a check that refuses a marker the text does not print, because a marker nobody prints would leave every answer unattributed; a marker cannot be composed into the text by the code, since the text carries %{url_effective} and is never formatted. The two public helpers of the parallel query grew an engine argument, and so did the callers they serve (fetch_public_addresses, detect_country, the public address report, the country report and the 3x-ui task), which is the price of one definition instead of three copies. Eight test files followed the new signatures.

Done 2026-09-12: the task module of port_forwarding_setup left its literals, first half of that task. [port_forwarding_setup] gained service_template_file_name and service_module_name (the unit template and the module the unit runs), the four systemctl_* commands (daemon reload, enable, restart, is-failed, each carrying the unit name as {service_unit_name} except the reload) and start_check_attempts with start_check_retry_delay_seconds, which were the numbers 10 and 1 inside the readiness call. The unit path, the systemctl argv and the ExecStart line of the deployed service now come from the table, and a configured command with an extra argument proves it in a test. The same audit found the venv layout in three places: the interpreter of the shared deployment venv was composed as venv_dir / "bin" / "python" in system_metrics_setup twice and in port_forwarding_setup once, so [system_metrics_setup] gained venv_python_relative_path and all three read it.

Remaining for that task: the deployed service itself, src/pyntara/port_forwarding.py, still spells its own argv (ip -o addr show, ssh-agent -s, ssh-add and the ssh call with its options), and src/pyntara/port_forwarding_state.py shares the state vocabulary with it. That block must move the service commands and the askpass helper text into the same table, and its spec section describes the service, so the Parameters section grows with it.

Done 2026-09-12: the deployed service of port_forwarding_setup left its literals, closing that task. [port_forwarding_setup] gained the whole vocabulary the service runs on: own_addresses_command, agent_start_command, key_add_command and collector_trigger_command with collector_trigger_timeout_seconds (the trigger used to run under the ssh connect timeout, so it now has its own bound), ssh_forward_command as one template that carries every option of the tunnel with {ssh_port}, {key_path}, {remote_port}, {local_port}, {user}, {host}, {remote_bind_address} and the keepalive and connect bounds substituted, remote_bind_address, the variable names the unlock reads and sets (agent_socket_env_key, agent_pid_env_key, display_env_key, passphrase_env_key and the askpass_env table), the askpass helper (dir prefix, file name and content, with a check that the content prints passphrase_env_key), forward_outcome_poll_seconds, state_temp_file_suffix and state_json_indent. The module now composes the ssh call through the shared substituted_command, kills the agent with signal.SIGTERM instead of the bare 15, and reads every environment name from the table; a check demands the placeholders of each command, so a template that lost one fails the suite instead of building a broken call. Three new tests hold the behaviour: the ssh call follows a changed template, the unlock sets the configured variable names and runs the configured ssh-add call, and the state write uses the configured suffix and indentation. The Parameters section of the spec names all of them.

Done 2026-09-12: the report vocabulary of the address commands left the code. [engine] gained report_json_indent (the indentation of every printed document, which was the literal 2 in seven printers), report_record_keys (the name of each report field by its meaning, so the commands and the collector agree on the shape without a copy in each module), ssh_report_command_format, ssh_report_proxy_option_format, ssh_report_socks_command_format and ssh_report_proxy_host (the printed ssh command of a record, which ssh_access.py carried as three constants and six modules rendered through it). The four channel names became report_channel_name of the sections that own the channels: i2pd_service_setup, tor_setup, yggdrasil_service_setup and port_forwarding_setup. pyntara.ssh_access now takes the engine, so the builder is still the only place that renders the command, and every caller (i2pd_address, tor_address, yggdrasil_address, port_forwarding_state, network_addresses, public_address_report, country_report) reads the field names from the mapping. The record fields of the port forwarding state kept their exact names (server, local_port, remote_port), because the collector parses them, so report_record_keys carries those too. A new test changes the channel name and two field names of a report and asserts the printed document follows them.

Done 2026-09-12: the tool vocabulary of the yggdrasil task left the code. [yggdrasil_service_setup] gained the fifteen commands the task and the deployed address command run, each carrying the name it acts on as a placeholder: installed_version_command, export_key_from_config_command ({config_path}), generate_config_command, export_key_from_stdin_command, peers_latency_command and self_address_command (yggdrasilctl -json getPeers and getSelf, now one definition shared with pyntara.yggdrasil_address), journal_connected_query_command ({service_unit_name}, {probe_seconds}), service_start_command, service_restart_command and service_enable_command, nmcli_reload_command, nmcli_connection_show_command and nmcli_connection_delete_command, ip_link_show_command and ip_link_delete_command. The document the task writes left the code with them: config_document_keys names every key of the rendered yggdrasil configuration (including the multicast block keys) and admin_output_keys every field of the admin socket answer, so the writer and the reader of the peers key cannot disagree, and the schema is visible in the config instead of the module. The remaining values of the section are asset_name_template with release_tag_prefix, the body of the NetworkManager drop-in (nm_unmanaged_conf_body), the netplan marker and the two suffixes of that file, the temporary tarball name parts, the markdown suffix of the peer list, line_separator and config_json_indent. utils.substituted_command fills the placeholders, pyntara.yggdrasil.self_address_from_output takes the field name, and tests/config_checks.py refuses a command whose configured placeholder is missing, so a typo fails the suite instead of raising on the target machine. Three proofs hold the block: another asset template changes the name the task looks for, another document key set plus another separator is what the rendered file and the reader follow, and another journal command is the argv the task runs. Deliberately left for its own block: the shared service helpers of utils.py (service_is_active, service_is_enabled, service_show_main_pid) still spell systemctl is-active, is-enabled and show themselves, and the probe commands of network_addresses (ip -j addr show and its family tables) belong to the system_metrics_setup audit.

Done 2026-09-12: wave 5, the guard. tests/test_config_value_guard.py reads every module of the package except the config layer and refuses a value of a listed type written in code, so the rule of docs/spec/config-content.md is checked by the suite instead of by attention. Four shapes are recognised: a module level constant of a value (the name in capitals, which is the shape the audit calls a module level constant), a command argv written as a list literal whose first element is a program name, an absolute path literal outside /proc, /sys and /dev, and a regular expression literal that two or more modules share, because a value of an exception type is written once and imported. Each shape carries the allowlist of what is left, documented as a spec exception (kernel and device paths, regular expressions, the wording of a message, the names of the config keys a deployed component reads, the algorithm tables of the proquint encoding and of the I2P address format) or as the work of the migration; the comparison is symmetric, so a new value in code fails the suite and an entry whose value moved into the config fails it until the entry is deleted in the same commit. The guard is proven by two tests of its own: the comparison refuses a new entry and a stale entry, and each rule finds its shape in a planted module, including the kernel path it must not report. The allowlists are now the precise work list of the migration, and the guard makes it shrink: 27 modules with a value constant, 18 modules with a command argv and the augeas node prefix are the values still in code.

Done 2026-09-12, the first task after the guard, taken from its allowlist: swapfile_service_install. Nine values left the module, and the guard entry for it is gone. [swapfile_service_install] gained the whole command set the task runs - swap_show_command, swap_on_command, swap_off_command, create_command ({size_mb}, {swapfile_path}), chmod_command ({file_mode}, {swapfile_path}), format_command ({swapfile_path}), systemctl_daemon_reload_command and systemctl_enable_command ({service_unit_name}) - and unit_template_file_name, the name of the unit template under task_data/, which was the last string the task formatted into a path by hand. The helper _swap_active now takes the section, the mode reaches chmod as the configured {file_mode} text, and every command is rendered through the shared utils.substituted_command. The checks gained a helper, _placeholder_command_field, which refuses a command array that lost one of the placeholders its call fills in, so a typo fails the suite instead of raising on the target machine; the four declaration places carry the nine keys. Two proofs hold the block: another allocation, mode and enable command in the config are exactly the argv the run carries out with the placeholders filled in, and another template name in the config is the template the run renders while the other file of the directory stays unread. The task test helpers gained the parameters the proofs need (make_config keywords and the fixture template name), and the docstring of the module no longer names the tools it used to spell out.

Done 2026-09-12, the second task after the guard: zswap_service. Two values left the module, the two systemctl calls of the run, which are now [zswap_service] keys with the unit name as their placeholder (systemctl_daemon_reload_command and systemctl_enable_command). The task renders them through the shared utils.substituted_command, the checks validate both arrays and demand the placeholder of the enable call, the mirror of the shared test document carries them, and a proof shows that another enable command in the config is the argv the run carries out while the parameters are already at their target values. The section was otherwise already complete: the parameter names, the kernel attribute directory, the unit template name and the service unit name all came from the config, and the guard entry for the module is gone.

Done 2026-09-12, the third task after the guard: zram_service, taken in full. Sixteen values left the module. The commands of the run are now section keys with the placeholders their call fills in: swap_show_command (the active swap listing), module_load_command ({module_name}), swap_off_command, format_command and swap_on_command ({device_path} and {swap_priority}) and the two systemctl calls, the second with {service_unit_name}. The module name itself is a key, because the load command and the rendered unit both name it, and the unit template name is one too, so the last path the task composed by hand is gone. The ExecStart block of the unit, which the task expands per device at render time, became seven line templates that carry the systemd directive, the programs and the redirections as text while the kernel attribute paths, the device paths, the size and the priority enter as placeholders: unit_load_line, unit_add_read_line, unit_add_write_line, unit_algorithm_line, unit_disksize_line, unit_format_line and unit_swap_on_line. The kernel and device paths themselves stay in code, which is the exception of the config spec. The checks gained one helper next to the command one, _placeholder_text_field, which refuses a line template that lost a placeholder, and every command array is checked for the placeholders it is rendered with. The four declaration places carry the sixteen keys, the mirror of the shared test document and the two config files follow, and the guard entries of the module are gone. Two proofs hold the block: another load command, another activation call (the same program with an extra flag, so the harness still recognises the swap action) and another algorithm line in the config are what the run carries out and writes, and the unit fixture now reports the path of its attribute, so the rendered unit names the fixture paths the task read the values from.

Done 2026-09-12, the fourth block after the guard, a cross cutting one: the wrapper that runs a command as the desktop user. The same argv shape lived in four task modules (chrome_setup, kde_settings, kde_keyboard_setup and vocalinux_setup, 27 call sites in total), so the guard counted it four times. It is now one key per owning section, runuser_command with the {username} placeholder, following the decision the config spec takes for a value that belongs to a section: a section owns the calls of the tools it drives, which is why the systemctl commands are keys of five sections as well. The four helpers keep their signature and read the prefix from their own config, so no call site changed; the helper renders it through the shared utils.substituted_command. The four declaration places carry the key, the mirror of the shared test document and the four config files follow, and the guard entries of the four modules lost the wrapper line. Four proofs, one per task, show that another wrapper in the config is the argv the task builds. A decision worth recording: the plain wrapper could live in [engine] as one value, but playwright_setup already needed a wrapper of its own that also exports HOME, and the four tasks read their own table today; unifying them is a possible later cleanup, not a blocker.

Done 2026-09-12, the fifth block after the guard: ssh_client_setup. One value left the module, the command that prints the effective client configuration for the verification of the drop-in, which is now the [ssh_client_setup] key effective_config_command (ssh -G example.com by default, with the host the probe asks about inside the command). The helper takes the section and the check reads the array, the four declaration places carry the key, the mirror of the shared test document and the config file follow, and the guard entry of the module is gone, which the guard itself demanded in the same commit. A proof shows that another probe host in the config is the argv the run uses to verify the drop-in while the default probe is never called. The task is otherwise complete: the drop-in path, the mode, the header, the augeas lens and container, the package and the directives all came from the config.

Done 2026-09-12, the sixth block after the guard: the vocabulary of the UPnP client, which lived inside the shared pyntara.upnp module. It is now the [engine] table, because the module is shared and the calling task only names its own binary: upnpc_status_command, upnpc_mapping_list_command and upnpc_mapping_add_command are the three calls with the {command} placeholder for that binary, upnpc_external_address_key the field of the status output the router address is read from, upnpc_protocol_names the protocols of a mapping line and upnpc_mapping_arrow the arrow of that line. The helpers gained the engine as their first argument (router_external_address, list_mappings, ensure_port_forwarding, forward_inbound_port) and the parse helpers take the vocabulary they read with (parse_external_address(text, address_key), parse_port_mappings(text, protocol_names, arrow), mapping_exists(..., protocol_names, arrow)), so no literal of the client is left in the module; the module constant EXTERNAL_ADDRESS_KEY and the protocol tuple are gone together with the guard entry. The 3x-ui task passes the engine and its own [three_x_ui_xray_setup] key upnp_protocol (TCP by default), which replaces the two protocol literals of its call sites. The four declaration places carry the keys of both tables, the tests of the shared module read the vocabulary from the engine table instead of repeating it and gained a proof that another field name in the config is the one the parser reads, and the tests of the task follow the new signatures and prove that the protocol of the calls comes from the section. The spec of the task names the engine vocabulary and the new key.

Done 2026-09-12, the seventh block after the guard: the KConfig vocabulary of chrome_setup. The task reads and writes the appletsrc through the KConfig tools, and both calls spelled the tool, the file selector and the group and key selectors in code. They are now four keys of the section: kreadconfig_command and kwriteconfig_command carry the tool and the file as {file_name}, config_group_flag the group selector as {group} and config_key_flag the key selector as {key}, and the value of a write stays a positional argument the code appends, because it is a value of the record and not of the call. The two calls now share one builder, _kconfig_command(cfg, base_command, group_segments, key), so the reader and the writer can never drift apart, which is also what makes the block testable as a unit. The four declaration places carry the keys, the mirror of the shared test document follows, the guard entries of the module are gone, and a proof shows that another reader, another file selector and another group selector in the config are exactly the argv the task builds. The same KConfig shape lives in kde_settings, kde_keyboard_setup and vocalinux_setup; the next KDE block moves it there with the same builder per module, because those tables own their own tool calls.

Done 2026-09-12, the eighth block after the guard, the second half of the KConfig vocabulary: kde_settings, kde_keyboard_setup and vocalinux_setup. Each section gained the two base calls with the {file_name} placeholder and the group and key selectors ({group} and {key}), kde_settings additionally the boolean type spelling (config_bool_type_flag), the live write flag (config_notify_flag) and the delete flag (config_delete_flag), and kde_keyboard_setup the boolean type spelling, so the three tasks spell no KConfig token of their own. Each module built the same call in up to four places; every module now has one builder (_kconfig_command), which is what makes the block testable and keeps the reader, the writer and the delete of a module from drifting apart. The kde_settings builder also serves the system files of SDDM, whose two helpers gained the config as their first argument. The four declaration places carry the keys of the three sections, the mirror of the shared test document follows, and the guard entries of the three modules lost the KConfig lines. Three proofs, one per task, show that another reader and writer and other selectors in the config are exactly the argv the task builds.

Done 2026-09-12, the ninth block after the guard: the three plasma-apply calls of kde_settings. The global theme, the color scheme and the cursor theme are applied by three tools whose flags and whose value were spelled in the module; they are now the section keys apply_look_and_feel_command, apply_color_scheme_command and apply_cursor_theme_command, each carrying its value as a placeholder, and one builder _appearance_command(cfg, value_name) renders them, so the three call sites share the mapping between the value and the call. The four declaration places carry the keys, the mirror of the shared test document follows, the guard lines of the module are gone, and a proof shows that another tool and another flag in the config are the argv the task runs while the value substituted into the placeholder is the configured one.

Done 2026-09-12, the tenth block after the guard: the file operations of the three desktop tasks. kde_settings, kde_keyboard_setup and vocalinux_setup created their directories, gave files to the desktop user and set their modes with argv spelled in the modules, and the same three shapes appeared eight times. They are now the section keys mkdir_command ({path}), chown_command ({owner} and {path}), chmod_command ({file_mode} and {path}) and, for kde_settings alone, chown_recursive_command, which owns the recursive flag of the copied theme tree instead of leaving it to the call site. The mode enters the template in the spelling the section chose: kde_settings and kde_keyboard_setup write four octal digits, vocalinux_setup keeps the plain octal form its task already used, and the two spellings live in the config where they can be read. The four declaration places carry the keys of the three sections, the mirror of the shared test document follows, and the guard entries of kde_settings and kde_keyboard_setup are gone in full, so the two modules hold no argv literal at all; the vocalinux entry keeps the three lines of the user manager and the group membership, which the next block moves. Three proofs, one per task, show that another program in the config is the argv the task runs around the file, and the keyboard proof also needed the shared config factory to accept the command, so tests/support.py gained kde_keyboard_setup_mkdir_command.

Done 2026-09-12, the eleventh block after the guard: the group membership and the user manager of vocalinux_setup, which closed the module. The reader of the membership (["id", "-nG", username]) and the group writer (["usermod", "-aG", group, user]) became group_members_command and group_add_command, and the prefix the task built for the user manager became two full templates instead of a prefix with the verbs appended in code: service_active_command and service_enable_command, each naming the machine selector and the {username} and {service_unit_name} placeholders, so the state query and the enable read as the commands they are and the guard of the module is empty. Two proofs show that another reader, another group writer, another state query and another enable in the config are exactly the argv the task runs, one for the membership and one for the unit. The section test grew the four keys in its section literal, without which a variant would have mutated nothing and passed silently, and four wrong values now fail the section: a string where the membership array belongs, an array without its placeholders, an empty array and a string where the enable array belongs. Found on the way and fixed: the Parameters list of docs/spec/vocalinux-setup.md had two entries run together on one line and it named none of the command keys of the last three blocks, so the list now carries the runuser wrapper, the KConfig vocabulary and the file operations as well; the dataclass docstring of the section describes the command vocabulary too.

Done 2026-09-12, the twelfth block after the guard: the commands of ssh_daemon_setup, which closed that module. Seven argv lines left it: the daemon query (["sshd", "-T"]) and the listener query (["ss", "-tlnp"]) became effective_config_command and listening_sockets_command, and the five systemctl calls became socket_disable_command, service_enable_command, service_start_command, service_restart_command and service_reload_command, each naming its unit through {socket_unit_name} or {service_unit_name}. The two query helpers now take the config, because the command is a value of the section: _verify_effective_config(cfg, directives, timeout) and _verify_listening_port(cfg, port, timeout). Two proofs: the first run of the task shows that the daemon query, the listener query, the socket disable, the enable and the start are the configured argv, and a parametrised proof shows the restart on a port change and the reload on any other change, so all seven keys are held by a test. The spec lists the seven keys and the guard entry of the module is empty. Noted for the next blocks: the progress lines of this module still name the commands it runs (enabling service: systemctl enable ...); that is message wording, which the spec keeps out of the config, and the same shape lives in the other tasks that log a command they are about to run.

Done 2026-09-12, the thirteenth block after the guard: the systemctl vocabulary of system_metrics_setup, which closed that module. Eight argv lines spelled the reload of systemd and the enable, restart and start of a unit, and three of them were the same call on a fourth unit, so the block moved four shapes instead of eight calls: systemctl_daemon_reload_command, systemctl_enable_command, systemctl_restart_command and systemctl_start_command, the unit entering through the {unit_name} placeholder, which serves the service, the ingest path unit and the collector timer because the task runs the same call on each of them. The four keys carry no unit name of their own on purpose: the unit keys of the section (service_unit_name, ingest_service_unit_name, ingest_path_unit_name and the two of the collector) stay the only place where a unit file name lives. One proof covers all four keys: the first run of the task shows the configured reload and start, and a forced second run shows the configured enable and restart. The guard entry of the module is empty, and the dataclass docstring names the four keys.

Next in the migration, with the shape of the work that remains: src/pyntara/augeas.py (the augtool argv and the /files node prefix, shared by the two ssh tasks), src/pyntara/public_address.py (three iproute2 queries, which need an engine argument threaded to their callers), src/pyntara/xui.py (the panel vocabulary of the settings payload), src/pyntara/tasks/three_x_ui_xray_setup.py (the x-ui tool calls, the acme commands and the listener probe; the guard sees only four of them, so this module must be read, not scanned) and src/pyntara/utils.py (the apt and package group first, then the service group, with 88 call sites).

Done 2026-09-12, the fourteenth block after the guard: the address queries of the shared helper pyntara.public_address. Three iproute2 calls lived in the helper: the global-scope addresses of the interfaces, the kernel routes of both families and the default route. They are now [engine] keys, because they are queries of the host any task may need and not the vocabulary of one task: local_addresses_command, directly_connected_networks_command, whose {family} placeholder takes the -4 or -6 flag of the loop, and default_route_command. The three functions grew an engine argument (local_addresses(engine, timeout), directly_connected_networks(engine, timeout), default_route_address(engine, timeout)), and their four call sites followed: upnp.forward_inbound_port, which already had the engine, and the two sites of three_x_ui_xray_setup. One proof replaces the three commands with a marker program and checks the argv of all three helpers, including the rendered family flags. The guard entry of the module is empty. Note on the documentation: the engine vocabulary of this kind is documented by the comments of config/engine.toml, as the upnpc keys of the earlier block are, because no spec file enumerates the [engine] table; the docstring of each helper now says the query is configured instead of quoting it.

Done 2026-09-12, the fifteenth block after the guard: the augtool vocabulary of the shared helper pyntara.augeas. The driver argv (["augtool", "--noautoload"]) was written twice and the node prefix /files three times in two functions; both are now [engine] keys, augtool_command and augeas_files_node_prefix, because the tool serves the two ssh tasks and not one of them. The three public functions grew an engine argument and the node of the script is built once from the prefix, so read_dropin_state, write_dropin and sync_dropin spell no node path of their own; the two call sites in ssh_client_setup and ssh_daemon_setup pass ctx.config.engine. The block also closed the last entry of the path literal allowlist, so PATH_LITERALS_ALLOWED is now empty and only the kernel paths remain exempt. A new unit test file covers the helper directly for the first time (tests/test_augeas.py had no counterpart): the proof replaces both values with a marker tool and a marker prefix and checks the argv and the script, which also documents why the parse still returns the map. Note: the augeas program itself (the /augeas/load entries and the set and rm lines) stays in the helper, because it is the interface of the tool and a template of it would hide the logic that builds one directive per record.

Done 2026-09-12, the sixteenth block after the guard: the address vocabulary of pyntara.network_addresses, the command the System Metrics collector runs as its ipv4 and ipv6 network modules. Four module constants left the module: the iproute2 query in JSON form, the mapping of the command line family flag to the report family, the mapping of that family to the name iproute2 prints, and the scope value that counts as a link scope. They are [engine] keys now (interface_addresses_command, address_family_by_flag, iproute2_address_family_names, link_scope_name), because they are the vocabulary of iproute2 and of the command line of that module, the same kind as the address queries of pyntara.public_address that moved in an earlier block. parse_interface_addresses takes the engine, InterfaceAddress.ssh_target became a method taking the configured link scope name, because a property cannot read the config, and main reads the flag mapping from the loaded config, so the unknown-family usage error is judged against the configuration instead of a constant: tests/test_network_addresses.py needed a real config in test_usage_requires_a_known_family and passes the engine at its two parser call sites. One proof replaces all four values with marker ones and checks the argv the command runs and the family and zone the records carry; it showed on the way that the zone rule keys on the family name ipv6, which is a protocol fact and stays in the code. The guard entry of the module is empty, and docs/spec/system-metrics.md names the four keys, so the collector specification says where the read vocabulary lives.

Done 2026-09-12, the seventeenth block after the guard: the country word constant is gone. DEFAULT_COUNTRY_WORD = "russia" of pyntara.location was read by nothing in src: the word comes from the country_word key of [three_x_ui_xray_setup], which the task passes to detect_country, and find_country_word already returns None for an empty word. The constant therefore existed for the tests only, which is a value of the config type living in code without a reader. It is deleted, and tests/test_location.py declares the word it searches for in its own COUNTRY_WORD constant, so the module owns no country at all. The guard entry of location.py is removed with it; nothing else changed, because the docstring of find_country_word already carried the semantics the comment above the constant described (the case and separator insensitive search, including the Prussia caveat).

Done 2026-09-12, the eighteenth block after the guard: the panel vocabulary of pyntara.routing_policy. Three constants named the words of the 3x-ui panel itself: the inbound protocol the policy asks for (PANEL_MIXED_PROTOCOL), the rule protocols that count as a panel restriction (PANEL_BLOCKED_PROTOCOLS) and the address category of the rule that blocks every private destination (PANEL_PRIVATE_BLOCK_CATEGORY). They are [three_x_ui_xray_setup] keys now (panel_inbound_protocol, panel_blocked_rule_protocols, panel_private_block_category), because the panel is the external system that section configures, and LocalProxyPolicy carries them like every other value the rules are built from, so _is_panel_restriction and build_local_proxy_inbound read the policy instead of a module constant. The builder of the inbound grew a protocol keyword, because it receives the values of one inbound rather than the whole policy, and the task passes cfg.panel_inbound_protocol. Two hand-built fixtures needed the three fields: the defaults dict of tests/test_xui.py, which builds ThreeXuiXraySetupConfig by hand, and the make_policy helper of tests/test_routing_policy.py, plus the policy of the routing stage test of tests/test_3x_ui_xray_setup.py. One proof sets all three to marker values and checks the payload protocol and the two rules the policy then removes; the guard entry of the module is empty and docs/spec/3x-ui.md lists the three keys.

Done 2026-09-12, the nineteenth block after the guard: the panel vocabulary of pyntara.xui, the client of the 3x-ui REST API. Three constants named the two kinds of the panel geodata check (GEODATA_DOMAIN_KIND, GEODATA_IP_KIND, GEODATA_KINDS) and one payload line asked the panel to sniff http and tls on the universal inbound. The kinds are [three_x_ui_xray_setup] keys now (panel_geodata_domain_kind, panel_geodata_ip_kind), because the panel receives them as its own words in the validation request form, and the sniffing set is inbound_sniffing_protocols, the same shape the local proxy inbound already reads through local_proxy_sniffing_protocols. validate_geodata_tokens judges the kind against the configured pair instead of a module tuple, the two call sites of the task pass cfg.panel_geodata_domain_kind and cfg.panel_geodata_ip_kind, and build_vless_reality_payload takes the sniffing protocols as a keyword, because it receives the values of one payload rather than the whole config. tests/test_xui.py reads the kinds from its own config helper where it used the constants, passes the protocols at its two builder call sites, and its hand-built config needed the six fields of this and the previous block. One proof sets a marker protocol pair and marker kinds and checks the payload and the request form the panel receives; the guard lists no entry for xui.py at all, and docs/spec/3x-ui.md names the three keys next to the panel vocabulary of the earlier block.

Done 2026-09-12, the twentieth block after the guard: the tag prefix lost its second copy. rustdesk_setup and three_x_ui_xray_setup each carried the identical TAG_VERSION_PATTERN = re.compile(r"^v?") and the identical _normalized_version helper, and the guard counted the pattern as one regular expression shared by two modules. Both are one helper now, utils.version_without_tag_prefix, and the block also dropped the regular expression altogether: a single leading v is stripped by str.removeprefix, so the shared pattern disappears instead of moving, which is simpler and exactly the same behaviour (the docstring of the helper states that only the first character is judged). Both tasks call it, the guard's duplicated pattern entry for "^v?" is gone, and the TAG_VERSION_PATTERN lines left the two module entries of the value constant allowlist. A new test in tests/test_utils.py holds the behaviour: both spellings compare equal, a double v loses only one character, an empty string stays empty.

Done 2026-09-12, the twenty-first block after the guard: the root of the clone is computed by the composition root alone. utils.REPO_ROOT was the single definition and the entry point imported it, which made utils the owner of a path that belongs to the place where the run starts; the constant now lives in src/pyntara/pyntara.py next to its own CONFIG_PATH, where the Context is built, because both name where the running code itself lives and neither is a value of the target machine. The guard of tests/test_utils.py was rewritten to say that: no module except the composition root may name REPO_ROOT, and the composition root must compute it. Three places that read the constant followed: tests/support.py (its own root, computed from the test file), tests/test_tor_setup.py (the same) and the value constant allowlist of the guard, whose composition root entry now carries both paths with a comment that names the exception they belong to (the location of the code, not a setting of the machine).

Done 2026-09-12, the twenty-second block after the guard: the identity of the panel binary in three_x_ui_xray_setup. The task built the path of the binary as install_dir / "x-ui" in seven places and handed the process name "x-ui" to the port helpers in four, so one file name and one process name were repeated eleven times. They are [three_x_ui_xray_setup] keys now (binary_file_name, service_process_name) and the task builds the path through one helper, _panel_binary(cfg), so a release that renames its binary or its process is a config change. Two proofs, one per value: another file name is the argv the version probe runs, another process name is what the task hands to the port check. Two test fixtures that build the section by hand needed the fields: the defaults dict of tests/test_xui.py and the mirror of tests/config_helpers.py, whose duplicate insertion was caught by the section becoming unparsable (the tolerant reader returned None for github_repo, and 62 tests failed) and fixed in place. Deliberately left for the next block, with the reason: the argv shapes of the panel calls themselves (setting -show, -port, -username/-password/-webBasePath, cert -webCert, the -v probe). About fifteen assertions of tests/test_3x_ui_xray_setup.py match on command[1:3], so moving those verbs into templates is a sweeping edit of that test file and belongs in a block of its own.

Done 2026-09-12, the twenty-third block after the guard: the argv of the panel CLI in three_x_ui_xray_setup. Five calls of the installed binary spelled their verbs and flags in code: the version probe, the settings query, the port setting, the credentials setting and the certificate pointer, the last one used twice. They are [three_x_ui_xray_setup] keys now (panel_version_command, panel_settings_query_command, panel_port_command, panel_credentials_command, panel_certificate_command), each carrying the path of the binary as {binary} and the values of its call site as their own placeholders, and one builder _panel_command(cfg, template, **values) fills them, so the binary path enters all of them from the one place that knows it. The worry recorded in the previous block did not materialise: the shipped templates render exactly the argv the module used to build, so the fifteen test assertions that match on command[1:3] and the fakes that dispatch on those verbs kept working unchanged; tests/test_xui.py, whose hand-built section feeds the whole client suite, needed the five fields. Two proofs: another binary name and another version flag are the argv the version probe runs, and the shared builder fills the placeholders of another port template (its flag and its value), which is the mechanism every one of the five call sites uses. Next in this module, with the same shape: the installer script call, the acme installer pipeline, the python3 listener probe, the five acme.sh steps, the two openssl calls and the four systemctl restarts.

Done 2026-09-12, the twenty-fourth block after the guard: the commands of the tools of three_x_ui_xray_setup, which closed the module. Fifteen values left it: the downloaded installer script call (installer_run_command), the acme.sh installer pipeline (acme_install_command), the directory and the file name the tool installs itself under (acme_dir_relative_path, acme_file_name, read by _acme_path(cfg) now), the temporary HTTP listener of the port probe (acme_port_listener_command), the four acme.sh steps of the certificate issue (acme_set_default_ca_command, acme_issue_command, acme_installcert_command, acme_upgrade_command, carrying {acme}, {domain}, {http_port}, {key_file}, {fullchain_file} and {reload_command}), the restart that acme runs through its reloadcmd (acme_reload_command, a text template filled with str.format like the chrome_setup flags), the two openssl calls (openssl_check_command, openssl_generate_command) with the subject template and the days of a self-signed certificate inside the generate template, and the restart of the panel unit (service_restart_command, four call sites). The number of days a certificate lives is part of its template: it is a config value that way without a key of its own. _ensure_acme and _acme_path take the config, and _run_installer takes it too, because the script call is a value. Two proofs: another template set for the four acme steps plus another reload line and another tool path are exactly the argv the issue runs, and another restart template is what the port convergence runs. Two test fakes and two lambdas that patched the acme helpers needed the new signature. The guard's command argv list now names only utils.py: every task module of the package is free of a literal command.

Done 2026-09-12, the twenty-fifth block after the guard, the first piece of the shared apt helpers: the architecture query. dpkg_architecture spelled ["dpkg", "--print-architecture"] in the shared helper, and the tool is the same for the five tasks that read the architecture of the machine, so the command is the [engine] key dpkg_architecture_command. The helper takes the engine (dpkg_architecture(engine, timeout)) and the five call sites pass their ctx.config.engine or the engine they already hold: rustdesk_setup, yggdrasil_service_setup, i2pd_service_setup, dnsproxy_setup and vocalinux_setup twice. One proof: another query in the engine table is exactly the argv the helper runs. The plan of the following blocks is recorded so the sweep stays a choice and not a surprise: package_is_installed (13 call sites), then install_packages with install_package_once and refresh_apt_index plus the non-interactive apt environment (about 25 call sites), then the service group (is-active, is-enabled, MainPID, the port listener and the stop call). The status query of dpkg-query carries the literal ${Status}, which is why it will need a template whose braces are escaped for the substitution helper; the note is kept here for that block.

Done 2026-09-12, the twenty-sixth block after the guard, a small one that closes the record rather than a module: the allowlists of tests/test_config_value_guard.py now say what every remaining entry is. The comment above the module level constants named values that had already left the code (the iproute2 vocabulary of the address module, the panel vocabulary of the routing policy and of the xui client, the country word the echo services report, the field name of the UPnP answer, the augeas node prefix of the two ssh tasks), so a reader could not tell the exceptions from the work that is left. It now separates them. The exceptions of docs/spec/config-content.md are the regular expressions (the augeas node pattern, the profile identifier of the dnsproxy, the release and address patterns of the setup tasks), the wording of a message (the note an address lookup leaves when no service answers), the numbers of an encoding (the proquint alphabet, the identity size and the certificate type of an i2pd key) and the kernel paths of the swapfile and the zram modules, with the config path and the repository root of the entry point as the layout of the installation, which stays where the run starts. The entries that still wait are named one by one: the environment of the noninteractive apt, the key names the three metrics modules read from the component config, and the version pattern the setup tasks copy. The comment above the command argv list says that the package group of the shared helpers is the last module on that list, and the comment above the shared patterns says that a fifth copy of the three part version fails the suite and asks for the shared vocabulary of the engine table instead. No code changed, so the gate after this block is the gate before it.

Done 2026-09-12, the twenty-seventh block after the guard, the second piece of the shared apt helpers: the package status query. package_is_installed spelled ["dpkg-query", "-W", "-f=${Status}", package] in the shared helper, so the flags of dpkg-query lived in code. They are now the [engine] key package_status_query_command, whose value doubles the braces of the literal ${Status} because the substitution helper formats the command as a template, so the run receives a single pair; the TOML comment says so and a proof checks both the shipped template and a replaced one. The helper takes the engine (package_is_installed(engine, package, timeout)) and thirteen call sites follow: the tasks that own a package list pass ctx.config.engine or the engine already in hand, and two helpers of other modules grew the parameter, augeas.ensure_augtool(engine, package_name, ...) and chrome_setup._ensure_chrome_installed(cfg, engine, ...). The four helpers of three_x_ui_xray_setup that reach the query through each other (_ensure_upnp_client, _ensure_openssl, _ensure_self_signed_cert, _stage_ssl) take the engine as their first argument, so it travels from _collect_run_facts and from task; their tests followed in one file, which is where the whole sweep landed on the test side (the fakes of the four helpers and of the query itself). Two proofs in tests/test_utils.py: the shipped template with the doubled braces and another argv in the engine table, then the installed status line deciding a leftover configuration. The guard lost its dpkg-query line; the apt install argv, the apt-get update argv with the noninteractive environment, and the service group of utils are the blocks that remain on that list.

Done 2026-09-12, the twenty-eighth block after the guard, closing the shared apt helpers: the index refresh, the install of one package and the noninteractive environment. utils spelled ["apt-get", "update"], ["apt-get", "install", "-y", package] and held APT_NONINTERACTIVE_ENV = {"DEBIAN_FRONTEND": "noninteractive"}; the three are now the [engine] keys apt_update_command, apt_install_command and apt_noninteractive_environment, so the vocabulary of apt is a config value and no module states the variable apt needs to stay quiet on the target machine, where nobody answers a prompt. The three helpers take the engine (refresh_apt_index(engine, timeout), install_package_once(engine, package, timeout), install_packages(engine, packages, *, ...)) and hand it to each other, and every call site follows: the package lists of cli_tools, ffmpeg_setup, imagemagick_setup, playwright_setup and vocalinux_setup, the per-package loops of kde_settings and kde_keyboard_setup, the two clients of three_x_ui_xray_setup, the single install of chrome_setup and of tor_setup, the three _install_deb helpers of rustdesk_setup, i2pd_service_setup and yggdrasil_service_setup (each grew the parameter and is called from its task), the direct refresh of add_extra_repos, the refresh and install of ssh_daemon_setup through _ensure_package(engine, cfg, timeout, skip_update), and the caller of install_packages in augeas.ensure_augtool. Two proofs in tests/test_utils.py: the configured argv and environment of both calls with a replaced table, and one refresh for a list of two packages with no refresh at all when the run skips it. The guard lost the environment constant and the two apt argv lines; the service group of utils (is-active, is-enabled, MainPID, the port listener and the stop call) is the last block on that list.

Done 2026-09-12, the twenty-ninth block after the guard, the first half of the service group of utils: the state queries. service_is_enabled and service_is_active spelled ["systemctl", "is-enabled", name] and ["systemctl", "is-active", name] and compared the output against the literals "enabled", "enabled-runtime" and "active". The argv are now the [engine] keys systemctl_is_enabled_command and systemctl_is_active_command (both with the {unit} placeholder) and the states are systemd_enabled_states together with systemd_active_state, so the vocabulary of systemd is a config value like the iproute2 one. The two helpers take the engine (service_is_enabled(engine, name, timeout)) and every call site follows: the tasks that only check their own unit pass ctx.config.engine, and five modules carry it into their readiness loop, _wait_active(engine, service_name, attempts, retry_delay_seconds, timeout) in dnsproxy_setup, i2pd_service_setup, ssh_daemon_setup, three_x_ui_xray_setup and tor_setup, plus _started_ok(engine, cfg, service_name, timeout) of port_forwarding_setup and the two cleanup helpers of yggdrasil_service_setup, _cleanup_leftover_interface(engine, cfg, timeout) and _restart_service(engine, cfg, timeout), which needed the EngineConfig import as well. One proof in tests/test_utils.py: a replaced table with its own argv and state words returns true and runs exactly those commands; the five cleanup tests of yggdrasil_service_setup and the two state tests of utils pass the engine. The guard lost the is-enabled and is-active lines; the port listener, the MainPID query and the stop call of utils are the last three entries on its list.

Done 2026-09-12, the thirtieth block after the guard, which closes the service group of utils and the command argv rule of the guard. utils spelled ["ss", "-tlnp", f"sport = :{port}"], ["systemctl", "show", "-p", "MainPID", "--value", name] and ["systemctl", "stop", unit]; the three are now the [engine] keys socket_listener_command, systemctl_main_pid_command and systemctl_stop_command, so port_listener_pid(engine, port, timeout), service_main_pid(engine, service_name, timeout) and ensure_port_free(engine, port, service_unit_name, timeout, *, ...) read the argv of ss and systemctl from the same table as everything else, the stop included. The call sites followed: the two port checks of chrome_setup through _local_proxy_server(engine, cfg, *, timeout), and four calls of three_x_ui_xray_setup, which carried the engine into _converge_panel_port(engine, cfg, timeout) and _issue_trusted_cert(engine, cfg, ip, timeout). Two proofs in tests/test_utils.py: another argv for the two queries fills the port and the unit placeholders, and the stop path is the shipped table with its own stop command, so the whole sequence (ss query, MainPID query, stop, confirming query) is pinned. The command argv rule of the guard is empty now: no module of the package writes an argv in code, and this is the first of the four rules to reach its final state. The value constants that remain are the documented exceptions plus the key names of the three metrics modules; the shared version pattern count and the documentation closing with a live installer run are the work that is left.

Done 2026-09-12, the thirty-first block after the guard: the key names of the deployed metrics components moved to the config layer, which closes the last value constant that was not an exception. The three components carried their own copy of the keys they read from [system_metrics_setup] and from its collector sub-table, SERVICE_CONFIG_KEYS in metrics.py, COLLECTOR_SECTION_KEYS with COLLECTOR_TABLE_KEYS in metrics_collect.py and INGEST_CONFIG_KEYS in metrics_ingest.py, and the whole point of the list is to name the keys a component cannot find, so the list belongs to the table it describes and not to the reader. The four lists now live in src/pyntara/config/system_metrics_setup.py, next to the fields they name, and the components read them from pyntara.config; the config layer is the place the guard leaves out, because a name of a field is not a value the run uses. The proof grew as well: tests/test_config_coverage.py already checked that every listed name is a field of its table, and it now also reads the list through the component module and demands the very same object, so a copy written back into a component fails the suite. The guard allowlists lost the three module entries; only the documented exceptions stay, with the version pattern count (3) still open. What is left: that count, then the documentation closing with a live installer run on a target machine.

Done 2026-09-12, the thirty-second block after the guard, which closes the duplication rule of the guard: the three part version has one definition. dnsproxy_setup, i2pd_service_setup, three_x_ui_xray_setup and yggdrasil_service_setup each compiled VERSION_PATTERN, and three of them wrote the very same pattern (the count the guard kept); dnsproxy added an optional leading v, which changes nothing, because the search finds the version inside the text either way. The pattern and its reader now live in pyntara.utils: VERSION_PATTERN with version_from_output(text) returning the first three part version or None, so a task that reads a version command calls one helper and the accepted shape changes in one place. The four modules lost their constant and, where the pattern was the last use, the re import; rustdesk_setup keeps its own pattern, because a rustdesk version may carry two parts, and that difference is a fact about the tool rather than a copy. One proof in tests/test_utils.py: the reader finds the version next to words, rejects a two part number and an empty text. All four allowlists of the guard are now either empty or hold documented exceptions only: no command argv anywhere in the package, no shared pattern copy, no path literal outside the kernel, and the value constants that remain are the exceptions of docs/spec/config-content.md.

Done 2026-09-12, the thirty-third block after the guard, which closes the documentation side of the audit: docs/spec/config-content.md says what the guard now shows. The introduction described the types as naming the values the code still carries outside the config, and the section What is left to move listed the constants, the assembled commands, the foreign paths and the site addresses as the work of the migration; both were true when the audit started and had become wrong. The introduction now says the audit is closed, the paragraph about the guard states that the command argv, the path and the shared pattern allowlists are empty and that the list of value constants holds the exceptions and nothing else, the section is named The audit is closed and says that a value added to the code after this point is a defect of the same kind as one that was never moved. One decision is recorded as well, because it is a rule and not a detail: a list of the key names a table holds is not a value, it belongs to the table it describes and lives in the config layer next to the fields it names, which is why the four lists of the deployed metrics components moved there. The only work left in the audit is the live installer run on a target machine.

Done 2026-09-12, the thirty-fourth block after the guard, the live run on a real machine: the audit is now closed against the machine, not only against the suite. Two tasks ran through the installed entry point with the production vault password and the repository config: add_extra_repos reported already satisfied, and cli_tools reported already installed for its whole package set, both without touching the system, which is the idempotence the task contract promises. The vocabulary the last blocks moved was then exercised live through that same config. package_is_installed answered True for mc and False for a package that does not exist, so the doubled braces of the status query really render to -f=${Status} on the machine. service_is_active and service_is_enabled answered True for ssh.service, and is-enabled answered False for systemd-journald.service, a static unit that is correctly not enabled, which is the case the state list of the engine table exists for. service_main_pid returned the live MainPID of both units, 1780 and 451. port_listener_pid returned the pid of the listener on port 631 and None for the ports where nothing listens, so the ss filter and its pid parse work as the code spells them. version_from_output read 3.7.0 out of a version line. No defect was found: every mechanism the audit moved behaves on the machine as the suite says it does.

Done 2026-09-12, the thirty-fifth block after the guard, and the last one that moves values: the guard rule for commands was blind to a command list written over several lines, and four commands had been hiding there. The rule searched every module line by line, so a list whose first element sat on its own line, which is the shape a formatter produces, was invisible; the rule now reads the whole module with a search that crosses line breaks and records the program of the list, and a proof in tests/test_config_value_guard.py shows a multi-line tar list being caught. The four commands it had missed are now config values: three_x_ui_xray_setup compiled the openssl expiry check by hand while the [three_x_ui_xray_setup] key openssl_check_command already held the same command and nothing read it, so the key is used now; telegram_setup extracted its archive with a hand written tar argv, which is the new telegram_setup.tar_extract_command with the archive and the directory as placeholders; kde_settings built three qdbus6 calls to kwin's VirtualDesktopManager by hand, which are the new kde_settings keys kwin_desktop_count_command, kwin_desktop_create_command and kwin_desktop_remove_command, the create one carrying the empty desktop name as its own placeholder so no template holds an empty argument by accident. The two task specs grew the parameter lines, the section test literals of kde_settings and telegram_setup grew the keys, and a scan of the whole package now reports zero command literals, on one line or on many.

Done 2026-09-12, the thirty-sixth block after the guard: two commands whose first element is an expression, which the argv rule cannot see because it looks for a list that starts with a string. xui.panel_cert_value asked the panel for its certificate path with a hand written path, install_dir / "x-ui" plus setting -getCert true, while the same module already had five commands of the panel in the config table, so the call is now the new three_x_ui_xray_setup.panel_cert_query_command with the binary as its placeholder, and the path of the binary comes from binary_file_name as everywhere else. dnsproxy_setup asked the installed binary for its version with a hand written argv, which is the new dnsproxy_setup.installed_version_command with the path of the binary as its placeholder; the helper takes the config now, because the argv is a value. Two proofs: another template runs, with the binary path filling its slot, and the shipped table runs the shipped argv. The scan of the package still reports five commands whose first element is an expression, the two python -c clients of kde_settings and the three module commands of system_metrics_setup, and they are the next blocks.

Done 2026-09-12, the thirty-seventh block after the guard, which moves the five commands that a search for a list starting with a string cannot see: the interpreter stands in front of them, so the program name is an expression and the literals after it are flags and module names. system_metrics_setup built the ExecStart line of its three deployed units by hand, the send loop, the ingest oneshot service and the collector service; the three are now the keys send_service_command, ingest_service_command and collector_service_command, each with the venv interpreter and the system config path as placeholders, and the three rendering helpers take the section config. kde_settings started an embedded python client twice, for the DBus script of the desktop count and for the release of the foreign shortcut owners; the prefix of both calls is now the key python_script_command, whose {python} slot carries the system interpreter of the [engine] table, and the client source follows as the next argument of the command line, so the source itself never passes through the substitution and a client that carries braces cannot break it. Two proofs: another interpreter command in the table is exactly what a rendered unit starts with, and another prefix is exactly what the release call runs before the system interpreter and the client. The rendered unit was compared with the unit installed on the machine and is identical, so the deployment stays idempotent. The scan now reports one command left, the flag list of the dnsproxy daemon in dnsproxy_setup, which is the next block.

Done 2026-09-12, the thirty-eighth block after the guard, which closes the command search: the flag list of the dnsproxy daemon. dnsproxy_setup assembled the ExecStart line of its unit by hand, gluing every flag to its value with a string concatenation in the code, nine flags in all, including the trailing s of the timeout and the switch that takes no value. The flags are now the [dnsproxy_setup] key daemon_flag_templates, a table of templates with {value} for the value a flag carries and plain text for a switch, and the task fills them through the small _flag helper, so a daemon that renames a flag is answered in the config. One proof: a replaced table gives another listener flag and another cache switch, and the renamed flag is gone from the command. The unit installed on the machine was read and compared: the flags are the same, so the deployment stays idempotent. The two searches of the audit, the one for a list that starts with a string and the one for a list that starts with an expression, now report zero lines: every command the run invokes comes from the config, and the guard refuses a new one written in code.

Done 2026-09-13, the thirty-ninth block after the guard: the commands whose first element is an expression, in the tasks that own a config. A third search, a list that starts with something other than a string but carries a flag inside it, found what the earlier two had missed, because the second one looked for str(...), cfg. and a few other spellings of a prefix. Four commands are now keys. kde_keyboard_setup started its apply_hotkeys client by hand; the prefix is the new kde_keyboard_setup.python_script_command, the same shape the kde_settings section already carries. system_metrics_setup built four commands of the deployed venv by hand: the check that the venv imports the package and reports the repository version (venv_version_command), the creation of the venv with the configured interpreter (venv_create_command), the sync from the repository lockfile (venv_sync_command) and the arguments that reinstall the local package when the venv is refreshed (venv_reinstall_flags); the helpers _venv_package_version and _ensure_venv take the section config now. port_forwarding_setup assembled the ExecStart line of its unit from a hand written python -m, which is its module_run_command with the interpreter, the module and the config path as placeholders. Three proofs: another prefix is exactly what the client call runs, another command is exactly what the version check runs, and another command is exactly what a rendered unit starts with. The same search still reports the engine itself, and that is the next block.

Done 2026-09-13, the fortieth block: the journal vocabulary left src/pyntara/logger.py the same way. The writer read the identifier from PYNTARA_JOURNAL_IDENTIFIER and spelled systemd-cat as a literal: the tool and its identifier were the two values of the logging path no config reader could see. The block is now two [engine] keys, journal_command (["systemd-cat", "--identifier", "{identifier}"]) and journal_priority_command (the same command with "--priority" and "{priority}"), and src/pyntara/config/engine.py holds them next to journal_identifier. The one decision this block asks of the user, stated before the code: the logger receives the whole [engine] table instead of a bare identifier, so a machine whose journal tool differs names it in the config and no module spells the command. logger.configure_journal(cfg.engine) is called by the composition root right after the config load and by the entry point of every deployed service (metrics, metrics_collect, metrics_ingest, port_forwarding), because those start from systemd and never pass through pyntara.py; the logger may not read the config itself, it writes before and around the load. A logger nobody configured forwards nothing, and so does a table whose journal_command is empty, which is what an unreadable config produces; the console and the install log are unaffected, and _export_journal_identifier is gone. _rendered_command imports pyntara.utils inside the function, because utilities import the logger and a module level import would close the cycle. The follow-up the block leaves open: the four unit templates (system_metrics.service, system_metrics-ingest.service, system_metrics_collector.service, auto_forwarding.service) still carry Environment=PYNTARA_JOURNAL_IDENTIFIER=$journal_identifier, a setting their services no longer read, and the rendering parameter that feeds it; removing them rewrites installed units once, so the user decides when that happens.

Done 2026-09-13, the forty-first block: the process check of the entry module. src/pyntara/pyntara.py asked pgrep -x whether a process runs, the last command literal of the composition root, and the check decides the default install mode, so a machine whose process tool differs could not change it. The query is now [engine] process_check_command (["pgrep", "-x", "{process_name}"]) next to process_check_timeout_seconds, rendered by utils.substituted_command, and the executable is resolved with shutil.which as in the journal writer. detect_default_mode takes the whole engine table instead of the timeout and the process list, so its two call sites pass cfg and the four test call sites pass the engine of the test document; the two process fakes of tests/test_entry.py grew the engine argument. One proof proves that another tool and another flag produce the argv the check runs, one proves that a missing tool reports no running process instead of stopping the run, and the live check on the machine reports desktop through the configured command. With this block the entry module holds no command argv; the search for a list that starts with an expression reports nothing under src/pyntara outside the config layer.

Done 2026-09-13: the clone root pointed at the wrong directory. src/pyntara/pyntara.py computed REPO_ROOT as Path(__file__).resolve().parents[1], and because the module itself lives in src/pyntara/, that value is <clone>/src, not the clone root. Every task that renders a template reads task_data/ through ctx.repo_root, so on the target machine the template of system_metrics_setup, port_forwarding_setup, ssh_daemon_setup and every other rendering task was not found, while the whole suite stayed green: the tests compute their own root from tests/, which does sit at the repository root, so they never see this path. The live run showed it in one line: "cannot read the service template: [Errno 2] No such file or directory: /var/cache/pyntara/repo/src/task_data/port_forwarding_setup/auto_port_forwarding.service". The value is now parents[2], and a new proof closes the gap the guard cannot see: test_clone_root_points_at_the_repository_root asks that config/, task_data/ and src/pyntara/pyntara.py exist under REPO_ROOT, so a wrong depth fails the suite instead of the target machine. The guard allowlist line for the constant was updated in the same commit.

Done 2026-09-13, the forty-second block: the journal identifier of the deployed services. The previous block left Environment=PYNTARA_JOURNAL_IDENTIFIER=$journal_identifier in the four unit templates while the services already read the identifier from the config, and it made every service announce itself under the engine name: a regression, because that variable used to carry the name of the service itself, so a journal query could no longer separate the metrics service, its ingest, the collector and the auto forwarding service from the run that deployed them. The fix is one method on the engine table, EngineConfig.with_journal_identifier(identifier), which returns the same table under the name of a service, and the four entry points call it with the key of their own section (metrics and metrics_ingest read system_metrics_setup.service_journal_identifier, metrics_collect reads collector.journal_identifier, port_forwarding reads port_forwarding_setup.journal_identifier). The four templates lost the Environment line and the three renderers of system_metrics_setup and the one of port_forwarding_setup lost the parameter that fed it, so the units no longer carry a setting nothing reads; the task rewrites them on the target machine at its next run. Four proofs, one per service, assert that the logger receives the service identifier of the config and the journal command of the engine table.

Done 2026-09-13, the forty-third block: the test suite wrote into the real system journal. Since the journal vocabulary moved into the config, the entry point of a deployed service configures the journal itself from the config it loaded, and the shared test document of the suite names the real journal command, so every test that called such a main() announced itself in the journal under a production identifier: journalctl showed 240 lines under system_metrics and 287 under auto_port_forwarding, all carrying pytest temporary paths. tests/conftest.py now replaces the configuration call of the four service modules with a no-op for the length of every test, the same way it switched forwarding off when the identifier still travelled through the environment, and a test that exercises the configuration patches the same module name with its own recorder and wins because its patch is applied later. test_a_service_entry_point_keeps_the_journal_off proves the fixture: a service main() runs to its first pause and the logger holds no engine table afterwards. Measured on the machine: zero lines of a test run reach the journal, where the run right before the fix left 527.

Done 2026-09-13, the forty-fourth block: the last names written in code. The task system_metrics_setup composed all six paths of its templates by hand (template_dir / "system_metrics.service" and the five siblings), so the names of the files it reads were the one group of values of that section still invisible to a config reader, while every other rendering task reads its template names from its own section (unit_template_file_name, policy_template_file_name, dropin_template_file_name and the like). The six names are now [system_metrics_setup] keys, unit_template_file_name, ingest_unit_template_file_name, ingest_path_template_file_name, collector_unit_template_file_name, collector_timer_template_file_name and commit_command_template_file_name, documented by the TOML comments next to the unit names they belong to, and the task reads them from the table. A proof renames the shipped template to another name, names that file in the config and demands the rendered unit, so a name spelled in the code fails the suite instead of the target machine. This closes the search for a file name written in code: the only remaining names under src/pyntara are the two paths of the composition root, which the guard records as the location of the running code.

Done 2026-09-13, the forty-fifth block: the guard for the names written in the config. The class of defect of this session was a value that is correct in the code and wrong on the target machine, and the two instances came from opposite sides: a clone root one directory too deep, and template names written in code. The second side has no guard at all, so tests/test_task_templates.py now reads config/ as a whole and demands three things: the value of every key whose name ends in _template_file_name is a file under task_data/ of its section; every string value that starts with task_data/ is a file in the clone, which is the shape a section uses for a template tree; and every directory under task_data/ carries the name of a config section, so a renamed task cannot leave its directory behind. The rules were proved by mutation: naming a template that does not exist fails the suite with the key and the value in the message, which is exactly what the target machine would have reported as a missing template. The rules take the templates nobody names as out of scope on purpose, because a file that is a C source, a KWin script or an Apps Script deployment helper is not a config value.

Done 2026-09-13: the full desktop run passed on the target machine after the journal, unit, clone-root and template-name work. inst.sh updated the cache clone to the pushed main, resolved the lockfile and started the engine; all 28 tasks finished with exit code 0 and not one warning, which is the first clean full run of this session: add_extra_repos satisfied, hostname, the swapfile, zram and zswap services, the runtime vault, the System Metrics service, the ssh daemon and client, the auto forwarding service, the KDE keyboard and appearance settings, Vocalinux, NextDNS, dnsproxy active with its profile, i2pd, yggdrasil, Tor, the 3x-ui panel with its connection profile and routing policy, RustDesk, ImageMagick, ffmpeg, the initial metrics collect, the Chrome repository, Playwright, the cli tools, Telegram and the vault commit. The run also proved the clone root fix live: every task that renders a template found its file, where the run two hours earlier reported a missing template for each of them. A rerun of telegram_setup right after it reports "already installed the latest release", so the task is idempotent and the full run installed it only because its launcher entry was missing.

Done 2026-09-13: the counts of docs/spec/config-content.md are examples, not limits, so they are only corrected where they stopped meaning anything: the command argv type said about 21 values while the config now names 197 commands, one per call shape a task reads, so the sentence states the order of magnitude the migration reached. The other counts still describe their categories closely (55 durations against 52, 23 counts against 24, 35 modes against 33) and are left alone, because rewriting an example to a number that a slightly different way of counting would move again adds noise, not truth.

Done 2026-09-13, closing the two leftovers of the value audit as decisions rather than work. TaskConfig.description is read by no module of the package: the catalog contract makes it part of every [[tasks]] entry (docs/contracts/task-model.md, Task catalog), the checks demand a string, and nothing prints it, because the engine reports the task names it selected and a description per task would fill the install log with text that repeats the spec documents. It stays as metadata of the catalog, which is what the contract asks for, and a future feature that wants to show it reads the same field. The two scripts under secrets/ read their tables from the TOML by hand instead of through pyntara.config: they are standalone user-invoked utilities outside the engine flow, they validate the tables they read with messages written for the person running them, and the structure they read, the vault structure and the Google Script deployment pattern, is not used by any task at run time. Both are recorded here so the next reader does not count them as pending work; moving them onto the shared loader would be a change of their behaviour, not a cleanup.

Done 2026-09-13, the forty-sixth block: the values hidden inside function bodies, found with an AST scan of the package (51 numeric literals outside the config layer, most of them encodings and protocol values that the spec lists as exceptions). The 3x-ui task held five of the real ones and now reads them: random_username_bytes, random_secret_bytes and random_sub_id_bytes are the os.urandom lengths proquint encodes into the panel username and the client email, the panel password, the web base path, the client id and the subscription id, so the generated credential size is a config value instead of three numbers in the middle of a dict literal; route_test_port is the port every routing check knocks on, which was written twice; private_ipv4_networks is the set of networks that count as private when the task decides whether this machine sits behind NAT, and the decision now uses the standard library ipaddress.ip_network instead of hand-written prefix arithmetic over the string parts of an address. Client identity generation moved into _client_identity(cfg, stored), a function of its own, which is what makes the three lengths provable at all. Proofs: another size changes every proquint length, a stored identity still wins over a generated one, another route test port reaches the fake core for both the domain and the address check, and a carrier-grade network added to the config changes the private verdict of the same address. The live run of the task on the target machine passed with exit code 0: the routing checks answered for the domain, the address and localhost through the configured port, and the local proxy carried the request out. The still-open members of this front are two small ones and one exception: utils.ensure_port_free sleeps a literal 0.2 s between port checks, chrome_setup reads the "plugin=" prefix as line[7:], and the DNS header masks of dnsproxy_setup stay in code as an encoding.

Done 2026-09-13, the forty-seventh block: the last two values hidden in function bodies. utils.ensure_port_free waited for a killed process with a literal grace period and a literal pause: the grace was a default of the signature (5 seconds) and the pause a sleep(0.2) inside the loop, both invisible to a config reader, and the two values decide how long the run waits for a port before it kills the holder outright. They are [engine] keys now, port_kill_grace_seconds and port_kill_poll_seconds, read by the helper through the engine table it already receives, and the signature lost its default so no caller can hold a third copy. The proof gives the config a grace of two seconds, a pause of half a second and a clock that jumps past the grace: the process is killed after exactly one pause of the configured length. The chrome_setup parser read the "plugin=" prefix as line[7:], an index that says nothing to a reader; it is now removeprefix with the key named in a local variable. The live run of the two tasks on the target machine finished with exit code 0 and no warnings.

Done 2026-09-13: the ca-certificates finding of the fresh install log is not a defect. The line "2 added, 25 removed" belongs to the apt upgrade of ca-certificates, which is the normal behaviour of that package: the update drops the roots the upstream project has retired and the local rehash warning about ca-certificates.crt is the well-known message of a bundle that holds more than one certificate, not a broken store. Verified on the target machine: /etc/ssl/certs holds 246 entries, /etc/ssl/certs/ca-certificates.crt is present, update-ca-certificates --fresh rebuilds the store without an error, and a TLS request to a public site answers 200. Two other findings of the same log are already closed: the venv python no longer leaks into the official 3x-ui installer (its environment drops the venv from PATH and clears VIRTUAL_ENV, so the installer resolves the system python3), and the final report already prints one block per task with a blank line between them, a [done] or [warn] line for every task, and a closing line that names the counts; when warnings exist that closing line names the tasks, which is the nonzero-exit cause, and a task that failed a step is deliberately reported as done with warnings because a recoverable failure must not stop the run (architecture contract, Task contract).

Done 2026-09-13, the forty-eighth block: the inbound payload document of the 3x-ui panel. The payload was a dict literal of two dozen field names and protocol words (remark, port, protocol vless, settings, clients, decryption none, streamSettings, network tcp, security reality, realitySettings with show, xver, dest, serverNames, privateKey, shortIds, the nested settings with publicKey and fingerprint, sniffing with enabled and destOverride, enable). Those names are the vocabulary of the panel API, and the spec keeps them out of the Exceptions list, so the block follows the rule the spec states for a document longer than five lines: the document is a template now, task_data/three_x_ui_xray_setup/vless_reality_inbound.json, named by the [three_x_ui_xray_setup] key inbound_payload_template_file_name. Every $placeholder is filled with a JSON value through json.dumps, so the port stays a number and a list stays an array, and the filled document is parsed once, which turns a broken template into a failure of this task instead of an API error on the target machine. build_vless_reality_payload takes the template text; a stage that cannot read it reports a warning and the remaining stages still run. Proofs: the shipped template produces the documented structure, another template with another protocol word and another field name is exactly what the caller receives, and a template that is not valid JSON raises. Live check on the target machine: the clone carried the template, and rendering it there produced the payload with the protocol vless, the port as an integer and every nested key in place. This is the shape the remaining protocol documents of the panel take: a document longer than five lines becomes a template under task_data/, and the keys that are values of the machine stay in the section.

Done 2026-09-13, the forty-ninth block: the first task of the recoverable failure front. playwright_setup returned an error result for three different situations, and an error result is the one shape the task contract asks tasks to avoid: the runner turns it into a completed task carrying the reason in warnings, so the run continues either way, but the task itself stopped its own remaining steps. Now the task reports every step it could not perform and completes: a failed apt install of the runtime packages (nodejs and npm) stops only the further steps of this task, because npm can install nothing without them, and the result is a completed task whose warning names the packages and the reasons; a failed npm install is a warning and the task completes; a binary that does not answer the version probe after a successful install is a warning naming the path it expected. The live check on the target machine forced the reinstall and finished with exit code 0, so the success path is intact. Next in this front: i2pd_service_setup, ssh_daemon_setup, zram_service, then the remaining modules of the audit.

Done 2026-09-13, the fiftieth block: the client SSH task follows the recoverable failure policy. ssh_client_setup stopped at the first problem with an error result: a missing Include directive, an augeas package that would not install, a drop-in that augeas could not write, and a drift the ssh -G verification reported. Each of these is now a warning of a completed task: the missing Include is reported and the drop-in is still written, because the value is persistent and starts to work the moment the directive appears; an unwritable drop-in and a reported drift are warnings too; only the missing augeas tool stops the further steps of this task, because without it the drop-in cannot be written at all, and even that is a completed task with the reason in warnings. The four tests follow the new behaviour and assert the warnings. The front continues with i2pd_service_setup, ssh_daemon_setup and zram_service.

Done 2026-09-13, the fifty-first block: zswap_service follows the recoverable failure policy. The task stopped at the first problem with an error result, so one parameter the kernel rejects, one attribute file the kernel does not have, a missing unit template, an unwritable unit and a failed systemctl enable each ended the whole task. Now every one of them is a warning of a completed task, and the independent steps still run: the rejected parameter is reported while the other mismatching parameters are written, because they are independent records; the verification reports the parameters that did not take effect without stopping; a unit that cannot be rendered or written is reported while the parameters stay configured; a failed enable is reported with the change kept. The live run on the target machine forced the task and finished with exit code 0, so the whole path is intact. The front continues with i2pd_service_setup, ssh_daemon_setup and zram_service.

Done 2026-09-13, the fifty-second block: the browser setup task follows the recoverable failure policy. chrome_setup stopped the whole task with an error result in five places: a missing apt source template, a missing mirror unit template, a failed registration of the Google apt repository, a failed package install and a failed update of the settings repository. Each of them is a warning of a completed task now, and the steps that do not depend on the failed one still run: a missing apt source template skips the repository registration alone and the settings of a cloned repository still deploy, a missing mirror unit template skips the mount and the desktop entry leaves the --user-data-dir flag out, a failed repository registration is reported while the install and the settings steps proceed, and a failed package install is reported while the system tree, the profile merge and the desktop entry are written. Only the total absence of the mechanism of a step stops that step, never the others. Proofs: the two failure tests are warnings tests now and assert that the profile merge and the desktop entry happened anyway, and a new test with a temporary clone root proves that both missing templates leave the settings in place while no key is fetched and no unit is enabled. The live run on the target machine ran the task against the pushed revision and finished with exit code 0, the mirror mounted and the message "browser already set up", so the normal path is intact. The front continues with i2pd_service_setup, ssh_daemon_setup and zram_service.

Done 2026-09-13, the fifty-third block: the i2pd installation task follows the recoverable failure policy. The task stopped the whole run with an error result in seventeen places, so a broken config, a missing template, a distribution outside the Debian family, an unknown architecture, a failed release query, a missing release asset, a missing or non-numeric SSH Port directive, a failed download, a failed install, an unwritable configuration, a failed enable, a failed start, a service that never became active and an unwritable address file each ended the task. Every one of them is a warning of a completed task now, and the step that owns the missing mechanism is skipped alone: a machine outside the Debian family never queries the release, because no asset of that family could install, while its configuration files and its service state are still deployed; a missing template skips the file it renders; an unknown architecture or a missing asset skips the install; a missing SSH Port directive skips the tunnels file while the main configuration is written; a failed download or install skips the package step alone; a failed enable, start or readiness wait keeps everything else. Proofs: nine tests follow the new behaviour, three of them prove that the independent steps still run (the configuration file exists, the service restart appears in the calls) and one proves that a missing tunnels template leaves the main configuration in place. The live run on the target machine forced the task against the pushed revision and finished with exit code 0, reinstalling i2pd 2.61.0, rewriting both configuration files, restarting the service and reporting the tunnel address. The front continues with ssh_daemon_setup, zram_service and the remaining modules of the audit.

Done 2026-09-13, the fifty-fourth block: the SSH daemon task follows the recoverable failure policy. ssh_daemon_setup stopped the whole task with an error result in fifteen places: a missing key file, a missing port-forwarding key file, a failed package install, a missing Include directive, a missing augeas tool, a failed drop-in sync, a verification of sshd -T that disagreed, a failed socket disable, a failed enable, a failed start, a service that never became active, a missing listener, a failed restart, a failed reload and the same verifications of the restart path. Each of them is a warning of a completed task now and the missing mechanism skips its own step alone: a missing key file skips the key deployment while the drop-in and the service state are handled, a missing augeas tool skips the drop-in while the keys are deployed, a failed package install skips both while the directives are still written, a missing Include leaves the drop-in in place because the directives start to work the moment the line appears, and a failed socket disable, enable, start, reload or restart keeps every earlier step. Proofs: nine tests follow the new behaviour and three of them prove that an independent step still ran (the drop-in exists after the failed install and after the missing key file, the keys are deployed after the failed augeas install). The live run on the target machine ran the task twice against the pushed revision, once as a plain run that reported "already configured" and once forced, which rewrote the drop-in, restarted the service and verified the listener on port 30222, both with exit code 0. The front continues with zram_service and the remaining modules of the audit.

Done 2026-09-13, the fifty-fifth block: the ZRAM task follows the recoverable failure policy. zram_service stopped the whole task with an error result in thirteen places: an unreadable RAM size, a rejected swapoff, a reset that stayed busy, a hot_remove that stayed busy, a failed modprobe, an unreadable hot_add interface, a failed device creation, a rejected sysfs write, a failed mkswap or swapon, a verification that disagreed, a missing unit template, an unwritable unit file and a failed systemd setup. Every one of them is a warning of a completed task now, and the step that owns the missing mechanism is skipped alone: a rejected swapoff, reset or removal is reported while the devices are still configured, an unreadable hot_add falls back to its write spelling, a rejected device configuration skips that device while the others are configured, a failed device activation is reported and the next device is still tried, a failed verification reports the parameters that did not take effect without stopping, a missing template or an unwritable unit leaves the parameters configured, and a failed enable keeps the change. The unreadable RAM size is the single exception: without it no device size can be computed at all, so the task ends immediately with the reason in the warnings. The changed flag now reports the devices that were really configured instead of assuming success, so a run that configured nothing does not claim a change. Proofs: five tests follow the new behaviour and three of them prove that an independent step still ran (the other device is still tried after a failed mkswap, the unit is skipped alone after a missing template, the devices stay configured after a failed enable). The live run on the target machine forced the task against the pushed revision and finished with exit code 0, configuring 12 devices of 1319026688 bytes each. The front continues with tor_setup, system_metrics_setup and swapfile_service_install.

Done 2026-09-13, the fifty-sixth block: the swapfile task follows the recoverable failure policy. swapfile_service_install stopped the whole task with an error result in eight places: unreadable RAM or disk space, a failed swapon of an existing file, a failed swapoff of the old file, a failed removal, a failed fallocate, chmod, mkswap or swapon of the recreated file, a missing unit template, an unwritable unit file and a failed systemd setup. Each of them is a warning of a completed task now, and the step that owns the missing mechanism is skipped alone: unreadable measurements skip the swapfile steps while the boot service is still installed, because the deployed unit creates the file at boot on its own; a swapoff that failed leaves the file in place instead of removing it under a running swap, and the removal and creation are skipped while the unit and the enable step still run; a failed setup step is reported and the service state is still converged; a missing template or an unwritable unit skips the unit alone and keeps the configured swapfile. The changed flag now reports the file that was really written and the unit that was really enabled instead of assuming success. Proofs: four tests follow the new behaviour, among them the two that prove an independent step still ran (the boot service is installed after a failed fallocate and after a failed mkswap) and the one that proves the unit is skipped alone when its template is missing. The live run on the target machine reported the task already configured with exit code 0; the forced path was not run on purpose, because it recreates a swapfile of about 36 GiB on the only machine at hand, and the failure branches are covered by the proofs instead. The front continues with tor_setup and system_metrics_setup.

Done 2026-09-13, the fifty-seventh block: the Tor task follows the recoverable failure policy. tor_setup stopped the whole task with an error result in ten places: a failed package install, an unreadable SSH Port directive, a missing main torrc, a failed drop-in write, a verification that disagreeing tor --verify-config call, a Tor system user that does not exist, a failed enable, a failed start or restart, a service that never became active and an unwritable address file. Each of them is a warning of a completed task now: a failed install is reported while the include line, the drop-in and the service state are still handled, a missing main torrc is reported while the drop-in is written so the settings work once the file appears, a failed verification is reported while the hidden service directory and the service state are converged, and a missing Tor user or a failed enable, start or restart leaves every earlier step in place. A missing or non-numeric SSH Port directive skips the drop-in alone, because the forward target of the onion service is unknown. Proofs: seven tests follow the new behaviour, three of them prove that an independent step still ran (the drop-in exists after a failed install and after a missing torrc, the service is enabled after a missing Tor user, the service restart happens after a failed verification) and two prove that the drop-in is skipped alone when the port is unknown. The live run on the target machine forced the task against the pushed revision and finished with exit code 0, rewriting the drop-in, restarting the service and reporting the SSH onion address with virtual port 30222. The front continues with system_metrics_setup and the remaining modules of the audit.

Done 2026-09-13, the fifty-eighth block: the System Metrics deployment follows the recoverable failure policy. system_metrics_setup stopped the whole task with an error result in seven places: a missing uv executable, a failed virtual environment build, an unwritable system config, an unwritable unit file, a failed systemd setup, an unwritable command file and a spool directory that could not be created. Each of them is a warning of a completed task now, and the step that owns the missing mechanism is skipped alone: without uv the venv step is skipped while the configuration, the five units, the command and the spool directory are still deployed, a failed venv build is reported while every later step runs, a unit that cannot be written is reported and the remaining units are still written, a failed systemd block keeps the files that were deployed, and a directory on the command path is reported while everything else is converged. The systemd block now marks the run as changed, because converging the enablement or the running state of a unit is a change of the machine. Proofs: five tests follow the new behaviour, two of them prove that an independent step still ran (the configuration exists after the missing uv, a unit is enabled after the failed venv build). The live run on the target machine finished with exit code 0, refreshing the venv, the five units, the command and the spool directory.

A note on the tests of this front: taking an early return away can let a test reach the real system. test_uv_missing_fails was written as task(_ctx(tmp_path)) without the fixture config, so with the old code it stopped at the missing uv and never touched a path, and with the new code it walked into the real /etc/pyntara/config.toml (the write was refused by the file permissions, so nothing changed and the timestamp proved it). The fixture config is passed now, and every test of a converted task is read with that question in mind: which paths does the run reach, now that the early return is gone.

Done 2026-09-13, the fifty-ninth block: four small tasks follow the recoverable failure policy in one pass. imagemagick_setup stopped at a failed package install and at an unwritable policy; both are warnings of a completed task now, and the policy is still deployed after a failed install, while a policy that cannot be written leaves the packages that were installed. hostname stopped at an unwritable hostname file and at a failed hostnamectl apply; both are warnings now and the two steps are independent, so a failed write still applies the kernel name and a failed apply keeps the generated name in the file for the next run. cli_tools stopped when the installed share fell below package_success_threshold_percent; the shortfall and the reasons are a warning of a completed task now, because every package that could be installed was installed and the run must not stop at a package count. system_metrics_initial_collect stopped at a failed systemctl start; the reason is a warning now and the collector retries through its own restart policy. Proofs: twelve tests follow the new behaviour, and four of them prove that an independent step still ran (the policy exists after a failed install, the kernel name is applied after a failed write, the generated name stays in the file after a failed apply). The live run on the target machine finished with exit code 0 for all four tasks, including the collector service start.

Done 2026-09-13, the sixtieth block: ffmpeg_setup and nextdns_setup_system_wide follow the recoverable failure policy. ffmpeg_setup stopped at a failed package install, a failed wayrecord build and an unwritable desktop entry; all three are warnings of a completed task now, and the steps are independent: the engine is still built after a failed package install, the desktop entry is still deployed after a failed build, and the packages stay installed after a failed entry. nextdns_setup_system_wide stopped at a vault that could not be opened, a profile that could not be derived from the vault group and the hostname, and an unwritable profile ID file; writing the profile ID is the only step of that task, so each of the three is a warning naming the reason and the file is left untouched. Proofs: five tests follow the new behaviour, two of them prove that an independent step still ran (the desktop entry exists after a failed build). The live run on the target machine finished with exit code 0 for both tasks, reporting the ffmpeg package set as installed and the profile ID file as already carrying the selected profile. The front continues with three_x_ui_xray_setup, port_forwarding_setup, dnsproxy_setup, commit_final_system_metrics, add_extra_repos, rustdesk_setup, local_vault_setup, vocalinux_setup and telegram_setup.

Done 2026-09-13, the sixty-first block: the Telegram task follows the recoverable failure policy. telegram_setup stopped the whole task with an error result in four places: a redirect that could not be resolved, an archive that could not be downloaded, an archive that could not be installed and an unwritable launcher entry. Each of them is a warning of a completed task now, and the step that owns the missing mechanism is skipped alone: a release that cannot be resolved skips the download and the install while the launcher entry and the icon are still handled, a failed download skips the install, and a failed install keeps the launcher entry, which is the part that makes Telegram reachable. Proofs: two tests follow the new behaviour and both prove that an independent step still ran (the launcher entry exists after an unresolved redirect; nothing is installed after a failed download while the entry is written). The live run on the target machine finished with exit code 0, reporting the cached release as already installed.

Done 2026-09-13, the sixty-second block: the Vocalinux task follows the recoverable failure policy. vocalinux_setup stopped the whole task with an error result in five places: a failed package install, a failed AppImage install and a missing app config, autostart or empty-action template. Each of them is a warning of a completed task now, and the step that owns the missing mechanism is skipped alone: a failed package install leaves the AppImage deployment and the user files, because the AppImage is self-contained; a failed AppImage install skips the autostart entry alone, which would otherwise point at a file that does not exist, while the app config, the empty-action desktop file, the input group membership, the user unit and the Meta+S shortcut are still handled; a missing template skips the single file it renders. Proofs: three tests follow the new behaviour and each proves that an independent step still ran (the config exists after a failed package install, the autostart entry exists after a missing config template, the config exists after a failed download while the autostart entry is skipped). The live run on the target machine finished with exit code 0, reporting the installed version and the reminder to log out and back in. The front continues with three_x_ui_xray_setup, port_forwarding_setup, dnsproxy_setup, commit_final_system_metrics, add_extra_repos, rustdesk_setup and local_vault_setup.

Done 2026-09-13, the sixty-third block: the RustDesk task follows the recoverable failure policy. rustdesk_setup stopped the whole task with an error result in six places (an unreadable release, an unreadable dpkg architecture, a release without a deb for this architecture, a failed download, a failed install and a daemon that did not answer) and it ended the task early in four more places that were already warnings, so a step after them never ran. All of them are warnings of a completed task now: a release that cannot be read skips the download and the install while the service, the options, the credentials and the machine ID file are still handled; an unknown architecture or a missing asset skips the install alone; a failed download skips the install; a failed install keeps the service work; a daemon that does not answer is reported and the credential and ID steps still run; an option error, a missing password, a failed password write and an unreadable machine ID no longer cut the run short. Proofs: the failure test follows the new behaviour and proves that nothing is installed when the release has no asset for the architecture, while the task completes. The live run on the target machine finished with exit code 0, reporting the task as already configured. The front continues with three_x_ui_xray_setup, port_forwarding_setup, dnsproxy_setup, commit_final_system_metrics, add_extra_repos and local_vault_setup.

Done 2026-09-13, the sixty-fourth block: the runtime vault task follows the recoverable failure policy. local_vault_setup stopped the whole task with an error result in five places: no source vault opened with the run password, the local password entry missing or empty, a runtime vault that could not be written, a password file that could not be written and a verification that failed. The first two are warnings of a completed task now and the run ends there, because the runtime vault is the only thing the task builds and an empty password would be unsafe; the last three are warnings too, and a runtime vault that could not be written skips the password file and the verification that depend on it, while a password file that could not be written leaves the verification to run on the vault that exists. Every serious finding keeps its journal entry at the configured error priority. Proofs: four tests follow the new behaviour and two of them prove that nothing was written when the password entry is missing or empty. The live run on the target machine finished with exit code 0, reporting the existing runtime vault and its password file. The front continues with three_x_ui_xray_setup, port_forwarding_setup, dnsproxy_setup, commit_final_system_metrics and add_extra_repos.

Done 2026-09-13, the sixty-fifth block: the apt components task follows the recoverable failure policy. add_extra_repos stopped the whole task with an error result in seven places: an unwritable keep-debs drop-in, no apt source files at all, unreadable source files or sections that cannot be repaired, no Ubuntu archive section, unwritable source files, unreadable files during the verification and components that were still missing after the rewrite. Each of them is a warning of a completed task now, and the work that is still possible is still done: an unwritable drop-in leaves the source files to be handled, an unreadable file is named while the other files are rewritten, a file that cannot be written keeps the files that were written, and the verification reports the missing components after the rewrite without discarding the change. The two cases with nothing to do (no source file at all, no Ubuntu section) end the task with the reason in the warnings. Proofs: four tests follow the new behaviour, among them the drop-in error that now proves the sources were still handled. The live run on the target machine finished with exit code 0, reporting the components as already satisfied. The front continues with three_x_ui_xray_setup, port_forwarding_setup, dnsproxy_setup and commit_final_system_metrics.

Done 2026-09-13, the sixty-sixth block: the DNS proxy task follows the recoverable failure policy. dnsproxy_setup stopped the whole task with an error result in seven places, and this task is the one where an error result had a safety meaning: after the resolver was cut over to dnsproxy a failed verification already reverted the drop-in and the service, so the machine kept a working resolver. That revert stays exactly as it was; what changed is the result shape: every one of the seven findings is a warning of a completed task now, with the same reason text. A missing NextDNS profile id and a failed release or architecture query end the run before anything is touched, because there is no upstream to answer from; a listen port that stays occupied ends the run before the cut-over, so the old daemon and the old resolver stay; a service that does not become active or does not answer direct queries stops that service alone and leaves the system resolver untouched; a failed verification after the cut-over reverts the drop-in and the service and reports the reason, so the machine falls back to the resolver it had. Proofs: six tests follow the new behaviour, among them the three verification failures that keep the working resolver and the probe failure that stops the service, and one test proves that the name resolution of the machine still answers after the run. The live run on the target machine finished with exit code 0, reporting dnsproxy active with the NextDNS profile, and a public HTTPS request answered 200 afterwards. The front continues with three_x_ui_xray_setup, port_forwarding_setup and commit_final_system_metrics.

Done 2026-09-13, the sixty-seventh block: the panel task follows the recoverable failure policy. three_x_ui_xray_setup stopped the whole task with an error result in seven places: an unreadable release, a panel port that stayed occupied, an ACME port that stayed occupied, a failed installer download, a failed installer run, a panel that did not become active after the installer and a panel port that could not be converged. Each of them is a warning of a completed task now, and the work that does not depend on the failed step still runs: an unreadable release skips the installer alone, because the installed version cannot be compared without a tag, and the port convergence and every panel stage still talk to the panel that is there; a busy panel port skips the installer, a busy ACME port turns off only the certificate attempt, a failed download or install skips the installer, a panel that does not become active is reported and the stages report their own result, and a port that cannot be converged leaves the panel reachable through the port it already uses. The stage results carry their own warnings into the same list, so one run reports every reason at once. Proofs: four tests follow the new behaviour, and the installer failure and the release failure prove that the task completes with the reason in the warnings. The live run on the target machine finished with exit code 0, reporting the target state as already reached. The front continues with port_forwarding_setup and commit_final_system_metrics.

Done 2026-09-13, the sixty-eighth block: the auto port forwarding task follows the recoverable failure policy. port_forwarding_setup stopped the whole task with an error result in seven places: a missing unit template, an unwritable unit file, a failed systemd daemon reload, a failed enable, a state file that could not be removed in force mode, a failed service start and a service that entered the failed state after the start. Each of them is a warning of a completed task now, and the step that owns the missing mechanism is skipped alone: a missing template skips the unit write while the enable and the restart still act on the unit installed on the machine, an unwritable unit skips the reload that belongs to it, and every other failure is reported while the deployed unit stays in place. Proofs: two tests follow the new behaviour, and the missing-template test proves that the run completes with the reason in the warnings. The live run on the target machine finished with exit code 0, reporting the service as configured. The front continues with commit_final_system_metrics.

Done 2026-09-13, the sixty-ninth block: the runtime vault backup task follows the recoverable failure policy. commit_final_system_metrics stopped the whole task with an error result in seven places: a missing runtime vault, an empty vault, a vault that could not be stat, an empty commit_command, a failed copy to the temporary file and a failed commit, both by timeout and by a nonzero exit. The task produces one document, so every one of them is a warning of a completed task now and the run ends without a backup, with the reason in the warnings: the temporary copy is still removed in every path, and the command detail of a nonzero commit stays in the warning text. Proofs: five tests follow the new behaviour, and each asserts the warning instead of the error while keeping the checks that the commit command never ran and the temporary file is gone. The live run on the target machine finished with exit code 0, committing the runtime vault as dozor-gunid.kdbx with 21221 bytes. With this block every task module of the catalog follows the recoverable failure policy.

Done 2026-09-13, closing the recoverable failure front: no task module builds an error result any more, and a new proof keeps it that way. src/pyntara/tasks/ holds no success=False: every task reports a step it could not perform in warnings and completes, while the runner keeps its conversion of any error result or raised exception into a completed result, so a run never stops. test_task_modules_report_findings_in_warnings in tests/test_runner.py walks every module under src/pyntara/tasks/ and demands the literal is absent; the check was proved by mutation (a comment carrying the literal in cli_tools.py failed the test with the file name and was removed again). The front is closed with the sixty-nine blocks recorded above, each with a green gate, a live check on the target machine, an entry here and a pushed commit.

Done 2026-09-13: the full desktop run on the target machine after the whole front. The run resolved 28 tasks, finished with exit code 0 and printed no warning line at all, from the first task to the last, where the runtime vault was committed as dozor-gunid.kdbx. This is the live proof of the front: every task reports its steps, none of them stops the run, and no task needed a warning, so the machine reached its target state completely. With the config migration closed earlier by the guard (no command argv, no value constant and no duplicated pattern outside the documented exceptions) and the recoverable failure policy closed by the proof above, the two fronts of this work are done.

Done 2026-09-13, the seventieth block: the last body written in the code moves to task data. An audit of every string constant of src/pyntara/tasks/ longer than five lines returned exactly one hit, a ten line python3-dbus client built by concatenation inside _release_hotkeys_live of kde_settings and passed to the interpreter as the argument of python -c; the config content spec keeps a body of that size in a file under task_data/<section>/ and not in the code. The client is task_data/kde_settings/kglobalaccel_release.py now, its name is the config value kglobalaccel_release_script_file_name, and the function reads the text from the path the task passes down, exactly as _apply_desktop_count_live and kde_keyboard_setup._apply_hotkeys_live already do. A path that cannot be read is reported through the existing warning of that step, so a missing file costs the live release alone while the config clearing stays in place. The key follows the full set of five declarations: the TOML key with its comment, the dataclass field with its docstring, the check in tests/config_checks.py, the mirror line in tests/config_helpers.py, and a new rule of tests/test_task_templates.py that demands every key ending in _script_file_name names a file under task_data/<section>/, which now also covers the desktop id client and the keyboard hotkey client. Proofs: test_free_script_hotkeys_clears_and_releases_live asserts the text of the client file inside the release command, test_release_client_text_comes_from_the_config proves that another name in the config runs another client, and both were proved by mutation: dropping the read fails both tests, and a config name without a file fails the template rule. With this block no body longer than five lines and no command argv remains in the code. The live run on the target machine forced kde_settings against the pushed revision and finished with exit code 0, the log showing the client loaded from task data and the daemon releasing two hotkey owners, so the mechanism is proved on the machine and not only in the suite.

Done 2026-09-13, the seventy-first block: the journal levels of the run are values of the [engine] table and nothing else. An audit of the number literals outside the config layer found the last two kinds that describe the run instead of a protocol. logger.py held the level 6 in seven signatures and in the branch that reuses the shared journal process, while the config already names error_priority and progress_priority, so progress_priority was read by nobody and a run stayed at the informational level whatever the config said. The logger now answers a call that names no level with progress_priority of the table, and the shared process is started from journal_priority_command with that level, because a journal tool fixes the level at process start; a table whose priority command is empty falls back to the plain journal command, so an incomplete config still reaches the journal. The three metrics modules wrote priority=3 at fifteen call sites; they read error_priority from the same table now, which threaded the value through _entry_uploadable, _publish_entry and _acquire_lock as a parameter, the shape the setup tasks already use. The second kind is the factor that turns a commit time into the unit of the kernel: metrics_commit.py multiplied by 1_000_000_000, and the config names nanoseconds_per_second next to percent_scale and the byte counts now. The tests read the same table through make_config, and the two new parameters keep every proof honest. Proofs: the default level of a journal line is the configured one, another progress level reaches the journal, another error level reaches the journal in the ingest and in the sender, and another nanosecond factor is the modification time the queue entry receives. Live check: the journal integration tests write into the real journal and read the level back, and a full run of the machine keeps its exit code 0.

Done 2026-09-13, the seventy-second block: the default port of a remote share link is a config value. routing_policy.parse_vless_link read port=parsed.port or 443, a network port with a literal default, while the section that owns the import owns the meaning of a link that carries no port. The function takes default_port now, the task passes three_x_ui_xray_setup.remote_link_default_port, the key sits next to route_test_port with its comment, its dataclass docstring, its check, its mirror line and its entry in docs/spec/3x-ui.md, and the test helper of the task reads the port from the fixture config instead of a global one. Proof: a link without a port takes the port the call names (8443), so the operator decides what such a link means without a code change.

Done 2026-09-13, the seventy-third block: the ExecStart line of the zswap boot service is a config value. The task built the line as an f-string inside _render_unit, which is the type of a text of at most five lines that becomes part of a file the run writes, while the zram task already keeps that kind of line in its own section, so the two agreed on the idea and not on the place. The key is zswap_service.unit_exec_line_template with the value and the attribute path as its placeholders, checked by the placeholder text rule of the checks, and the test builds its expected unit from the same key instead of repeating the line, so a template change is answered in the config alone. Proof: another line template is the ExecStart line of the installed unit. Live check: the forced run of zswap_service against the pushed revision finished with exit code 0, kept the configured parameters and rewrote /etc/systemd/system/zswap.service with the five rendered lines.

Done 2026-09-13, the seventy-fourth block: the collector schedule is the times the user named. The spec carried a once-a-day gate that was never implemented and that would have suppressed the report of a machine which reboots several times a day; the user stated the rule instead: a report is built when a producer commits new data, when the machine boots, and at noon and at midnight. The single daily_send_time key became daily_send_times, the shipped value is ["12:00:00", "00:00:00"], the timer template renders one OnCalendar line per entry, the check demands a non-empty array of times of day (a time, an empty array and a malformed value are all refused by the mutation list), and the spec now names the four occasions and says plainly that there is deliberately no gate. Proof: another pair of times is the OnCalendar lines of the deployed timer.

Done 2026-09-13, the seventy-fifth block: the protocol forms of a bootstrap address are config values. _protocol_forms of dnsproxy_setup built four forms per address with the ports 53, 853, 443 and 853 written in the code, while the section already names the DoT, DoH and DoQ endpoints of the profile. The key is dnsproxy_setup.bootstrap_form_templates, a list of templates with {host}, shipped as the four forms the task used to spell; the check refuses an empty list and an entry without the placeholder. Proof: two templates of the table are exactly the arguments the helper builds, brackets of an IPv6 host included.

Done 2026-09-13, the seventy-sixth block: the augtool program is config vocabulary. src/pyntara/augeas.py spelled ten lines of the augtool language (the load entry with its lens and include, the load and save calls, the print, the ownership comment, the container node, the directive, the removal outside and inside a container) while the node prefix was already an [engine] key, so the same helper was half configured. The eleven keys live next to augeas_files_node_prefix now, each checked for the placeholders the helper fills, and the helper only formats them. Proof: marker templates from the table are the lines of the script the helper feeds to the tool, in the read path and in the write path.

Done 2026-09-13, the seventy-seventh block: the random suffix alphabet is a config value. metrics_commit kept _SUFFIX_ALPHABET = ascii_letters + digits while the suffix length next to it was already a key; the alphabet is now system_metrics_setup.queue_file_suffix_alphabet, checked as a non-empty string, and _random_suffix takes it as a parameter. Proof: the ingest still builds names of the configured length, and the alphabet travels with the length through the same call.

Done 2026-09-13, the seventy-eighth block: the guard no longer looks past a private constant. VALUE_CONSTANT_DEFINITION matched only names that start with a capital letter, so a module level value written as _NAME was invisible to the rule: the audit of the private constants found seven, of which four are the paths and the title prefix of the version tool. The rule reads _?[A-Z][A-Z0-9_]* now, the proof module carries _PRIVATE_THRESHOLD_SECONDS and the rule test demands it in the offender set, and the four remaining values of bump_version.py are recorded in the allowlist as a candidate for the Exceptions section, with the reason: they describe the layout of this repository and the tool is not part of the installer run, so whether the spec gains such a type or the tool reads them from the config is the user's decision and not mine. The two regular expressions of the tool are exceptions of the spec already.

Done 2026-09-13, the seventy-ninth block: two decisions of the user and the first half of the vocabulary they opened. The user decided that the values of the version tool are an exception (the tool runs from a git hook on a developer machine, never on a target machine, and the files it names are the repository itself), so docs/spec/config-content.md gained that type in the Exceptions section and the four entries of the guard allowlist are marked as exceptions instead of pending work. The user also decided the other way for the vocabulary of a foreign protocol and API: it moves into the config. This block takes the panel half, the module src/pyntara/xui.py, which spelled the HTTP interface of the 3x-ui panel in code: seven maps of [three_x_ui_xray_setup] hold it now, the header names and their values, the methods of the calls, the two schemes of the panel URL, the names of the install-result.env variables the official installer writes, the fields of the panel JSON answers (success, payload, message, token, reason) and the field names of the request bodies and records (client, id, tag, port, protocol, network, inboundTag, outboundTag, matched, domain, ip, kind, tokens, privateKey, publicKey, subPath, subJsonPath, subClashPath, xraySetting, outboundTestUrl and the rest). parse_install_result_env takes the required key names from those maps, build_panel_url takes the scheme, and the two helpers that read answers take the field names, so the client holds no name of the panel any more. Proofs: another header name, another header value, another success field and another port field are the request the client sends and the answer it reads, and the fixture config of the test module carries the maps. Gate green at 1978 tests.

Done 2026-09-13, the eightieth block: the second half of the foreign vocabulary, the Xray document. src/pyntara/routing_policy.py spelled the names of the Xray configuration and the words written into it: 44 field names (tag, protocol, settings, streamSettings, realitySettings, vnext, users, inboundTag, outboundTag, domain, rules, outbounds, routing, domainStrategy, finalRules and the rest), eleven values (vless, reality, none, tcp, socks, http, noauth, field, the api tag and the two synthetic domain rules domain:.onion and domain:.i2p) and eight query parameter names of a share link (security, pbk, fp, sid, sni, spx, flow, type). They are three maps of [three_x_ui_xray_setup] now, xray_field_keys, xray_values and vless_link_query_keys, and LocalProxyPolicy carries the first two, so the rule builders, the ownership checks and the panel restriction check read them from the policy; the outbound builders, the inbound payload builder and the link parser take them as parameters, and parse_vless_link takes the scheme and the query keys as well. The nine call sites of the task, the fixtures of three test modules and the spec entry followed. Proof: another field name, another protocol word and another synthetic domain are the document the policy builds, in the outbound and in the first rules. Gate green at 1979 tests. Live run of three_x_ui_xray_setup on the machine: exit 0, the task read the stored template, rebuilt the same document from the new maps and reported the routing policy as already configured and verified, so the keys match the running core.

Done 2026-09-13, the eighty-first block: the last two places that still spelled the Xray fields, and a check that keeps the maps complete. The task module read streamSettings, realitySettings and publicKey by name while issuing the REALITY key pair, and the ownership check of the policy compared outboundTag by name; both read the map now, so a run whose config names other fields no longer mixes the two. The block also closes the defect the previous one exposed: the first version of xray_field_keys missed private_key, and nothing failed until a test reached the key pair step with the fixture config, which is exactly the way such a config would fail on the target machine, with a KeyError nobody can repair there. tests/config_checks.py now carries the required meanings of the three Xray maps and of the panel maps (XRAY_FIELD_KEY_MEANINGS, XRAY_VALUE_MEANINGS, VLESS_LINK_QUERY_KEY_MEANINGS) and refuses a shipped config that misses one, so the class of defect is caught in development. Gate green at 1979 tests.

Done 2026-09-13, the eighty-second block: the last numbers that described a request or an artifact instead of a protocol. The audit of the numeric literals outside the config layer is down to the ones the spec keeps as exceptions (the HTTP status, the fields and flag masks of a DNS header, the certificate size and type of an i2pd key, the proquint shift table) and to the indices and lengths of a parse, which describe the shape of a line and not a value of the machine. Two groups were real values: the local proxy inbound was created with an enabled flag, a traffic limit, an expiry time and three sniffing options written in the code, because a local proxy must not stop working after a quota; those six are three_x_ui_xray_setup keys now (local_proxy_enabled, local_proxy_traffic_limit_bytes, local_proxy_expiry_time, local_proxy_sniffing_enabled, local_proxy_sniffing_metadata_only, local_proxy_sniffing_route_only) with the shipped values unchanged, and build_local_proxy_inbound takes them from the task. The second is the random part of the temporary name the ingest gives a spool copy: secrets.token_hex(8) is system_metrics_setup.temp_name_random_bytes now. Proofs: another traffic limit, expiry time, enabled flag and sniffing pair are the payload the panel receives, and another random byte count is the length of the temporary name. Gate green at 1981 tests.

Done 2026-09-13, the eighty-third block: the client flag the task sends to the panel. create_client wrote fields["enable"]: True, the last boolean of a request body that described a decision and not the shape of a call; it is three_x_ui_xray_setup.client_enabled now, so a client staged as a draft without being enabled is a config change. Proof: a client created with the flag off carries enable false in the payload the panel receives. Gate green at 1982 tests. With this block the audit of the boolean literals finds only structural flags of a call (return values, capture and check arguments), and the numeric audit only the exceptions of the spec and the lengths of a parse, so nothing that describes the machine is written in the code.

Done 2026-09-13, the eighty-fourth block: the two last values shaped like a file name. The audit of the string literals that look like a file name returned four hits after the version tool (an exception) and the wildcard address of the UPnP search: the address suffix of an I2P host and the name of the armored key file Chrome is installed with. The suffix .b32.i2p was spelled in the decoder of pyntara/i2pd.py and travelled to the address command through it, and the armored file google-chrome-key.pub was built inside the temporary directory of chrome_setup; both are config values now. i2pd_service_setup.address_suffix is passed by the task to b32_address and to resolve_address, and chrome_setup.keyring_armored_file_name names the file the download writes and the dearmor command reads. Each key follows the full set of five declarations, the section literal of tests/test_config_chrome_setup.py carries the new line because that literal is built by hand, and the factory tests/support.py gained the matching parameter. Proofs: another suffix in the config is the address the decoder returns (test_the_address_suffix_comes_from_the_config) and another armored file name is the name the download writes and the gpg command reads (test_keyring_armored_file_name_comes_from_the_config), both proved by mutation. Gate green at 1984 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9. Live run on the target machine with both tasks forced together with their dependencies: exit code 0, six tasks finished and no warning line, chrome_setup installed google-chrome-stable 153.0.8010.36 through the keyring and the source file it registers, and i2pd_service_setup reinstalled i2pd 2.61.0, kept the service active and computed the tunnel address with the configured suffix, wrxibxq6sfxznxa6qva6fwratbe7xksvrr4owzurjjwqjo5f66qq.b32.i2p, which is the same address the saved file carries. With this block the value front is closed: every value the run uses comes from config/, and the only entries left in docs/spec/config-content.md are the documented exceptions of the version tool and of a protocol shape.

Done 2026-09-13, the eighty-fifth block: the vocabulary the run hands to a foreign tool while it hides as a default argument and as a comparison, the two shapes the guard cannot see. The guard reads module level constants, command argv lists, absolute path literals and shared patterns; with that front closed, the audit of the shapes it does not read (the defaults of a signature, the strings a condition compares, the keys of a dictionary literal) found four values. Two were the defaults of xui.route_test, which the routing check of three_x_ui_xray_setup used for every destination class without ever naming them: the network and the protocol of the question are [three_x_ui_xray_setup] route_test_network and route_test_protocol now, both became required arguments of the function, and the task passes the configured words. One was the comment sign of the shared add_line_to_file helper, a default "#" that protected a commented line of the main Tor configuration: the sign is an argument of the helper now and [tor_setup] torrc_comment_sign carries it, because the sign that marks a comment belongs to the syntax of the edited file. The last two did not become keys: they left the code for the standard library, the road the NAT decision took earlier. The upnpc status parser refused the literal 0.0.0.0 and refuses the unspecified address of either family through ipaddress.is_unspecified now, and the ssh target of a link scope address compared the family name to the literal "ipv6" and asks the address itself for its version now, so a machine whose engine table names the families differently keeps the zone index while an IPv4 link address never receives one. Proofs: test_route_test_words_come_from_the_config (another pair of words is what the core is asked about, for a domain and for an address check), test_the_comment_sign_protects_the_line_it_marks (another sign protects the line carrying it), test_the_comment_sign_comes_from_the_caller, test_the_zone_follows_the_address_and_not_its_family_name and the extended test_ignores_an_empty_address (the IPv4 and the IPv6 form of the unspecified address); the first three were proved by mutation, which failed exactly those tests. One default stays in code and is reported as a judgement, not as a migration: the separator of proquint_encode, the canonical separator of an encoding whose alphabet, index maps and trailing marker the guard already records as the exception of that encoding, with tests/test_utils.py proving that a caller may pass another one. Gate green at 1991 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Done 2026-09-13, the eighty-sixth block: the two values of the metrics chain that the same audit found next, the format of the moment the run writes down and the vocabulary of the report document. The format was the literal "%Y-%m-%d-%H-%M-%S" written in two modules, the prefix of a progress line in logger.py and the generated_at field of a report in metrics_collect.py; it is [engine] datetime_format now, read by both, so the console and the report name the same moment the same way and the two copies cannot drift. A logger nobody configured writes no timestamp, which is the shape a test run wants and which the docstring states. The second is the document itself: the collector wrote the field names generated_at, ready_percent, network, system, name, status and output and the words ok, empty and error in code, and the readiness compared the status field to the literal "ok". The collector table carries report_keys and report_status_words now, next to report_file_name whose name the document is written under, and _run_module, percent_ready, collect and collect_until_ready read them; the check demands every meaning the code indexes, so a map missing one fails in development and not on the target machine with a KeyError. Proofs: test_the_report_vocabulary_comes_from_the_config (another map and another word are the document the collector writes and the module the readiness counts, with another datetime format of the same table) and test_the_progress_timestamp_format_comes_from_the_config (another format is the prefix of a progress line, and a logger nobody configured writes none); both proved by mutation, which failed exactly those tests. The live run of the block before this one, on the target machine with tor_setup and three_x_ui_xray_setup forced together with their dependencies, finished with exit code 0 and no warning line: the task confirmed the %include line of the main Tor configuration, and every routing check passed through the request that now carries the configured network and protocol (onion to tor, i2p to i2p, doubleclick blocked, localhost and 10.77.0.0 direct, example.com remote). Gate green at 1993 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9. Live run on the target machine with system_metrics_setup and system_metrics_initial_collect forced: exit code 0, five tasks, no warning line, the collector service deployed and started, and a report committed into the spool by commit_system_metrics. The document built by the deployed code was then read back on the machine and carries exactly the configured vocabulary: the keys generated_at, ready_percent, network, system and, inside a module result, name, status and output; the words ok, empty and error; the moment in the configured format of the [engine] table (nineteen characters, the shipped pattern); readiness 100 with the ten network modules answering.

Done 2026-09-13, the eighty-seventh block: the field names of the foreign files the run reads, which no guard shape sees because they are literals inside functions. Four values in three tasks. The deb822 keys of an apt source file were spelled uris: and components: in add_extra_repos, with the colon and the split that follows it; they are [add_extra_repos] uris_field_name and components_field_name now, compared without case, and the rewrite keeps the spelling and the indentation of the file because the code cuts the key it found instead of searching for a colon. The name of the /proc/meminfo line that carries the installed RAM was MemTotal: in swapfile_service_install and in zram_service, and zram_service counted the /proc/cpuinfo cores by the literal processor; they are [swapfile_service_install] meminfo_total_key and [zram_service] meminfo_total_key with cpuinfo_processor_key now, carried from the section into _read_ram_kib and _read_cpu_count. Proofs: test_the_deb822_field_names_come_from_the_config (a file that spells its fields archive-uris: and parts: is still recognized and rewritten while the configured names are the ones read), test_the_meminfo_line_name_comes_from_the_config and test_the_kernel_line_names_come_from_the_config (another line name is the line the task reads for RAM and for the core count); all three were proved by mutation, which failed exactly those tests. Gate green at 1996 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9. Live run on the target machine with add_extra_repos, swapfile_service_install and zram_service forced: exit code 0 and no warning line, add_extra_repos recognized the real /etc/apt/sources.list.d/ubuntu.sources through the configured deb822 field names and reported its section satisfied with the four components listed, the swapfile was sized from the real /proc/meminfo line (29254M), and zram reported twelve devices at the configured size and core count.

Done 2026-09-13, the eighty-eighth block: the device name of the zram task, a config value the code spelled thirteen times. The section already carried module_name, and the task still built every device name by hand: the /sys/block entry was the literal zram followed by the index, the reset and comp_algorithm and disksize attributes were joined to that literal, the swap device was /dev/zram<index> in the target check, in the unit renderer, in the formatting, in the activation and in the verification, and the name appeared in the log lines of those steps. Two helpers carry the layout now, _device_name(module_name, index) and _device_path(module_name, index), the helpers that read the kernel files take the configured name, and every loop computes the name once and uses it for the paths, the messages and the comparisons; the final message names the configured module as well. Proof: test_the_device_name_comes_from_the_config (another module name in the table is the /sys/block entry the task counts and the swap device path it builds, while an entry of the shipped name is not counted), proved by mutation. Gate green at 1997 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Done 2026-09-13, the eighty-ninth block: the names a foreign file carries, read while the run edits it. Two of them were in kde_settings, which collected the touchpad device groups of kcminputrc by the literals "Libinput" and "Touchpad" while the section already carried every other group name as a value; they are touchpad_group_root and touchpad_device_word now, and _touchpad_groups takes them. The third was the desktop entry key "Exec=" in chrome_setup, which the task prefixes with the launch flags on every run; it is desktop_entry_exec_key now. The delimiters of those formats (the brackets of a kconfig group header, the equals sign of a key line, the hash of a comment) stay in the code on purpose and are reported as a judgement: they are the grammar of the edited files and not a name of the machine, the way the spec keeps the proquint alphabet as part of that encoding. Proofs: test_the_touchpad_group_words_come_from_the_config (another root word and another device word collect another group, while the shipped pair collects nothing) and test_the_desktop_entry_key_comes_from_the_config (another key is the line that receives the flags, and no line of the shipped key is touched); both proved by mutation, which failed exactly those tests. Gate green at 1999 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9. The next block is the comment sign of the shared text editors (config_edit.sync_directives_by_key, config_edit.sync_toml_root_directive, augeas.read_dropin_state and augeas.sync_dropin), which spell "#" while the sibling add_line_to_file already takes the sign as an argument.

Done 2026-09-13, the ninetieth block: what the audit of the comment sign really found, which is smaller and different from what the plan named, and one finding that the proof itself produced. The sign in augeas is a value: parse_augtool_print read a comment node of the augtool listing by the literal "#", and that read decides whether the ownership comment of the drop-in is seen at all (a listing whose comment is not recognized stores it as a directive and makes the task rewrite the file on every run). The sign is an argument of parse_augtool_print, read_dropin_state, sync_dropin and include_covers_dropin now, carried by [ssh_daemon_setup] dropin_comment_sign and [ssh_client_setup] dropin_comment_sign; the proof test_the_comment_label_sign_comes_from_the_config shows that another sign leaves the comment unrecognized and the node becomes a directive, and it was proved by mutation. The second finding is a defect the planned work exposed: the comment branch of config_edit.sync_directives_by_key decided nothing, because a commented directive line is already protected by the key comparison (the sign becomes part of the key), and in the pathological case where the sign is a prefix of a key it would have protected a line the task must replace; the branch is gone, the helper keeps its four arguments, and no key was added for a value that had no behaviour, which is what the user's rule forbids. The third finding is dead code: config_edit.sync_toml_root_directive had no caller in the package, in the secrets scripts or in the installer, only its own six tests; it is removed with them, and the exports list of docs/guides/project-structure.md no longer names it. Also removed in the same block: the "#" that the two ssh tasks passed nowhere and the resolved_comment_sign key that this block had added before the proof showed it decided nothing; it is not in the config, not in the dataclass and not in the checks. Gate green at 1995 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9. Live run on the target machine with ssh_daemon_setup and ssh_client_setup forced: exit code 0 and no warning line, both tasks checked their Include directive as found and synced the drop-in through augeas, and both drop-ins carry the ownership comment their section names, which is the line the read path recognizes by the configured sign.

Closed 2026-09-13, the ninety-ninth block. The stray file was a leftover of an interrupted or older write, not of the code that runs today: probing on the target machine showed that a forced run of both ssh tasks leaves the drop-in directory byte-identical (same names, same sizes, same timestamps), because the write path goes through augtool with --noautoload and a load entry of its own and Augeas saves the file only when its tree changed, and because no module of the package stages a file beside a drop-in (searched: os.replace, os.rename, shutil.move, mkstemp, NamedTemporaryFile, suffix building). The file itself was removed from the machine, to the trash and not deleted, as the rules require: /home/i/.local/share/Trash/files/pyntara.conf.fNfida carries the copy dated Aug 13 (304 bytes), so the user can restore it and see it was the old second copy of /etc/ssh/ssh_config.d/pyntara.conf. The directory now holds the systemd symlink and the managed drop-in alone, and a run over the ssh tasks keeps it that way.

Done 2026-09-13, the ninety-first block: the field names of the country record, the last module that still spelled them. pyntara.country_report built its document with the literals word, in_country, values, answers, source and raw while its siblings (public_address_report, network_addresses, the channel commands) build theirs from the report_record_keys map of the [engine] table, so one document of the report had a shape the config could not describe. The six meanings joined that map, the map grew from twelve to eighteen entries, and country_document takes the map; the completeness check of tests/config_checks.py turned from an inline list of eight required fields into the named REPORT_RECORD_KEY_MEANINGS tuple with every meaning the code indexes, applied through the same _complete_string_map helper the Xray maps use, so a config that misses any of them fails in development and not on the target machine with a KeyError. The hand-built map literal of tests/test_i2pd_address.py carried the same six names for the same reason. Proof: test_the_record_keys_come_from_the_engine_table (another map is the document the command prints), proved by mutation. Gate green at 1996 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9. Live check on the target machine after deploying the metrics with the new map: the deployed country command printed its record with exactly the eighteen-meaning vocabulary of the table (word, in_country, values, answers and, inside an answer, source, values and raw), answered from three real services with exit code 0, and reported Argentina, which is outside the configured word. Live run on the target machine with zram_service forced: exit code 0 and no warning line, the task read the installed RAM and the core count through the configured line names, reset and reconfigured every device through the configured module name (the machine carries twelve sys-block entries of it and twelve active swaps at the configured priority), and its final message names the module of the table.

Done 2026-09-13, the ninety-second block: the vocabulary of the resolvectl output, which the DNS task reads to prove that every query goes through its resolver. The task found the global block of that output, the per-link lines, the DNS server lines, the routing domain lines, the line that proves the stub resolver mode and the wildcard domain by the literals Global, Link, Current DNS Server, DNS Servers, DNS Domain, resolv.conf mode: stub and ~. while its whole command vocabulary already came from the config, so a resolved release that renames a label or a mode would have left the task with a wrong verdict and no way to answer it. Six keys of [dnsproxy_setup] carry them now (resolved_status_global_marker, resolved_status_link_prefix, resolved_status_dns_server_labels, resolved_status_dns_domain_label, resolved_stub_mode_line, resolved_wildcard_domain), read by _global_block_lines, _resolved_uses_dnsproxy and _per_link_dns_addresses; the docstring of the section names them and the specification of the task records that the names of the output are config values. Proof: test_the_resolvectl_vocabulary_comes_from_the_config (another marker, another per-link prefix and another set of labels parse another output, while the shipped ones parse nothing of it), proved by mutation. Gate green at 1997 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Done 2026-09-13, the ninety-third block, two values found by the same audit of string comparisons in the top level modules: the order in which the deployed sender walks the queue, and the keyword that pulls an ssh drop-in in. The sender compared its configured order against the literal newest_first (src/pyntara/metrics_send.py, _ordered_entries) while the checks of the same section spelled the two accepted answers by hand and the config comment named neither, so the two places that had to agree agreed only by luck. The vocabulary now lives with the rest of the section in src/pyntara/config/system_metrics_setup.py as SEND_ORDER_OLDEST_FIRST, SEND_ORDER_NEWEST_FIRST and SEND_ORDERS, re-exported through pyntara.config the way MODES is, imported by the sender and by tests/config_checks.py, which no longer defines its own copy. The second value is a name of a foreign file: augeas.include_covers_dropin compared the keyword of every directive line against the literal include before matching the pattern that must cover the drop-in, so an ssh release that renames the keyword, or a file configured with another spelling, would have made both ssh tasks report that their drop-in is not pulled in while it is. The two ssh sections carry include_directive now, augeas takes the keyword as its argument next to the comment sign and compares it casefolded, which keeps a file writing Include, include or INCLUDE equally valid, and both tasks pass their own value. The four declaration places carry the key of each section, the two specifications name it in their Parameters list, and two proofs hold the block: the order proof reads the constant of the config layer, and test_the_include_keyword_comes_from_the_config shows that another keyword finds another directive while the shipped word finds nothing of it. Gate green at 1998 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Done 2026-09-13, the ninety-fourth block, the words the code compared against in four places, and one stale comment the audit corrected. Four values left the modules. The shared reader of the sshd listen port (src/pyntara/ssh.py, ssh_port_from_directives) looked for the directive by the literal port while the ssh_daemon_setup table already carried port_directive for the reload-or-restart decision, so the same keyword lived twice and the reader followed a copy; the reader takes the section now (cfg.directives and cfg.port_directive from one object), its two messages name the configured keyword instead of the word Port, the six call sites pass the section (tor_setup, i2pd_service_setup, i2pd_address, yggdrasil_address, port_forwarding, network_addresses, public_address_report) and the same literal in the ssh_daemon_setup task now reads cfg.port_directive. The address reader of pyntara.public_address found the source token of the default route by the literal src, which is a word of the iproute2 output; it is [engine] default_route_source_key. The add_extra_repos task decided which apt sources it reads and how by the two literals .list and .sources, which are the file names apt itself reads; they are [add_extra_repos] legacy_source_suffix and deb822_source_suffix, and _collect_source_files takes the section, so the directory scan and the format decision follow one pair. The vocalinux task treated the ydotool unit as applied only when the user manager answered active, a word of that answer; it is [vocalinux_setup] service_active_state. Proofs: test_the_port_directive_comes_from_the_config (two directives carry a port, the configured keyword decides which value comes back), test_the_source_keyword_comes_from_the_engine_table, test_the_source_file_suffixes_come_from_the_config (another deb822 suffix makes a file ending in .apt the file the task rewrites, while the shipped .sources name is not an apt source and the file is left alone) and test_the_running_state_word_comes_from_the_config; the port proof was proved by mutation. The four declaration places carry the four keys, the section literals of tests/test_config_vocalinux_setup.py and tests/test_config_engine.py follow, the Parameters list of docs/spec/vocalinux-setup.md names the state word, and the extra_repos and engine tables are documented by their TOML comments as those two tables are. Found on the way and corrected: the comment above VALUE_CONSTANTS_ALLOWED in tests/test_config_value_guard.py still promised three pending migrations (the noninteractive apt environment, the key names of the metrics components, the version pattern of the setup tasks) that are all done, the first as [engine] apt_noninteractive_environment, the second as constants of the config layer the components import and the third as the shared utils.VERSION_PATTERN; the paragraph said the allowlist carries values waiting for a block while it carries exceptions alone. Gate green at 2009 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of that block on the target machine with add_extra_repos, tor_setup and vocalinux_setup forced: add_extra_repos read the real sources directory by the configured suffixes (six files found, five of them deb822 by suffix and the legacy file beside them) and reported the machine already satisfied; tor_setup read its forward target through the new reader ("reading SSH listen port from ssh_daemon_setup directives: 30222") and published the same onion address; vocalinux_setup queried the real user unit with the configured command (systemctl --user --machine i@.host is-active ydotool.service answered active), matched it against the configured word and left the enable step alone. Finding of that run, recorded here and not fixed: the forced vocalinux_setup could not replace the AppImage of a running Vocalinux ([Errno 26] Text file busy), which the task reports as a warning of a completed task and the engine turns into exit code 1. On a fresh target the app is not running, so this appears only on a re-run over a live session; a later block should let the task notice the running image and report a skip instead of a warning. The machine stayed healthy: the AppImage kept its published digest 0ee0d8e6 and the running app kept running from it.

Done 2026-09-13, the ninety-fifth block, the words that duplicated the config and the answer of the upload channel. Four values, three of them already in the config under another name, which is what made them worth finding. pyntara.public_address read the address family words of the plain iproute2 output as the literals inet and inet6 while the engine table already carried the same pair under the name it gives that vocabulary; local_addresses reads the values of iproute2_address_family_names now, so one mapping describes the family names for the JSON reader and for the plain one and a release that renames them is answered once. The same function asked the kernel about the families in a hardcoded pair of flags -4 and -6 while the engine already mapped the flag to the family; directly_connected_networks walks the keys of address_family_by_flag, so the families the task asks about are the configured ones. The panel comparison of three_x_ui_xray_setup left the traffic counters out by the literals up and down while the payload it compares was built from the configured field map; the function takes the config now and reads the two counter names from xray_field_keys, so the pair the panel uses is the pair the comparison ignores. The one new key is the answer of the upload channel: metrics_send accepted the upload by the literal prefix OK from the deployed Apps Script web app, so a script that answers with another word could never be accepted without a code change; it is [system_metrics_setup] google_script_answer_ok_prefix, named in the service key list the deployed sender reports against, in the dataclass and in its docstring, in the four declaration places and in the send section of docs/spec/system-metrics.md, and the comment of the address vocabulary in config/engine.toml now names both readers of it. Proofs: test_the_family_names_come_from_the_engine_table and test_the_family_flags_come_from_the_engine_table in tests/test_public_address.py, test_the_traffic_counter_keys_come_from_the_config in tests/test_3x_ui_xray_setup.py, and test_the_answer_prefix_comes_from_the_config in tests/test_metrics_send.py, which was proved by mutation. The hand-built expected line of tests/test_metrics.py and the shared test document followed the new key in the same edit, which is the recurring gotcha of this migration. Gate green at 2013 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of that block on the target machine with system_metrics_setup and three_x_ui_xray_setup forced: the address readers ran against the real kernel (ip -o addr show scope global for the local addresses, ip -o -4 route show proto kernel and ip -o -6 route show proto kernel, both families taken from the configured flag mapping), the panel comparison found the stored local proxy already configured, which means the traffic counters named by the configured field map were the ones left out of the comparison, and the whole routing policy was verified again; the metrics task redeployed the venv (0.3.428 to 0.3.431) and wrote /etc/pyntara/config.toml, which now carries google_script_answer_ok_prefix for the deployed sender, and a restart of system_metrics.service came back active. Exit code 0, no warning line.

Done 2026-09-13, the ninety-sixth block, one value that lived twice in one package: the type name a KConfig record carries. The config layer already held the vocabulary of that name (KCONFIG_TYPES, the tuple the checks validate a record against), and the kde_settings task compared the record type against the literal string "bool" to decide whether the write needs the type flag, so the two places that had to agree agreed by spelling alone. The vocabulary now carries the names it is built from (KCONFIG_STRING_TYPE and KCONFIG_BOOL_TYPE, with KCONFIG_TYPES derived from them, so no third copy can appear), the package re-exports both, and the task imports the boolean name and compares against it. Proof: test_the_bool_type_word_comes_from_the_config_vocabulary (another name in the task's constant is the name it compares against, so a record of the boolean type stops carrying the type flag, and the name stays a member of the tuple the checks read), which was proved by mutation. Gate green at 2014 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of that block on the target machine with kde_settings forced: the task applied the whole table (696 kwriteconfig6 calls, exit code 0, no warning line), and the boolean records went out with the configured type name, for example runuser -u i -- kwriteconfig6 --file kdeglobals --group KDE --key AutomaticLookAndFeel --type bool true --notify, which is exactly the flag the configured vocabulary decides.

Remaining of the same family, recorded for the next block rather than guessed at: the two probe labels resolvectl and nmcli in the DNS task, which name the configured commands and select the parser of their output; the curl answer code 000 that marks "nothing answered" in the tunnel check of the 3x-ui task; the truth spellings 1, true and yes that the entry point accepts for its environment flags; and two judgements that need writing down instead of code: the separators of the two answer formats the country reader splits (the equals sign and the colon of a key=value line, with the quotes and commas of a JSON line) and the brackets of a kconfig group header, which block ninety already recorded as the grammar of the edited files.

Settled 2026-09-13, the hundred-and-fifth block: the last three values of that list were moved in block ninety-seven and block ninety-eight settled the two judgements, and the user has now authorised the exception itself ("1 - оставляем как есть"). The syntax of a foreign format is a type of the Exceptions section of docs/spec/config-content.md, with the kconfig brackets and the equals sign that joins a key and its value, the hash of a comment and the separators of an answer the country reader parses named as its examples, so the judgement of block ninety-eight is a rule of the spec now and no longer a note that waits for the user.

Done 2026-09-13, the ninety-seventh block, the three remainder values of that family, and one of them closed a defect the guard cannot see. The DNS task carried the labels resolvectl and nmcli next to the commands of the same names and chose the reader of the answer by comparing that label, so the program name lived twice and the branch decided which of two configured commands had answered. The list is now a pair of (command, reader) and a diagnostic names the first element of the configured command, so the label cannot disagree with the command and no program name stands in the code; the two readers became named functions, each with the docstring of the output it understands. The 3x-ui task read the code curl prints when nothing answered at all (000) in four places of its two path checks, and it is [three_x_ui_xray_setup] tunnel_probe_no_answer_code now, next to the write-out text that produces it, with the reason for its own key written in the comment. The entry point accepted the answers 1, true and yes for the boolean environment variables by a literal list inside the function; the list is [engine] environment_flag_true_values, read without case, which is the last value of the run's own interface that stood in code. Proofs: test_the_program_name_in_a_diagnostic_comes_from_the_command in tests/test_dnsproxy_setup.py, test_the_no_answer_code_comes_from_the_config in tests/test_3x_ui_xray_setup.py (an answer carrying the configured code counts as no answer while the shipped code reads the same answer as a status) and test_the_true_answers_of_the_flag_come_from_the_engine_table in tests/test_entry.py (another list makes another answer true and the shipped answers stop being true). The four declaration places carry both new keys, the wrong-value variants of tests/test_config_engine.py and tests/test_config_three_x_ui.py name them, the send section of docs/spec/dnsproxy-setup.md and the Parameters list of docs/spec/3x-ui.md follow, and the flag description of docs/contracts/architecture.md now says where its answers come from. Gate green at 2021 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9. Left for the decision of the user, because neither is a value of a listed type and neither is an exception in docs/spec/config-content.md, which only the user may extend: the word all of PYNTARA_FORCE_TASKS (documented in the architecture contract as the keyword of the environment interface) and the single characters that mark the structure of a foreign file the run parses (the brackets of a kconfig group header, which block ninety recorded as the grammar of the edited file, and the equals sign, the colon and the JSON punctuation the country reader splits an answer by).

Live check of the ninety-seventh block on the target machine with dnsproxy_setup forced: both configured commands ran through the new pair of a command and its reader, the task produced no diagnostic line (so the program names came from the commands themselves), the resolver cutover and the routing check passed, and the run finished with exit code 0 and no warning line. The same run carried PYNTARA_SKIP_APT_UPDATE=1 and never refreshed the apt index, which is the flag path the new engine list feeds; the list itself is held by the unit test.

Done 2026-09-13, the ninety-eighth block, the last word of the interface, and a correction of my own earlier note. The entry point decided that PYNTARA_FORCE_TASKS means "all tasks" by the literal all, and the note above it left that word for the user to decide because no line of the Exceptions section covers it. The decision was not needed: the package already keeps the accepted words of its own interface in the config layer (MODES, which production reads to accept a mode, and SEND_ORDERS beside it), and a deployment whose operators say every instead of all is a machine the config describes. The word is [engine] force_all_keyword now, read without case by _resolve_force_tasks, which takes the engine as its first argument; the docstring, the dataclass field and its description, the check, the shared test document, the wrong-value test and the flag description of the architecture contract follow, the contract no longer names the word all itself, and a proof shows that another word in the table forces the whole run set while the shipped word becomes a name the catalog does not know. Gate green at 2023 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9. Live check: every live run of this session carries PYNTARA_FORCE_TASKS with task names and PYNTARA_SKIP_APT_UPDATE, and the forced runs of blocks ninety-four to ninety-seven ran with exactly this code path, so the interface reads its word from the config on the target machine.

Written judgement of the same block, kept here because the Exceptions section of docs/spec/config-content.md may only be changed by the user: the single characters that mark the structure of a foreign file stay in the code. They are the brackets of a kconfig group header (kde_settings, kde_keyboard_setup, chrome_setup), the equals sign that joins a key and its value in those files, the hash that marks a comment, and the equals sign, the colon and the JSON punctuation the country reader splits an answer of a service by. The reasoning: they are not names, paths, numbers or texts of the machine but the syntax of a format, in the same sense as the dot of a file name or the slash between path segments, and a run that answered a service spelling its record with another separator would need a reader for another format and not another value. The judgement is recorded instead of a key because the alternative, a key per character of every format the run reads, would describe the syntax of foreign files in the machine config. The user authorised the exception on 2026-09-13 and the syntax of a foreign format is a type of the Exceptions section of docs/spec/config-content.md now, so this paragraph is the history of the judgement and not a request.

Live check of the ninety-eighth block, and an accident of that check reported here in full. The run carried PYNTARA_TASKS=hostname with PYNTARA_FORCE_TASKS=all, and the keyword from the config did exactly what the block claims: the log printed "Force: hostname" and the one task of the run set ran, so the interface reads its word from the engine table on the target machine. The accident: the hostname task was in that run set, and its force mode always generates a fresh name, so it renamed the machine from dozor-gunid to tukon-jojog and rewrote /etc/hostname. The name was restored in the same session with hostnamectl set-hostname dozor-gunid, the task then reported "already configured" for dozor-gunid again, the NextDNS profile id file still carried 9eb942 and the metrics and DNS services stayed active, so the machine is where it was. Lesson kept for every later block: a live proof of the force path must use a task that only verifies and never the word all over a run set that contains hostname, because that name feeds password generation and the deterministic profile choice (docs/spec/secrets-model.md, docs/spec/nextdns-profile.md).

Done 2026-09-13, the ninety-ninth block, the leftovers a staged write can leave, and two drifts found on the way. The audit of every temporary file the run creates (os.replace, os.rename, shutil.move, mkstemp, NamedTemporaryFile and every configured temp name searched) found one writer that did not clean up after itself and six that do. The clean ones: metrics_commit removes its ingest staging in a finally, the telegram and vocalinux downloads remove their partial file on failure, the telegram extraction directory is removed in a finally and the chrome keyring directory by a context manager, the ffmpeg build writes a staged binary and removes it on failure, and the dnsproxy staged binary is the run's own artifact. The one leak was port_forwarding.save_state: it wrote the state through a temporary file beside it and moved it into place, but when the write, the chmod or the move failed, the temporary file stayed in the state directory forever, in a directory the run owns and a reader of the machine looks at. It now removes the temporary file on every path through a finally, and a failure to remove it is logged instead of raised, because the deployed service must keep running and the function's contract is to log. Proof: test_a_failed_state_write_leaves_no_temporary_file (a failing os.replace leaves the previous state file untouched and nothing beside it), proved by mutation, which failed exactly that test. On the way: README.md still named the word all as the force keyword and the answers 1, true and yes as the flag answers after block ninety-eight moved both into the config, so the two interface paragraphs now point at force_all_keyword and environment_flag_true_values of the engine table. Gate green at 2024 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of the ninety-ninth block on the target machine: the ssh drop-in directory was byte-identical before and after a forced run of both ssh tasks (the probe described above), and the stale copy went to the trash. For the fixed writer the run deployed the current revision into the deployed venv (0.3.431 to 0.3.439, the line "missing or stale" in the task log), the auto_port_forwarding.service stayed active, and a probe with the deployed interpreter on a temporary state path showed the behaviour the block claims: a successful save leaves only state.json, and a save whose move into place fails with "no space left on device" leaves the directory holding state.json alone, with the previous state intact and the failure logged. The real state directory /var/lib/pyntara held only port_forwarding_state.json before and after.

Done 2026-09-13, the hundredth block, two defects of the same kind found by the finding recorded in block ninety-four: work the run does for nothing, and a failure reported without saying what to do. The vocalinux task copied the cached AppImage over the installed one whenever force mode was on, without ever comparing them, so a forced rerun over a running app failed with [Errno 26] Text file busy, the task reported a warning and the engine exited 1, although the installed bytes were exactly the wanted ones. The install step compares the cached release with the installed file now (filecmp.cmp with a full content compare, the same rule the telegram task uses) and leaves the file alone when the bytes match, logging that it does; the ownership and the mode are applied either way, and the step reports a change only when it copied or moved something. The second defect is the message: a genuine difference under a running app produced a bare errno text, so the task now says in words that the running Vocalinux holds the file, that the installed image is left as it is, and that the app must be closed and the task rerun; the step stays a warning of a completed task, which is the contract for a step that cannot run. Found in the same read and fixed with it: the autostart entry was written only when the install step reported no error, although the entry points at the installed path and not at the outcome of the copy, so it is written whenever the file is there; a first install that could not fetch the image still skips it, because then there is no file to point at. Proofs: test_a_forced_rerun_does_not_rewrite_the_installed_image (a forced run over an installed image with the cached bytes refuses any copy at all and reports no warning) and test_a_busy_installed_image_is_reported_with_the_remedy (a differing image held by a running app keeps its old bytes, yields the message that names the remedy, and still gets the autostart entry); both proved by mutation, each failing exactly its own test. Gate green at 2026 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check planned for this block, the same run that produced the finding: vocalinux_setup forced again on the target machine with the app running, expecting no warning and exit code 0, with the log line that the installed image already matches the cached release.

Live check of the hundredth block, done and green: Vocalinux was running during the run (the mounted image process and the AppImage process were both alive), the task logged "the installed image already matches Vocalinux-0.16.2-x86_64.AppImage" and did not touch the file, the run finished with exit code 0 and no warning line, and the autostart entry was written, which the previous behaviour skipped exactly because the copy had failed. The AppImage kept its published digest 0ee0d8e6, its size and its date, and the app kept running from it.

Done 2026-09-13, the hundred-and-first block, and it closed the second front of the same order: a task reported a failure in a field the run does not count. The audit of the task modules against the recoverable-failure policy (a step that cannot run is a warning of a completed task) measured every module by its guarded steps and found one that broke the documented shape: cli_tools returned a successful result carrying the failing packages in the error field, so a machine where a configured package could not be installed ended the run with "All 20 tasks finished" and exit code 0, while the contract says there is no separate error outcome and every failure travels in warnings, which the entry point counts and turns into a nonzero exit (docs/contracts/task-model.md, docs/contracts/architecture.md, Task contract). The task puts each package that could not be installed into warnings now, with its own reason, in both branches: above the threshold the warning names the packages, below it the shortfall line names the installed share and the configured threshold and the per-package reasons follow; the failed names appear once, in the warn lines, and not again inside the done message; the error field is left empty, which makes cli_tools the last module that used it on a successful result (searched: the field is set nowhere else outside a failed result). The module docstring and the task docstring say what the result carries and why. Proofs: test_missing_package_does_not_block_others and test_apt_hang_reports_the_timeout_as_a_warning assert the empty error field beside the warning, test_gives_up_after_configured_retries, test_zero_threshold_never_fails and test_single_failure_within_threshold_is_not_fatal follow the warning, and test_update_failure_still_installs_from_cache reads the index-refresh warning from the warnings instead of the message; the shape was proved by mutation, which failed five of those tests when the error field came back. The entry point side of the chain is held by test_run_reports_warnings_and_exits_one: a task with warnings makes the run print "Finished N of N tasks, 1 with warnings" and exit 1. Gate green at 2026 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of the hundred-and-first block on the target machine: cli_tools ran over the real machine and reported the state it found (the configured set is installed, so no warning and exit code 0), which is the happy path of the changed result; the warning accounting of the engine itself was observed live in block ninety-four, where the vocalinux warning ended a run with "Finished 3 of 3 tasks, 1 with warnings: vocalinux_setup" and exit code 1. A package failure cannot be induced on this machine without editing the shipped package list, so the failure branch is held by the unit proofs named above and not by a live run, and this line says so instead of claiming otherwise.

Done 2026-09-13, the hundred-and-second block, the steps of the vocalinux task that were not guarded, found by the same measurement that found cli_tools. Counting the guarded steps of every task module showed that vocalinux_setup calls its write helpers without a try where its siblings catch: _write_user_file ran the mkdir, the chown and the chmod as the desktop user and _sync_echo_shortcut read and wrote the shortcut through kwriteconfig6, and a failure of any of those raised out of task(ctx). The runner's safety net turned such a run into a failed task, so one unwritable user file dropped the files after it, the shortcut and the reason of the failure, against the policy that a step which cannot run is a warning of a completed task. Both helpers return (changed, error) now and catch OSError and subprocess failures: the file that cannot be written is reported with its path, the shortcut with its component and action, the remaining files and the shortcut still run, and the task ends as done with those reasons in warnings, which the entry point counts and turns into a nonzero exit. The module docstring and the section A step that cannot run of docs/spec/vocalinux-setup.md say so. Proofs: test_a_failed_user_file_write_is_a_warning_and_skips_that_file (a failing ownership step on the app config leaves the autostart entry, the empty-action file and the shortcut written and the task done) and test_a_failed_shortcut_write_is_a_warning (a failing kwriteconfig reports the action and keeps the written files); both proved by mutation, each failing exactly its own test when the catch is narrowed to OSError. Gate green at 2028 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of the hundred-and-second block on the target machine, with Vocalinux running and the task forced: the run finished with exit code 0 and no warning line, the installed image was left alone ("the installed image already matches"), the app config, the autostart entry and the empty-action desktop file were written through the guarded helper, and the Meta+S shortcut was registered. The failure branch itself is held by the two unit proofs, because a write failure cannot be induced on this machine without breaking the desktop user's own files.

Done 2026-09-13, the hundred-and-fourth block, the key lists of the four address commands, which were inline tuples nobody proved complete. The audit of the absent-key report of the address components found that i2pd_address named three of the five keys it reads, tor_address four of five, yggdrasil_address one of four and public_address_report one of two. The consequence was invisible in the suite and visible on the machine: a config missing an unnamed key built a record whose value was None. The deployed code on this machine, run with the system config minus the report_channel_name key of the i2pd, tor and yggdrasil sections, printed a record with "channel": null and exited 0, so an incomplete config produced a nameless record instead of one line naming the key. The decision follows docs/spec/config-content.md (Where a value is declared and checked): a list of the key names a component reads is not a value, it belongs to the table it describes, so the four lists now live in the config layer next to the fields they name, as I2PD_ADDRESS_CONFIG_KEYS, TOR_ADDRESS_CONFIG_KEYS, YGGDRASIL_ADDRESS_CONFIG_KEYS and PUBLIC_ADDRESS_CONFIG_KEYS of src/pyntara/config/i2pd_service_setup.py, tor_setup.py, yggdrasil_service_setup.py and three_x_ui_xray_setup.py, exported through pyntara.config, and the four components import them and keep no copy; no allowlist of the value guard was touched, because the config layer is where the guard expects a declaration. public_address_report checks the missing keys before the empty service list now, so a config without server_ip_services names the key, and an empty list keeps its own message. Proofs: tests/test_address_component_config_keys.py compares each list with the keys its module reads in both directions (a read without a name fails, a name nothing reads fails, a repeated name fails) and demands that the report be built from the list of the config layer; the four entries of COMPONENT_KEY_LISTS in tests/test_config_coverage.py prove that every name is a field of its table and that the component reads the list of the config layer and not a copy, which the existing test could not prove for the components it covered before; test_a_key_missing_from_the_config_is_named proves the message. Each proof was mutated: removing report_channel_name from the i2pd list failed the completeness test, adding address_file_mode to it failed the other direction, a local copy of the tor list failed the coverage test, and the call site of the i2pd report replaced by an inline tuple failed the report proof. Gate green at 2037 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

The live check of that commit found one more case, in the same family and in the module the completeness proof could not see. yggdrasil_address checked its list inside the branch that reads the saved address file, so with the live admin query answering and report_channel_name absent the command still built a record with "channel": null and exited 0, while its siblings i2pd_address and tor_address checked the list before they used anything. The check moved to the top of access_record, before the live query, and the message keeps naming the keys; the fallback branch keeps only the read that can fail. yggdrasil_address.access_record now reads the section the way parts of tor and i2pd always did: one list, checked once, before both sources. Proof: test_a_missing_key_of_the_channel_is_named (the live query answers, the config has no report_channel_name, the command exits 1 and prints the key) proved by mutation, which fails exactly that test when the check is put back inside the branch.

Live check of the hundred-and-fourth block on the target machine, with the pushed revision deployed by a forced run of system_metrics_setup (installer version 0.3.447, then 0.3.448 after the second commit). Before the deploy, the venv of revision 0.3.439 ran the system config with the report_channel_name key of the i2pd, tor and yggdrasil sections removed from a copy: i2pd_address, tor_address and yggdrasil_address each printed a record with "channel": null and exited 0. After the deploy the same three commands name the key and exit 1 (the i2pd_service_setup, tor_setup and yggdrasil_service_setup sections respectively), and public_address_report names server_ip_timeout_seconds when the key is removed from its own section. The healthy path is unchanged on the same deployed revision: with the complete config the three address commands print their records with the channel names i2p, tor and yggdrasil and exit 0, and public_address_report prints the ipv4 record with its ssh command and the ipv6 reason and exits 0.

Done 2026-09-13, the hundred-and-fifth block: the journal made true, and the last judgement of the value work turned into a rule of the spec. The audit read every paragraph of this file that presents work instead of a record and checked each claim against the code; two were false or open, and both are corrected above. The section REPO_ROOT duplication said the constant is computed in src/pyntara/utils.py and called the move into Context an optional improvement, while that move was done on 2026-09-11 and the single computation has lived in src/pyntara/pyntara.py since 2026-09-12, with its depth corrected on 2026-09-13; the paragraph Remaining, by type listed the module level constants, the 139 call sites of 34 programs, the paths of foreign programs and of external API interfaces, the 24 file names and the site addresses as open work, and every one of them is done: the guard's allowlists of the command argv, of the absolute path and of the shared pattern are empty, its list of value constants holds the documented exceptions of the spec and nothing else, no module under src/pyntara outside the config layer spells a program name, and the paths, the file names and the site addresses are keys of the sections that own them. The user's decision on the first item of the remaining work, keep the syntax characters of foreign files in code as they are, is a rule now: docs/spec/config-content.md gained the type The syntax of a foreign format in its Exceptions section, with the brackets of a kconfig group header and the equals sign that joins a key and its value in that file, the hash of a comment, and the equals sign, the colon, the semicolon and the comma that separate the fields of an answer the country reader parses named as its examples, and the two paragraphs of this file that kept that judgement as a note waiting for the user (block eighty-nine and block ninety-eight) say so. No code changed, so the suite is unchanged, and the gate ran anyway: 2038 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Done 2026-09-13, the hundred-and-sixth block, the last item of the remaining work: the repository root travels in the Context, and the audit found that part already done, so the block added the proof it was missing instead of the code the note asked for. The note wanted the root computed once in the composition root and handed to the tasks through Context; the code does exactly that since 2026-09-11, when Context gained the field repo_root and every task began to read its context instead of importing a constant, and the single computation has lived in src/pyntara/pyntara.py since 2026-09-12 with the depth corrected to parents[2] on 2026-09-13, after a live run failed on the shallow value. The evidence of the audit: the field is declared by src/pyntara/context.py, the composition root is the only construction site of a Context, thirty-seven reads of the field run through the tasks and the package, tests/test_utils.py refuses a second computation of REPO_ROOT and any module that names the constant instead of the Context, and the two standalone scripts under secrets/ keep their own local definition, which the note allows. The gap the audit found: nothing proved that the root the Context carries is the clone root, so the wrong depth that broke every live task while the whole suite stayed green could return unnoticed. The construction left the body of run() for the named helper _run_context(cfg, mode, names), and tests/test_entry.py::test_the_run_context_carries_the_clone_root compares ctx.repo_root with the clone the tests run from and requires config/ and task_data/ under it, so the depth is held by the suite. The proof was mutated: REPO_ROOT at parents[1] fails exactly that test and nothing else. Gate green at 2039 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of the hundred-and-sixth block: the pushed revision was deployed on the target machine by a forced run of system_metrics_setup through inst.sh (installer version 0.3.451, exit code 0, no warning line). The run built its Context through the new helper and the task rendered and wrote all five units from task_data/ of the deployed clone, which is exactly the step that failed with the shallow root on 2026-09-13, so the root the Context carries is proven on the machine and not only by the suite.

Done 2026-09-13, the hundred-and-seventh block: the closure of the value front checked by a scan instead of by memory. The guard of the suite sees four shapes; a value could still hide as a literal inside a function body or as a word a comparison names, so this block read the sources of src/pyntara outside the config layer with an abstract syntax tree and counted what the guard cannot see. Numeric literals: 153 occurrences of 1 and 144 of 0, and the rest are parse lengths and indices (len(argv) != 2, data[:8], data[I2PD_IDENTITY_SIZE - 3]), the HTTP status 200 of the panel client, the sizes and masks of the DNS header of the dnsproxy probe (12, 256, 4096, 0x8000, 0x000F, 0x0100), the I2P identity size 387 and certificate type 5, the IP version checks 4 and 6, the digest prefix length 4 of the host hash, and the shift and mask arithmetic of the proquint encoding (0x3, 0xF, 10, 12). No duration, port, size, count or amount of the machine is written in code: the values of that kind are keys of the sections, which is why 60, 30222, 825 and their family no longer appear in the scan. String comparisons: 15 in the whole package, 14 of them the interpreter's `__name__ == "__main__"`, one an empty-string check, and one the internal kind marker of the route expectations of the 3x-ui task ("domain" or "address"), which decides which keyword argument the panel route test receives and is the shape of that module's own tuple rather than a value of the machine; the word is written once next to the destinations it classifies and the docstring of _route_expectations says what it means. Judgement recorded: the kind marker stays in code like a boolean argument of our own call, and if the user reads it as a value, the fix is a key of the section. With this the front of the standing order is closed and held: every value of a listed type comes from config/ (the guard allowlists hold the documented exceptions of docs/spec/config-content.md and nothing else), every task module follows the recoverable-failure policy, and the closure itself is now a checked statement.

Done 2026-09-13, the hundred-and-eighth block, found by the live functional check of the deployed metrics chain that this journal named as the next block. The check itself succeeded and proved the chain with the deployed revision 0.3.451: one forced cycle of system_metrics_collector.service wrote its report into the spool, the ingest path unit picked it up and left it in the outbox, the sender dispatched it into the Google Drive channel queue and the rename into main_sent followed, so collector, ingest, dispatch and upload all work on the machine. The finding came from the same journal: the first upload attempt was refused and the sender printed the whole answer of the provider into the journal, a 13 kilobyte HTML page of Google Drive ("No se pudo abrir el archivo en este momento"), because the failure message carried the raw body of the answer; the retry then succeeded, and fourteen days of journal carry eighty-one such refusals, each one a page in the log of the target machine. The decision: the message names the answer in a bounded way instead of pasting it. The sender prints the length of the whole answer and its first non-empty line with runs of whitespace collapsed, cut to the new key google_script_answer_excerpt_chars (200 characters) of [system_metrics_setup]; the retry mode, the queue behaviour and the answer prefix stay as they are, and the full answer is not lost, it stays in the queue for the next attempt. The key went into the four declaration places (the TOML comment, the dataclass field with its docstring, the check that demands a positive integer in tests/config_checks.py, the mirror of the shared test document), joined SERVICE_CONFIG_KEYS of the config layer, which changed the expected key list of test_main_reports_a_config_without_the_service_values in the same commit, and is named in the Retry mode section of docs/spec/system-metrics.md. Proof: test_a_refusing_answer_is_printed_as_one_bounded_line (a page of 413 characters with the configured 40 character excerpt leaves one line that carries the length of the whole answer and the first 40 characters and never the page), proved by mutation, which fails exactly that test when the cut is removed. Gate green at 2040 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of that block on the target machine, in the deployed revision 0.3.454 and with the new key present in /etc/pyntara/config.toml (google_script_answer_excerpt_chars = 200). The deployed sender ran over an isolated queue in a temporary directory whose configured upload command answers a 3153 character HTML page: the journal carried one line, "sending probe-report.txt failed: the web app answered 3153 characters without the 'OK ' prefix: " followed by the first 200 characters of the answer, the entry stayed in the queue for the next attempt, and no page reached the log. The temporary directory went to the trash afterwards. The same forced deployment proved the chain itself again: collector into the spool, ingest into the outbox, dispatch into the channel queue and the upload renamed into main_sent.

OPEN ITEM FOR THE USER, found by that live check and not touched: the Google Drive channel refuses an upload intermittently. Fourteen days of journal carry eighty-one refusals, each answered with a provider page instead of the OK prefix, and each followed by a success on a later attempt of the same entry; the queue entry is never lost and the retry mode sends it again, so the machine reaches the goal with a delay and an extra journal line. The cause is not established: the stored deployment URL is well formed and matches the configured pattern (script.google.com/macros/s/<id>/exec, checked on the machine), so what remains is the behaviour of the endpoint itself. The one hypothesis worth a decision: the configured google_script_upload_command uses curl --location, and curl converts a redirected POST into a GET, so the script may receive the redirect without the payload and answer the page; the App Script deployment answers doPost, so preserving the method across the redirect (--post302, optionally --post301) is the classical fix. That change belongs to the config of the sending channel and alters a live integration, so it needs the user's word: it is a one-line change of google_script_upload_command plus a live proof over the queue. Until then the channel keeps working through its retry.

Done 2026-09-13, the hundred-and-ninth block, the last residue the audit of the report documents found. The scan for the two shapes the guard cannot see, string literals inside function bodies and dict literals that mix configured names with written ones, returned one hit and one family of words: public_address_report wrote the field name of its reason record as the literal "reason" while every neighbouring field of the same document came from engine.report_record_keys, and it wrote the words of its family field as the literals "ipv4" and "ipv6" while the whole rest of the report shape lives in the config. Both are values of the shipped telemetry: a consumer that reads the document by its field names sees them, and another word would be another document. The fix follows the shape the project already uses for a report vocabulary: "reason" joined the meaning list and the map of engine.report_record_keys (so the check demands it and a config that misses it fails the suite instead of raising on the target machine), and the family words are the new engine mapping report_family_words, validated as a complete map against the meanings the code indexes. public_address_report reads both and never spells either; a table whose map cannot name a family is reported in words by families_without_a_word and the command exits 1 without printing a document nobody could read. Proofs: test_the_family_words_and_the_reason_field_come_from_the_config (another word and another field name are the printed document, the shipped names stop being used) and test_a_family_without_a_word_is_named (the missing entry is named, nothing is printed, exit 1); the first was proved by mutation twice, once for each write, each mutation failing exactly that test. The change also moved the anchor of test_record_follows_the_configured_report_vocabulary in tests/test_i2pd_address.py, which replaces the whole map line and would otherwise have stopped mutating anything (the gotcha of the hand-built section literals, met again). Gate green at 2042 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of that block on the target machine, in the deployed revision 0.3.457: /etc/pyntara/config.toml carries report_family_words and the reason meaning of report_record_keys, the deployed command printed its two records with the family words ipv4 and ipv6 and the reason under the field reason, and a copy of the same config with the words v4 and v6 and the reason field renamed to why made the deployed command print exactly those: the words and the field name of the shipped telemetry come from the config on the machine and not from the code. The temporary files of the check went to the trash.

Done 2026-09-13, the hundred-and-tenth block, two residues of classes the project had already decided, found by the scan for literal string lists. The first: country_report, the fifth report command of the collector, still built the list of the keys it reads as an inline tuple while the four address commands read theirs from the config layer since block hundred-and-four. The list is COUNTRY_REPORT_CONFIG_KEYS of src/pyntara/config/three_x_ui_xray_setup.py now, exported through pyntara.config, read by the command, and covered by the same completeness proof as its siblings, which was renamed to tests/test_report_component_config_keys.py because it covers all five report commands and not only the address ones. Mutation: dropping country_word from the list fails exactly the case of the country report. The second: chrome_setup wrote the appletsrc group of a task manager applet as the literal pair "Configuration" and "General" while the same section already carried appletsrc_file_name, appletsrc_launchers_key and taskbar_plugin_names, so the group is the value of a foreign file that was still in code, of the same class as the touchpad group words of block eighty-nine. It is the [chrome_setup] key appletsrc_launcher_group now (the group segments Plasma nests the file with, written as an array), read by _taskbar_launcher_groups, declared in the four places, listed in the Parameters section of docs/spec/chrome-setup.md, and held by test_the_launcher_group_comes_from_the_config, which the mutation of the write proved (the first mutation attempt of this part failed no test, so the proof was written before the code was trusted, which is the lesson of block ninety replayed). Gate green at 2046 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of that block on the target machine, in the deployed revision 0.3.459: the command of the country report ran against the system config and printed its record (the address values, the country and the word of the configured target) with exit code 0, and the deployed package imports COUNTRY_REPORT_CONFIG_KEYS in the command and in the section module, so the list comes from the config layer on the machine. chrome_setup ran unforced over the real desktop: it walked the pinning step, found the launcher already pinned (so the configured group matched the real appletsrc) and finished with "browser already set up", exit code 0 and no warning line; the new key appletsrc_launcher_group is in /etc/pyntara/config.toml with the two segments of the shipped config.

Done 2026-09-13, the hundred-and-eleventh block, the vocabulary of the one-line apt sources format and a judgement. The same scan that opened block hundred-and-ten found that add_extra_repos recognized a legacy source line by the written pair "deb " and "deb-src " and the archive URI token by the written pair "http://" and "https://", while the very same section already carried the deb822 field names, the two file suffixes and the keep-debs body. The two pairs are the vocabulary of the format of a foreign file, so they are the [add_extra_repos] keys legacy_source_type_keywords (the keywords that open such a line, with the space that separates the keyword from the rest, so a word merely starting with the letters of a keyword is not taken for a source) and source_url_schemes (the schemes that mark the archive URI), read by _process_legacy through the section; an operator whose mirror answers another scheme is answered in the config instead of a refusal. Proof: test_the_line_keywords_and_schemes_come_from_the_config (another keyword and another scheme rewrite the line of that format and leave the shipped line alone), proved by mutation twice, once for each value, each mutation failing exactly that test. Judgement recorded in the same block: the boolean spellings of the zswap kernel interface (("Y", "N") and the accepted ("Y", "1"), plus the y and 0 forms the normalisation accepts) stay in code. They are the interface the kernel defines and not a value of the machine: no operator can choose another spelling and keep a working swap device, which is the class of the DNS header masks and the proquint table, and the spec's Exceptions section covers numbers that encode a protocol while these letters encode the same thing. Gate green at 2047 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9. One honest note: the first run of that gate reported one failed case in the hook suite (1 passed, 1 failed) and the failing case was not captured, because the command kept only the summary line; five later runs of the same suite, including a second full gate, are green and the case never reproduced, so the flake stays unexplained and the lesson is that a suite which reports a failure must have its full output kept the first time.

Live check of that block on the target machine, in the deployed revision 0.3.461. The task ran unforced over the real apt configuration: it read the sources of /etc/apt/sources.list.d, found the Ubuntu section through the configured deb822 field names and finished with "already satisfied", exit code 0 and no warning line. The legacy vocabulary was proven directly on the deployed code, because this machine keeps its sources in the deb822 format and has no one-line file to rewrite: the section of the machine carries legacy_source_type_keywords = ["deb ", "deb-src "] and source_url_schemes = ["http://", "https://"], a one-line "deb http://..." entry was rewritten with the configured components, the same entry with another keyword and another scheme in the config ("repo ", "mirror://") was rewritten instead, and the shipped keywords ignored that line. So the two values are read from the config of the machine and not from the code. The deployment also showed a detail worth keeping: a live run that selects only add_extra_repos does not refresh the deployment venv, because the venv belongs to system_metrics_setup, so a first probe of the new code failed with an attribute error until a forced run of that task deployed the revision.

Done 2026-09-13, the hundred-and-twelfth block, the last shape of the audit and its record. With the literal string lists closed by blocks hundred-and-ten and hundred-and-eleven, the scans were extended to the two shapes that were still unread: a dict literal that mixes configured field names with written ones (one hit, fixed in block hundred-and-nine) and a literal parameter default, which the spec names as subject to the rule in the same sentence as a module constant and a literal in a function body. The scan of src/pyntara outside the config layer found forty-five defaults and every one of them is either the absence marker None, the empty string of an optional prefix or stop word, the zero port of a probe whose real port arrives as an argument, or the separator of the proquint encoding. The proquint separator and the trailing marker are the punctuation of that encoding, which the Exceptions section already lists by its bit shifts: they are not a name, a path, a number, a text or a vocabulary of the machine but the grammar of an algorithm the run applies, in the class of the dot of a file name, and an operator who changed them would be asking for another encoding and not for another value. No spec line was added, because the doubt rule sends a value of a listed type to the config and this is not one; the judgement is recorded here so the shape is not re-audited. With this the value front is closed against every shape the scans can see: module level constants and private ones, command argv, absolute paths outside the kernel, shared regular expressions, string comparisons, dict literals that mix configured and written names, literal string lists and tuples, numeric literals, and parameter defaults. The guard of the suite holds the first four; the rest are recorded class by class above. Two further shapes were read in the same block and are clean: no set literal of strings exists outside the config layer, and the only dataclass outside it that carries field defaults is TaskResult of src/pyntara/models.py, whose False and None are the shape of a result and not values of the machine. That makes nine shapes read, and the front is closed against the shapes a static scan can name; a value whose type no scan can name would need the spec's list of types to grow, which only the user may do.

Done 2026-09-13, the hundred-and-thirteenth block, the last code area the audit had not read: the bodies under task_data/. The three DBus clients the run passes to the system interpreter (kglobalaccel_release.py for the appearance task, apply_hotkeys.py for the keyboard task and list_desktop_ids.py for the desktop count) carried the interface of the desktop in their own text: org.kde.kglobalaccel, /kglobalaccel, org.kde.KGlobalAccel, org.kde.KWin, /VirtualDesktopManager, org.kde.KWin.VirtualDesktopManager, org.freedesktop.DBus.Properties and the property desktops. The spec names the interface path of an external API among the types that go into the config and gives /VirtualDesktopManager as one of its examples, and the same section already carried the three qdbus6 command lines with exactly those names inside them, so the values were in the config and in the code at the same time. The block moved them to their owners: the three KGlobalAccel names are engine keys (a shared daemon of two tasks, like the augtool and upnpc vocabularies), the KWin names are keys of [kde_settings], and the three command templates now name the interface through the placeholders {kwin_bus_name}, {virtual_desktop_manager_object_path} and {virtual_desktop_manager_interface_name}, so each name is written once. The clients are templates in the string.Template form the other task_data bodies use, and the task renders them before passing them to python3 -c, through utils.kglobalaccel_names for the shared daemon and a small builder of the section for the KWin names. A config that cannot name the interface would leave an unfilled placeholder, which the substitution refuses loudly instead of running a client against a wrong bus. Proofs: test_the_release_client_dbus_names_come_from_the_engine_table (another vocabulary in the engine table is the client text, the shipped names are absent), test_live_apply_runs_the_script_the_config_names (the keyboard client runs rendered, not as shipped), test_the_desktop_dbus_names_come_from_the_config (another vocabulary is what the count, create and remove commands carry and what the desktop list client receives, with no placeholder left unfilled); the first and the third were proved by mutation, and the second became value-sensitive in this block. Ten test call sites of the two helpers gained the new argument, and one existing test that asserted the shipped script text now asserts the rendered text. Gate green at 2049 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9.

Live check of that block on the target machine, in the deployed revision 0.3.465. The three names are in /etc/pyntara/config.toml (kglobalaccel_bus_name, kwin_bus_name, virtual_desktops_property_name) and the three qdbus6 commands carry the placeholders now. kde_settings ran unforced and walked the whole appearance path, including the line "released 2 hotkey owners in the running daemon": that is the KGlobalAccel client rendered with the engine names and executed for real against the running daemon, after the task cleared the conflicting records; kde_keyboard_setup reported itself already configured (its client applies only when a layout hotkey changes). The desktop list client was proven separately, because it runs only when the live desktop count differs from the configured one and changing the count would rearrange the user's session: the deployed code rendered the client from the machine's config into a temporary file (no placeholder left unfilled), and the system interpreter of the desktop user ran it against the real session, reading the four desktop ids through the configured bus name, object path, properties interface and property name with exit code 0. The temporary file went to the trash afterwards.

Done 2026-09-13, the hundred-and-fourteenth block, the last files the audit had not read: the C capture engine of ffmpeg_setup and the desktop entry that grants it the screencast interface. The literals there are the syntax and the vocabulary of the protocols the code speaks, judged one by one and recorded rather than moved: the GStreamer-style caps keys the parser reads (format, width, height, framerate) and the pixel format names of the KWin node (BGRA, BGRx) are the format of that string and the vocabulary of the protocol, the command line flags and the help and error texts are the wording of a message, and the Wayland interface names the client binds (zkde_screencast_unstable_v1 and wl_output) are the identity of the protocol its generated client implements: pointing the binary at another name would not configure the machine, it would hand the code a protocol its stubs do not speak, which is the class of the proquint table and the DNS header numbers already listed as exceptions. One thing in that area was a duplicate and is now held: the interface name stands in the C source and in the desktop entry, the two must agree or the engine stays without the grant and the capture breaks silently, so tests/test_ffmpeg_setup.py::test_the_desktop_entry_grants_the_interface_the_engine_binds reads the names the C source binds and demands every one of them in the X-KDE-Wayland-Interfaces line of the shipped template; the proof was mutated (renaming the interface in the desktop entry fails exactly that test). Gate green at 2050 tests, ruff and both mypy modes clean, the installer suite at 55, the pre-commit hook at 2 and the commit script at 9. With this the audit covers every file the run reads: the package, the config layer, the shipped bodies under task_data/ and the two scripts under secrets/, and the front of the standing order is closed against everything a reader can point at.


