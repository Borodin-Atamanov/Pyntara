[pytest]
testpaths = tests
pythonpath = src
timeout = 600
timeout_method = signal
markers =
	bootstrap_deep: slow, interactive bootstrap tests that emulate curl|bash and PTY flows
	bootstrap_contract: script-level bootstrap contract tests with mocked apt/git/uv
	bootstrap_slow: heavyweight bootstrap integration tests that may touch local source or full uv sync

